version: 1
source_type: postgresql
generated_by: decoct-infer
subjects:
- name: identity-and-connection
  description: Core identification and basic connection settings for PostgreSQL 
    instances.
  include_paths:
  - port
  - listen_addresses
  - max_connections
  related_paths:
  - path: hostname
    reason: Device identity
  example_questions:
  - What are the listening addresses and port numbers for all PostgreSQL 
    instances?
  - Which instances have a high maximum connection limit?
  - Compare the connection settings across different servers.
- name: logging-configuration
  description: Configuration parameters related to logging behavior and 
    destinations.
  include_paths:
  - log_destination
  - log_error_verbosity
  - log_min_messages
  - log_min_error_statement
  - log_connections
  - log_disconnections
  - log_duration
  - log_hostname
  - log_statement
  - log_lock_waits
  - log_timezone
  - logging_collector
  - log_rotation_age
  - log_rotation_size
  - log_truncate_on_rotation
  - syslog_facility
  - syslog_ident
  - syslog_sequence_numbers
  - syslog_split_messages
  - log_autovacuum_min_duration
  - log_checkpoints
  - log_min_duration_sample
  - log_parameter_max_length
  - log_parameter_max_length_on_error
  - log_recovery_conflict_waits
  - log_replication_commands
  - log_startup_progress_interval
  - log_statement_sample_rate
  - log_temp_files
  - log_transaction_sample_rate
  related_paths:
  - path: hostname
    reason: Device identity
  example_questions:
  - What are the common logging destinations across all PostgreSQL servers?
  - Which instances have detailed statement logging enabled?
  - Are there any instances logging to syslog with different identifies?
- name: autovacuum-settings
  description: Parameters controlling the autovacuum daemon and its behavior.
  include_paths:
  - autovacuum
  - autovacuum_analyze_scale_factor
  - autovacuum_analyze_threshold
  - autovacuum_freeze_max_age
  - autovacuum_multixact_freeze_max_age
  - autovacuum_naptime
  - autovacuum_vacuum_cost_delay
  - autovacuum_vacuum_cost_limit
  - autovacuum_vacuum_insert_scale_factor
  - autovacuum_vacuum_insert_threshold
  - autovacuum_vacuum_scale_factor
  - autovacuum_vacuum_threshold
  - autovacuum_work_mem
  - autovacuum_max_workers
  related_paths:
  - path: hostname
    reason: Device identity
  example_questions:
  - How do `autovacuum_max_workers` settings vary per instance?
  - Are there any instances with significantly different autovacuum thresholds 
    or scale factors?
  - Which instances have autovacuum disabled or configured differently?
- name: memory-and-io-buffers
  description: Critical memory and I/O buffer configurations.
  include_paths:
  - shared_buffers
  - work_mem
  - maintenance_work_mem
  - temp_buffers
  - wal_buffers
  - effective_cache_size
  - vacuum_buffer_usage_limit
  - logical_decoding_work_mem
  related_paths:
  - path: hostname
    reason: Device identity
  example_questions:
  - Compare the `shared_buffers` and `effective_cache_size` settings across the 
    fleet.
  - Which instances have customized `work_mem` or `maintenance_work_mem` values?
  - Identify servers with low `wal_buffers` or `temp_buffers`.
- name: wal-and-replication
  description: Write-Ahead Log (WAL) and replication specific configurations.
  include_paths:
  - wal_level
  - wal_log_hints
  - wal_compression
  - wal_init_zero
  - wal_recycle
  - wal_decode_buffer_size
  - wal_writer_delay
  - wal_writer_flush_after
  - min_wal_size
  - max_wal_size
  - wal_keep_size
  - synchronous_commit
  - max_wal_senders
  - archive_mode
  - archive_command
  - max_replication_slots
  - max_slot_wal_keep_size
  - wal_receiver_create_temp_slot
  - wal_receiver_status_interval
  - wal_receiver_timeout
  - wal_retrieve_retry_interval
  - wal_sender_timeout
  - wal_skip_threshold
  - hot_standby
  - hot_standby_feedback
  - max_standby_archive_delay
  - max_standby_streaming_delay
  - recovery_min_apply_delay
  - recovery_prefetch
  - recovery_target_action
  - recovery_target_inclusive
  - recovery_target_timeline
  - synchronize_commit
  - track_commit_timestamp
  - max_logical_replication_workers
  - max_parallel_apply_workers_per_subscription
  - max_sync_workers_per_subscription
  - summarize_wal
  - sync_replication_slots
  related_paths:
  - path: hostname
    reason: Device identity
  example_questions:
  - Which instances have archive mode enabled and what are their archive 
    commands?
  - Compare `wal_keep_size` and `max_wal_size` settings across all servers.
  - Are there any instances with non-default replication worker configurations?
- name: performance-tuning-costs
  description: Parameters influencing query optimizer cost estimations and 
    parallel processing.
  include_paths:
  - cpu_index_tuple_cost
  - cpu_operator_cost
  - cpu_tuple_cost
  - seq_page_cost
  - random_page_cost
  - cursor_tuple_fraction
  - geqo
  - geqo_effort
  - geqo_generations
  - geqo_pool_size
  - geqo_seed
  - geqo_selection_bias
  - geqo_threshold
  - parallel_leader_participation
  - parallel_setup_cost
  - parallel_tuple_cost
  - min_parallel_index_scan_size
  - min_parallel_table_scan_size
  - max_parallel_workers
  - max_worker_processes
  - max_parallel_workers_per_gather
  - max_parallel_maintenance_workers
  - hash_mem_multiplier
  related_paths:
  - path: hostname
    reason: Device identity
  example_questions:
  - What are the costs for sequential and random page access across instances?
  - Identify servers with custom GEQO settings.
  - Compare the `max_parallel_workers` and `max_parallel_workers_per_gather` 
    configurations.
- name: security-and-authentication
  description: Settings related to security, authentication, and SSL/TLS.
  include_paths:
  - authentication_timeout
  - password_encryption
  - scram_iterations
  - bonjour
  - gss_accept_delegation
  - krb_caseins_users
  - ssl
  - ssl_cert_file
  - ssl_key_file
  - ssl_min_protocol_version
  - ssl_passphrase_command
  - ssl_passphrase_command_supports_reload
  - ssl_prefer_server_ciphers
  related_paths:
  - path: hostname
    reason: Device identity
  example_questions:
  - Which instances have SSL enabled and what are their certificate file paths?
  - Are there any servers with non-default `ssl_min_protocol_version` settings?
  - Compare the `password_encryption` settings across the fleet.
- name: checkpoint-tuning
  description: Parameters controlling checkpoint behavior.
  include_paths:
  - checkpoint_timeout
  - checkpoint_completion_target
  - checkpoint_warning
  - bgwriter_delay
  - bgwriter_lru_maxpages
  - bgwriter_lru_multiplier
  - fsync
  - full_page_writes
  related_paths:
  - path: hostname
    reason: Device identity
  example_questions:
  - Identify instances with short `checkpoint_timeout` values.
  - Are there any servers with customized `checkpoint_completion_target`?
  - Compare `bgwriter` settings, especially `bgwriter_delay`.
- name: advanced-query-optimization
  description: Advanced settings for query planning and execution.
  include_paths:
  - constraint_exclusion
  - from_collapse_limit
  - join_collapse_limit
  - plan_cache_mode
  - default_statistics_target
  - recursive_worktable_factor
  - enable_partitionwise_aggregate
  - enable_partitionwise_join
  - jit
  - jit_above_cost
  - jit_inline_above_cost
  - jit_optimize_above_cost
  related_paths:
  - path: hostname
    reason: Device identity
  example_questions:
  - Which instances have JIT compilation enabled and what are their cost 
    thresholds?
  - Compare the `default_statistics_target` settings across servers.
  - Are there instances that have partition-wise aggregation or join enabled?
- name: system-resource-limits
  description: System-level resource limits configuration.
  include_paths:
  - max_files_per_process
  - max_prepared_transactions
  - max_stack_depth
  - reserved_connections
  - superuser_reserved_connections
  - temp_file_limit
  - huge_pages
  - huge_page_size
  - tcp_keepalives_count
  - tcp_keepalives_idle
  - tcp_keepalives_interval
  - tcp_user_timeout
  - backend_flush_after
  - client_connection_check_interval
  - commit_delay
  - commit_siblings
  related_paths:
  - path: hostname
    reason: Device identity
  example_questions:
  - What are the configured `max_files_per_process` values across the fleet?
  - Do any instances use huge pages, and if so, what size?
  - Compare `reserved_connections` and `superuser_reserved_connections` 
    settings.
