meta:
  subject: rules
  description: Rule file paths for alerting and recording rules
  source_type: prometheus
  total_instances: 3
class_assignments:
  _base_only:
    instances:
    - prometheus-dev
    - prometheus-prod
    - prometheus-staging
