meta:
  entity_type: yaml-app-config
  total_instances: 3
  max_inheritance_depth: 2
  tier_c_ref: yaml-app-config_instances.yaml
base_class:
  app.debug: 'false'
  app.env: prod
  app.name: ridgeline-platform
  logging.file.compress: 'true'
  logging.file.max_backups: '5'
  logging.file.max_size: 100MB
  logging.file.path: /var/log/ridgeline/app.log
  logging.format: json
  logging.handlers: console,file
classes:
  _base_only:
    inherits: base
    own_attrs: {}
    instance_count_inclusive: 3
subclasses: {}
assertions:
  base_only_ratio: 1.0
