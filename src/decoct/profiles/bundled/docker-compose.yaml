name: docker-compose
schema: ../../schemas/bundled/docker-compose.yaml
assertions:
  - ../../assertions/bundled/deployment-standards.yaml
passes:
  strip-secrets:
  strip-comments:
  strip-defaults:
  strip-conformant:
  prune-empty:
  annotate-deviations:
  deviation-summary:
