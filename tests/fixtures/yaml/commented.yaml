# Top-level comment
services:
  web:
    image: nginx:1.25.3  # pinned version
    # Restart policy
    restart: always
    ports:
      - "8080:80"  # HTTP
      - "443:443"  # HTTPS
  db:
    image: postgres:16
    # Database configuration
    environment:
      POSTGRES_DB: myapp
