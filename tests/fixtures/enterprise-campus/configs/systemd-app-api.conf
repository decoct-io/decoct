# Meridian API Service
# Meridian Manufacturing — systemd-app-api

[Unit]
Description=Meridian API Service
After=network.target postgresql.service

[Service]
Type=simple
User=meridian
Group=meridian
ExecStart=/opt/meridian/bin/api-server --port 8080 --config /etc/meridian/api.conf
Restart=always
RestartSec=5
Environment="DB_HOST=10.4.1.11"
Environment="DB_PORT=5432"
Environment="DB_PASS=DB_c0nn_Mrdn_Pk7"
LimitNOFILE=65535

[Install]
WantedBy=multi-user.target
