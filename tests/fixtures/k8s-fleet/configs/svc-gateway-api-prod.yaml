# Kubernetes Service — gateway-api (prod)
# WavePoint Financial — svc-gateway-api-prod
apiVersion: v1
kind: Service
metadata:
  name: gateway-api
  namespace: wavepoint-prod
  labels:
    app: gateway-api
    env: prod
    team: platform
    part-of: wavepoint
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
    service.beta.kubernetes.io/aws-load-balancer-type: nlb
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
    service.beta.kubernetes.io/aws-load-balancer-cross-zone-load-balancing-enabled: "true"
    service.beta.kubernetes.io/aws-load-balancer-ssl-cert: arn:aws:acm:us-east-1:333333333333:certificate/abc-123-def
    service.beta.kubernetes.io/aws-load-balancer-ssl-ports: "443"
spec:
  type: LoadBalancer
  selector:
    app: gateway-api
    env: prod
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  externalTrafficPolicy: Local
  healthCheckNodePort: 8080
  ports:
    - name: http
      protocol: TCP
      port: 443
      targetPort: 8080
    - name: metrics
      protocol: TCP
      port: 9090
      targetPort: 9090
