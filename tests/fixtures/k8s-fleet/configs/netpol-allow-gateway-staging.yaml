# Kubernetes NetworkPolicy — allow-gateway (staging)
# WavePoint Financial — netpol-allow-gateway-staging
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-gateway
  namespace: wavepoint-staging
  labels:
    app.kubernetes.io/name: allow-gateway
    env: staging
    team: platform
    part-of: wavepoint
  annotations:
    networkpolicy.kubernetes.io/managed-by: gitops
spec:
  podSelector:
    matchLabels:
      app: gateway-api
  policyTypes:
    - Ingress
  ingress:
    - from: []
      ports:
        - protocol: TCP
          port: 8080
        - protocol: TCP
          port: 443
