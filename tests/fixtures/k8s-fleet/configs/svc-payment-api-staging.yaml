# Kubernetes Service — payment-api (staging)
# WavePoint Financial — svc-payment-api-staging
apiVersion: v1
kind: Service
metadata:
  name: payment-api
  namespace: wavepoint-staging
  labels:
    app: payment-api
    env: staging
    team: platform
    part-of: wavepoint
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  type: ClusterIP
  selector:
    app: payment-api
    env: staging
  sessionAffinity: None
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
    - name: metrics
      protocol: TCP
      port: 9090
      targetPort: 9090
