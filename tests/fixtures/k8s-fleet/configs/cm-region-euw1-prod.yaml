# Kubernetes ConfigMap — region-euw1 (prod)
# WavePoint Financial — cm-region-euw1-prod
apiVersion: v1
kind: ConfigMap
metadata:
  name: region-euw1
  namespace: wavepoint-prod
  labels:
    app.kubernetes.io/name: region-euw1
    app.kubernetes.io/instance: region-euw1-prod
    env: prod
    team: platform
    part-of: wavepoint
  annotations:
    configmap.kubernetes.io/managed-by: gitops
    wavepoint.io/change-control: required
data:
  AWS_REGION: "eu-west-1"
  AWS_ACCOUNT_ID: "444444444444"
  VPC_CIDR: "10.20.0.0/16"
  CLUSTER_NAME: "wavepoint-prod-euw1"
  SERVICE_MESH: "istio"
  GDPR_MODE: "strict"
