# Kubernetes Service — risk-engine (staging)
# WavePoint Financial — svc-risk-engine-staging
apiVersion: v1
kind: Service
metadata:
  name: risk-engine
  namespace: wavepoint-staging
  labels:
    app: risk-engine
    env: staging
    team: platform
    part-of: wavepoint
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  type: ClusterIP
  selector:
    app: risk-engine
    env: staging
  sessionAffinity: None
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8083
    - name: metrics
      protocol: TCP
      port: 9090
      targetPort: 9090
