# Kubernetes ConfigMap — region-use1 (prod)
# WavePoint Financial — cm-region-use1-prod
apiVersion: v1
kind: ConfigMap
metadata:
  name: region-use1
  namespace: wavepoint-prod
  labels:
    app.kubernetes.io/name: region-use1
    app.kubernetes.io/instance: region-use1-prod
    env: prod
    team: platform
    part-of: wavepoint
  annotations:
    configmap.kubernetes.io/managed-by: gitops
    wavepoint.io/change-control: required
data:
  AWS_REGION: "us-east-1"
  AWS_ACCOUNT_ID: "333333333333"
  VPC_CIDR: "10.10.0.0/16"
  CLUSTER_NAME: "wavepoint-prod-use1"
  SERVICE_MESH: "istio"
