# Kubernetes Service — healthcheck-api (prod)
# WavePoint Financial — svc-healthcheck-api-prod
apiVersion: v1
kind: Service
metadata:
  name: healthcheck-api
  namespace: wavepoint-prod
  labels:
    app: healthcheck-api
    env: prod
    team: platform
    part-of: wavepoint
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  type: ClusterIP
  selector:
    app: healthcheck-api
    env: prod
  sessionAffinity: None
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8090
    - name: metrics
      protocol: TCP
      port: 9090
      targetPort: 9090
