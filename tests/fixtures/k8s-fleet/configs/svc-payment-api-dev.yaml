# Kubernetes Service — payment-api (dev)
# WavePoint Financial — svc-payment-api-dev
apiVersion: v1
kind: Service
metadata:
  name: payment-api
  namespace: wavepoint-dev
  labels:
    app: payment-api
    env: dev
    team: platform
    part-of: wavepoint
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  type: ClusterIP
  selector:
    app: payment-api
    env: dev
  sessionAffinity: None
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
