service_config:
  service_policy:
    interval: 30
    retry: 3
    telemetry:
      interval: 30
      protocol: grpc
      encoding: gpb
      collectors:
      - 10.0.0.1
      - 10.0.0.2
export_policy:
  service_policy:
    streaming:
      interval: 30
      protocol: grpc
      encoding: gpb
