# Prometheus Configuration — nrt
# ArcticCache CDN — prometheus-nrt

global:
  scrape_interval: 15s
  scrape_timeout: 10s
  evaluation_interval: 15s
  external_labels:
    pop: nrt
    provider: arcticcache

rule_files:
  - /etc/prometheus/rules/alerts.yaml
  - /etc/prometheus/rules/recording.yaml

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - localhost:9093
      scheme: http
      timeout: 10s

scrape_configs:
  - job_name: prometheus
    scrape_interval: 15s
    static_configs:
      - targets:
          - localhost:9090

  - job_name: nginx
    scrape_interval: 15s
    scrape_timeout: 10s
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - "10.6.1.10:9113"
        labels:
          pop: nrt
          service: nginx

  - job_name: haproxy
    scrape_interval: 15s
    scrape_timeout: 10s
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - "10.6.1.15:8404"
        labels:
          pop: nrt
          service: haproxy

  - job_name: varnish
    scrape_interval: 15s
    scrape_timeout: 10s
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - "10.6.1.11:9131"
        labels:
          pop: nrt
          service: varnish

  - job_name: node
    scrape_interval: 15s
    scrape_timeout: 10s
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - "10.6.1.10:9100"
        labels:
          pop: nrt
          service: node

  - job_name: node
    scrape_interval: 15s
    scrape_timeout: 10s
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - "10.6.1.11:9100"
        labels:
          pop: nrt
          service: node

  - job_name: keepalived
    scrape_interval: 15s
    scrape_timeout: 10s
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - "10.6.1.10:9165"
        labels:
          pop: nrt
          service: keepalived

  - job_name: compliance
    scrape_interval: 15s
    scrape_timeout: 10s
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - "10.6.1.30:9200"
        labels:
          pop: nrt
          service: compliance

remote_write:
  - url: https://metrics.arcticcache.net/api/v1/write
    remote_timeout: 30s
    authorization:
      type: Bearer
      credentials: prom_tk_nrt_Pq3rS7vW
    queue_config:
      capacity: 10000
      max_shards: 50
      min_shards: 1
      max_samples_per_send: 2000
      batch_send_deadline: 5s

# Alert thresholds
# cpu_threshold: 80
# cache_hit_threshold: 90
# latency_threshold_ms: 120
# retention_days: 90
