; DNS Zone file — lhr
; ArcticCache CDN — dns-lhr
; Zone: lhr.arcticcache.net

; TSIG key for zone transfers
; key tsig_lhr_Mn4wQ8jR2vN7xYz1

$ORIGIN lhr.arcticcache.net.
$TTL 300

; SOA Record
@ IN SOA ns1.lhr.arcticcache.net. admin.arcticcache.net. (
    2024030801 ; serial
    3600  ; refresh
    900   ; retry
    604800 ; expire
    300  ; minimum TTL
)

; NS Records
@ IN NS ns1.lhr.arcticcache.net.
ns1 IN A 10.3.1.2
@ IN NS ns2.lhr.arcticcache.net.
ns2 IN A 10.3.1.3

; A Records
edge IN A 10.3.1.10
cache IN A 10.3.1.11
api IN A 10.3.1.12
mon IN A 10.3.1.20

; CNAME Records
legacy-cdn IN CNAME edge.lhr.arcticcache.net.
old-api IN CNAME api.lhr.arcticcache.net.
