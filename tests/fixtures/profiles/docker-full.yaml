name: docker-compose-full
schema: ../schemas/docker-compose-full.yaml
assertions:
  - ../assertions/deployment-standards.yaml
passes:
  strip-secrets:
  strip-comments:
  strip-defaults:
  strip-conformant:
  prune-empty:
  annotate-deviations:
  deviation-summary:
