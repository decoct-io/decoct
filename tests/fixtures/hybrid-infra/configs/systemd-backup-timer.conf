# Ridgeline DB Backup Timer
# Ridgeline Data — systemd-backup-timer

[Unit]
Description=Ridgeline DB Backup Timer

[Timer]
OnCalendar=*-*-* 03:00:00
Persistent=true
Unit=db-backup.service

[Install]
WantedBy=timers.target
