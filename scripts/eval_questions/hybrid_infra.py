"""Hand-authored evaluation questions for the hybrid-infra source (Ridgeline Data).

600 total questions across 5 categories, 120 each.
Difficulty distribution per function: 48 easy, 48 medium, 24 hard.
"""

from decoct.qa.eval_models import Difficulty, EvalQuestion, EvalQuestionClass


def factual_retrieval() -> list[EvalQuestion]:
    """Return 120 FACTUAL_RETRIEVAL questions (prefix FR)."""
    return [
        # ── EASY (FR-001 .. FR-048) ──────────────────────────────────────────────
        EvalQuestion(
            id="FR-001",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the shared_buffers setting in the PostgreSQL primary config?",
            reference_answer="2GB",
            evidence_locations=["pg-prod-primary.conf -> shared_buffers"],
            reasoning_required="Direct lookup of PostgreSQL parameter",
        ),
        EvalQuestion(
            id="FR-002",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What port does the sshd bastion server listen on?",
            reference_answer="2222",
            evidence_locations=["sshd-bastion.conf -> Port"],
            reasoning_required="Direct lookup of sshd parameter",
        ),
        EvalQuestion(
            id="FR-003",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the max_connections setting in the MariaDB Galera node config?",
            reference_answer="300",
            evidence_locations=["maria-galera-node.cnf -> max_connections"],
            reasoning_required="Direct lookup of MariaDB parameter",
        ),
        EvalQuestion(
            id="FR-004",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What image tag is used for core-api in compose-app-prod-01.yaml?",
            reference_answer="2.1.0",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.image"],
            reasoning_required="Direct lookup of Docker Compose image tag",
        ),
        EvalQuestion(
            id="FR-005",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the scrape_interval in the Prometheus prod config?",
            reference_answer="15s",
            evidence_locations=["prometheus-prod.yaml -> global.scrape_interval"],
            reasoning_required="Direct lookup of Prometheus global setting",
        ),
        EvalQuestion(
            id="FR-006",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the character_set_server in maria-legacy-main.cnf?",
            reference_answer="latin1",
            evidence_locations=["maria-legacy-main.cnf -> character_set_server"],
            reasoning_required="Direct lookup of MariaDB parameter",
        ),
        EvalQuestion(
            id="FR-007",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the restart policy for core-api in compose-app-prod-01.yaml?",
            reference_answer="always",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.restart"],
            reasoning_required="Direct lookup of Docker Compose restart policy",
        ),
        EvalQuestion(
            id="FR-008",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the log level in the Traefik prod-static config?",
            reference_answer="ERROR",
            evidence_locations=["traefik-prod-static.yaml -> log.level"],
            reasoning_required="Direct lookup of Traefik log level",
        ),
        EvalQuestion(
            id="FR-009",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the work_mem setting in pg-prod-analytics.conf?",
            reference_answer="256MB",
            evidence_locations=["pg-prod-analytics.conf -> work_mem"],
            reasoning_required="Direct lookup of PostgreSQL parameter",
        ),
        EvalQuestion(
            id="FR-010",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the PermitRootLogin setting in sshd-dev.conf?",
            reference_answer="prohibit-password",
            evidence_locations=["sshd-dev.conf -> PermitRootLogin"],
            reasoning_required="Direct lookup of sshd parameter",
        ),
        EvalQuestion(
            id="FR-011",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the innodb_buffer_pool_size in maria-legacy-billing.cnf?",
            reference_answer="1G",
            evidence_locations=["maria-legacy-billing.cnf -> innodb_buffer_pool_size"],
            reasoning_required="Direct lookup of MariaDB parameter",
        ),
        EvalQuestion(
            id="FR-012",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the mem_limit for core-api in compose-app-prod-01.yaml?",
            reference_answer="2g",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.mem_limit"],
            reasoning_required="Direct lookup of Docker Compose resource limit",
        ),
        EvalQuestion(
            id="FR-013",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What region is specified in tfvars-prod.json?",
            reference_answer="eu-central-1",
            evidence_locations=["tfvars-prod.json -> region"],
            reasoning_required="Direct lookup of Terraform variable",
        ),
        EvalQuestion(
            id="FR-014",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the jwt_expiry_seconds in app-config-auth-jwt.json?",
            reference_answer="3600",
            evidence_locations=["app-config-auth-jwt.json -> auth.jwt_expiry_seconds"],
            reasoning_required="Direct lookup of app config parameter",
        ),
        EvalQuestion(
            id="FR-015",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the vm.swappiness setting in sysctl-db-tuned.conf?",
            reference_answer="10",
            evidence_locations=["sysctl-db-tuned.conf -> vm.swappiness"],
            reasoning_required="Direct lookup of sysctl parameter",
        ),
        EvalQuestion(
            id="FR-016",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the LogLevel in sshd-prod.conf?",
            reference_answer="VERBOSE",
            evidence_locations=["sshd-prod.conf -> LogLevel"],
            reasoning_required="Direct lookup of sshd parameter",
        ),
        EvalQuestion(
            id="FR-017",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the max_connections in pg-prod-primary.conf?",
            reference_answer="200",
            evidence_locations=["pg-prod-primary.conf -> max_connections"],
            reasoning_required="Direct lookup of PostgreSQL parameter",
        ),
        EvalQuestion(
            id="FR-018",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the restart policy for services in compose-ci.yaml?",
            reference_answer="no",
            evidence_locations=["compose-ci.yaml -> services.core-api-test.restart"],
            reasoning_required="Direct lookup of Docker Compose restart policy",
        ),
        EvalQuestion(
            id="FR-019",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the app_instance_type in tfvars-prod.json?",
            reference_answer="t3.xlarge",
            evidence_locations=["tfvars-prod.json -> compute.app_instance_type"],
            reasoning_required="Direct lookup of Terraform variable",
        ),
        EvalQuestion(
            id="FR-020",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the archive_mode setting in pg-prod-primary.conf?",
            reference_answer="on",
            evidence_locations=["pg-prod-primary.conf -> archive_mode"],
            reasoning_required="Direct lookup of PostgreSQL parameter",
        ),
        EvalQuestion(
            id="FR-021",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the Restart policy in systemd-core-api.conf?",
            reference_answer="on-failure",
            evidence_locations=["systemd-core-api.conf -> [Service].Restart"],
            reasoning_required="Direct lookup of systemd parameter",
        ),
        EvalQuestion(
            id="FR-022",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the net.core.somaxconn value in sysctl-db-tuned.conf?",
            reference_answer="65535",
            evidence_locations=["sysctl-db-tuned.conf -> net.core.somaxconn"],
            reasoning_required="Direct lookup of sysctl parameter",
        ),
        EvalQuestion(
            id="FR-023",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the ssl setting in pg-prod-replica.conf?",
            reference_answer="off",
            evidence_locations=["pg-prod-replica.conf -> ssl"],
            reasoning_required="Direct lookup of PostgreSQL parameter",
        ),
        EvalQuestion(
            id="FR-024",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the log_statement setting in pg-dev.conf?",
            reference_answer="all",
            evidence_locations=["pg-dev.conf -> log_statement"],
            reasoning_required="Direct lookup of PostgreSQL parameter",
        ),
        EvalQuestion(
            id="FR-025",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the requests_per_minute rate limit in app-config-auth-jwt.json?",
            reference_answer="1000",
            evidence_locations=["app-config-auth-jwt.json -> rate_limiting.requests_per_minute"],
            reasoning_required="Direct lookup of app config parameter",
        ),
        EvalQuestion(
            id="FR-026",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the MaxAuthTries setting in sshd-prod.conf?",
            reference_answer="3",
            evidence_locations=["sshd-prod.conf -> MaxAuthTries"],
            reasoning_required="Direct lookup of sshd parameter",
        ),
        EvalQuestion(
            id="FR-027",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the shared_buffers setting in pg-prod-replica.conf?",
            reference_answer="256MB",
            evidence_locations=["pg-prod-replica.conf -> shared_buffers"],
            reasoning_required="Direct lookup of PostgreSQL parameter",
        ),
        EvalQuestion(
            id="FR-028",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the become setting in the ansible-base-server.yaml playbook?",
            reference_answer="true",
            evidence_locations=["ansible-base-server.yaml -> become"],
            reasoning_required="Direct lookup of Ansible playbook parameter",
        ),
        EvalQuestion(
            id="FR-029",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the slow_query_log setting in maria-legacy-main.cnf?",
            reference_answer="OFF",
            evidence_locations=["maria-legacy-main.cnf -> slow_query_log"],
            reasoning_required="Direct lookup of MariaDB parameter",
        ),
        EvalQuestion(
            id="FR-030",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the MaxSessions setting in sshd-bastion.conf?",
            reference_answer="1",
            evidence_locations=["sshd-bastion.conf -> MaxSessions"],
            reasoning_required="Direct lookup of sshd parameter",
        ),
        EvalQuestion(
            id="FR-031",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What image is used for the CI test service in compose-ci.yaml?",
            reference_answer="ridgeline/core-api:${CI_COMMIT_SHA}",
            evidence_locations=["compose-ci.yaml -> services.core-api-test.image"],
            reasoning_required="Direct lookup of Docker Compose image",
        ),
        EvalQuestion(
            id="FR-032",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the PasswordAuthentication setting in sshd-prod.conf?",
            reference_answer="no",
            evidence_locations=["sshd-prod.conf -> PasswordAuthentication"],
            reasoning_required="Direct lookup of sshd parameter",
        ),
        EvalQuestion(
            id="FR-033",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the effective_cache_size in pg-prod-analytics.conf?",
            reference_answer="48GB",
            evidence_locations=["pg-prod-analytics.conf -> effective_cache_size"],
            reasoning_required="Direct lookup of PostgreSQL parameter",
        ),
        EvalQuestion(
            id="FR-034",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the scrape_interval in prometheus-staging.yaml?",
            reference_answer="30s",
            evidence_locations=["prometheus-staging.yaml -> global.scrape_interval"],
            reasoning_required="Direct lookup of Prometheus parameter",
        ),
        EvalQuestion(
            id="FR-035",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the Type setting in systemd-backup-svc.conf?",
            reference_answer="oneshot",
            evidence_locations=["systemd-backup-svc.conf -> [Service].Type"],
            reasoning_required="Direct lookup of systemd parameter",
        ),
        EvalQuestion(
            id="FR-036",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the backup_retention_days in tfvars-prod.json?",
            reference_answer="30",
            evidence_locations=["tfvars-prod.json -> features.backup_retention_days"],
            reasoning_required="Direct lookup of Terraform variable",
        ),
        EvalQuestion(
            id="FR-037",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the database host in app-config-auth-jwt.json?",
            reference_answer="db-prod-01",
            evidence_locations=["app-config-auth-jwt.json -> database.host"],
            reasoning_required="Direct lookup of app config parameter",
        ),
        EvalQuestion(
            id="FR-038",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the storage-driver in docker-daemon-prod.json?",
            reference_answer="overlay2",
            evidence_locations=["docker-daemon-prod.json -> storage-driver"],
            reasoning_required="Direct lookup of Docker daemon parameter",
        ),
        EvalQuestion(
            id="FR-039",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the innodb_flush_log_at_trx_commit in maria-galera-node.cnf?",
            reference_answer="2",
            evidence_locations=["maria-galera-node.cnf -> innodb_flush_log_at_trx_commit"],
            reasoning_required="Direct lookup of MariaDB parameter",
        ),
        EvalQuestion(
            id="FR-040",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the AllowTcpForwarding setting in sshd-prod.conf?",
            reference_answer="no",
            evidence_locations=["sshd-prod.conf -> AllowTcpForwarding"],
            reasoning_required="Direct lookup of sshd parameter",
        ),
        EvalQuestion(
            id="FR-041",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What version of Next.js is the web-app using according to package-json-web-app.json?",
            reference_answer="^14.1.0",
            evidence_locations=["package-json-web-app.json -> dependencies.next"],
            reasoning_required="Direct lookup of package.json dependency",
        ),
        EvalQuestion(
            id="FR-042",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the listen_addresses setting in pg-migration.conf?",
            reference_answer="localhost",
            evidence_locations=["pg-migration.conf -> listen_addresses"],
            reasoning_required="Direct lookup of PostgreSQL parameter",
        ),
        EvalQuestion(
            id="FR-043",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the Restart setting in systemd-legacy-dashboard.conf?",
            reference_answer="always",
            evidence_locations=["systemd-legacy-dashboard.conf -> [Service].Restart"],
            reasoning_required="Direct lookup of systemd parameter",
        ),
        EvalQuestion(
            id="FR-044",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the api.insecure setting in traefik-staging.yaml?",
            reference_answer="true",
            evidence_locations=["traefik-staging.yaml -> api.insecure"],
            reasoning_required="Direct lookup of Traefik parameter",
        ),
        EvalQuestion(
            id="FR-045",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the max_connections setting in pg-prod-analytics.conf?",
            reference_answer="50",
            evidence_locations=["pg-prod-analytics.conf -> max_connections"],
            reasoning_required="Direct lookup of PostgreSQL parameter",
        ),
        EvalQuestion(
            id="FR-046",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the X11Forwarding setting in sshd-dev.conf?",
            reference_answer="yes",
            evidence_locations=["sshd-dev.conf -> X11Forwarding"],
            reasoning_required="Direct lookup of sshd parameter",
        ),
        EvalQuestion(
            id="FR-047",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the app_instance_count in tfvars-prod.json?",
            reference_answer="3",
            evidence_locations=["tfvars-prod.json -> compute.app_instance_count"],
            reasoning_required="Direct lookup of Terraform variable",
        ),
        EvalQuestion(
            id="FR-048",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.EASY,
            question="What is the OnCalendar schedule in systemd-backup-timer.conf?",
            reference_answer="*-*-* 03:00:00",
            evidence_locations=["systemd-backup-timer.conf -> [Timer].OnCalendar"],
            reasoning_required="Direct lookup of systemd timer parameter",
        ),
        # ── MEDIUM (FR-049 .. FR-096) ────────────────────────────────────────────
        EvalQuestion(
            id="FR-049",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What users are allowed SSH access on the production sshd config?",
            reference_answer="deploy ops admins",
            evidence_locations=["sshd-prod.conf -> AllowUsers"],
            reasoning_required="Lookup of access control list in sshd config",
        ),
        EvalQuestion(
            id="FR-050",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the database port used by the billing app config?",
            reference_answer="3306",
            evidence_locations=["app-config-billing.json -> database.port"],
            reasoning_required="Lookup of database connection details in app config",
        ),
        EvalQuestion(
            id="FR-051",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="How many networks are defined in compose-app-prod-01.yaml?",
            reference_answer="2 (app-network and monitoring)",
            evidence_locations=["compose-app-prod-01.yaml -> networks"],
            reasoning_required="Count of network definitions in Docker Compose",
        ),
        EvalQuestion(
            id="FR-052",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the checkpoint_timeout in pg-prod-primary.conf?",
            reference_answer="15min",
            evidence_locations=["pg-prod-primary.conf -> checkpoint_timeout"],
            reasoning_required="Lookup of PostgreSQL WAL parameter",
        ),
        EvalQuestion(
            id="FR-053",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the collation_server in maria-legacy-main.cnf?",
            reference_answer="latin1_swedish_ci",
            evidence_locations=["maria-legacy-main.cnf -> collation_server"],
            reasoning_required="Lookup of MariaDB character set configuration",
        ),
        EvalQuestion(
            id="FR-054",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the remote_write URL in prometheus-prod.yaml?",
            reference_answer="http://remote-write.ridgeline.io/api/v1/write",
            evidence_locations=["prometheus-prod.yaml -> remote_write[0].url"],
            reasoning_required="Lookup of Prometheus remote write configuration",
        ),
        EvalQuestion(
            id="FR-055",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the server_id of the MariaDB Galera node?",
            reference_answer="100",
            evidence_locations=["maria-galera-node.cnf -> server_id"],
            reasoning_required="Lookup of MariaDB replication parameter",
        ),
        EvalQuestion(
            id="FR-056",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the long_query_time in maria-legacy-billing.cnf?",
            reference_answer="2",
            evidence_locations=["maria-legacy-billing.cnf -> long_query_time"],
            reasoning_required="Lookup of MariaDB slow query parameter",
        ),
        EvalQuestion(
            id="FR-057",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What hosts are in the app_servers group in ansible-inventory-prod.yaml?",
            reference_answer="app-prod-01, app-prod-02, app-prod-03",
            evidence_locations=["ansible-inventory-prod.yaml -> all.children.app_servers.hosts"],
            reasoning_required="Lookup of Ansible inventory group membership",
        ),
        EvalQuestion(
            id="FR-058",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the max_wal_size in pg-prod-analytics.conf?",
            reference_answer="8GB",
            evidence_locations=["pg-prod-analytics.conf -> max_wal_size"],
            reasoning_required="Lookup of PostgreSQL WAL parameter",
        ),
        EvalQuestion(
            id="FR-059",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the VPC CIDR in tfvars-prod.json?",
            reference_answer="10.0.0.0/16",
            evidence_locations=["tfvars-prod.json -> networking.vpc_cidr"],
            reasoning_required="Lookup of Terraform networking variable",
        ),
        EvalQuestion(
            id="FR-060",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the autovacuum setting in pg-migration.conf?",
            reference_answer="off",
            evidence_locations=["pg-migration.conf -> autovacuum"],
            reasoning_required="Lookup of PostgreSQL autovacuum parameter",
        ),
        EvalQuestion(
            id="FR-061",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the database host used by app-config-billing.json?",
            reference_answer="db-legacy-01",
            evidence_locations=["app-config-billing.json -> database.host"],
            reasoning_required="Lookup of app config database connection",
        ),
        EvalQuestion(
            id="FR-062",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the auth-service image tag in compose-staging.yaml?",
            reference_answer="1.8.0-rc1",
            evidence_locations=["compose-staging.yaml -> services.auth-service.image"],
            reasoning_required="Lookup of Docker Compose image tag",
        ),
        EvalQuestion(
            id="FR-063",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the max_parallel_workers_per_gather in pg-prod-primary.conf?",
            reference_answer="4",
            evidence_locations=["pg-prod-primary.conf -> max_parallel_workers_per_gather"],
            reasoning_required="Lookup of PostgreSQL parallelism parameter",
        ),
        EvalQuestion(
            id="FR-064",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the domain set in tfvars-staging.json?",
            reference_answer="staging.ridgeline.io",
            evidence_locations=["tfvars-staging.json -> networking.domain"],
            reasoning_required="Lookup of Terraform networking variable",
        ),
        EvalQuestion(
            id="FR-065",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the serversTransport.insecureSkipVerify setting in traefik-prod-static.yaml?",
            reference_answer="false",
            evidence_locations=["traefik-prod-static.yaml -> serversTransport.insecureSkipVerify"],
            reasoning_required="Lookup of Traefik transport security setting",
        ),
        EvalQuestion(
            id="FR-066",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the maintenance_work_mem in pg-prod-primary.conf?",
            reference_answer="512MB",
            evidence_locations=["pg-prod-primary.conf -> maintenance_work_mem"],
            reasoning_required="Lookup of PostgreSQL memory parameter",
        ),
        EvalQuestion(
            id="FR-067",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the log-opts max-size in docker-daemon-prod.json?",
            reference_answer="50m",
            evidence_locations=["docker-daemon-prod.json -> log-opts.max-size"],
            reasoning_required="Lookup of Docker daemon logging configuration",
        ),
        EvalQuestion(
            id="FR-068",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What users are allowed SSH access on the bastion host?",
            reference_answer="deploy ops",
            evidence_locations=["sshd-bastion.conf -> AllowUsers"],
            reasoning_required="Lookup of sshd access control",
        ),
        EvalQuestion(
            id="FR-069",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the default_statistics_target in pg-prod-analytics.conf?",
            reference_answer="500",
            evidence_locations=["pg-prod-analytics.conf -> default_statistics_target"],
            reasoning_required="Lookup of PostgreSQL planner parameter",
        ),
        EvalQuestion(
            id="FR-070",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the app_version variable defined in ansible-deploy-core-api.yaml?",
            reference_answer="2.1.0",
            evidence_locations=["ansible-deploy-core-api.yaml -> vars.app_version"],
            reasoning_required="Lookup of Ansible playbook variable",
        ),
        EvalQuestion(
            id="FR-071",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What timezone is set in the cloud-init-app-server.yaml config?",
            reference_answer="America/New_York",
            evidence_locations=["cloud-init-app-server.yaml -> timezone"],
            reasoning_required="Lookup of cloud-init configuration",
        ),
        EvalQuestion(
            id="FR-072",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the log_min_duration_statement in pg-prod-primary.conf?",
            reference_answer="500",
            evidence_locations=["pg-prod-primary.conf -> log_min_duration_statement"],
            reasoning_required="Lookup of PostgreSQL logging parameter",
        ),
        EvalQuestion(
            id="FR-073",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the db_instance_type in tfvars-prod.json?",
            reference_answer="r6g.2xlarge",
            evidence_locations=["tfvars-prod.json -> compute.db_instance_type"],
            reasoning_required="Lookup of Terraform compute variable",
        ),
        EvalQuestion(
            id="FR-074",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the synchronous_commit setting in pg-migration.conf?",
            reference_answer="off",
            evidence_locations=["pg-migration.conf -> synchronous_commit"],
            reasoning_required="Lookup of PostgreSQL WAL parameter",
        ),
        EvalQuestion(
            id="FR-075",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the kernel.shmmax value in sysctl-db-tuned.conf?",
            reference_answer="68719476736",
            evidence_locations=["sysctl-db-tuned.conf -> kernel.shmmax"],
            reasoning_required="Lookup of sysctl kernel parameter",
        ),
        EvalQuestion(
            id="FR-076",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the ClientAliveInterval in sshd-bastion.conf?",
            reference_answer="120",
            evidence_locations=["sshd-bastion.conf -> ClientAliveInterval"],
            reasoning_required="Lookup of sshd keepalive parameter",
        ),
        EvalQuestion(
            id="FR-077",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the requests_per_minute rate limit in app-config-search.json?",
            reference_answer="2000",
            evidence_locations=["app-config-search.json -> rate_limiting.requests_per_minute"],
            reasoning_required="Lookup of app config rate limit",
        ),
        EvalQuestion(
            id="FR-078",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What host is in the legacy_db_servers group in the prod inventory?",
            reference_answer="db-legacy-01",
            evidence_locations=["ansible-inventory-prod.yaml -> all.children.legacy_db_servers.hosts"],
            reasoning_required="Lookup of Ansible inventory group membership",
        ),
        EvalQuestion(
            id="FR-079",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the huge_pages setting in pg-prod-analytics.conf?",
            reference_answer="on",
            evidence_locations=["pg-prod-analytics.conf -> huge_pages"],
            reasoning_required="Lookup of PostgreSQL memory parameter",
        ),
        EvalQuestion(
            id="FR-080",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the log_timezone in pg-prod-primary.conf?",
            reference_answer="America/New_York",
            evidence_locations=["pg-prod-primary.conf -> log_timezone"],
            reasoning_required="Lookup of PostgreSQL logging parameter",
        ),
        EvalQuestion(
            id="FR-081",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the mem_limit for core-api in compose-staging.yaml?",
            reference_answer="1g",
            evidence_locations=["compose-staging.yaml -> services.core-api.mem_limit"],
            reasoning_required="Lookup of Docker Compose resource limit",
        ),
        EvalQuestion(
            id="FR-082",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the innodb_io_capacity in maria-galera-node.cnf?",
            reference_answer="500",
            evidence_locations=["maria-galera-node.cnf -> innodb_io_capacity"],
            reasoning_required="Lookup of MariaDB InnoDB parameter",
        ),
        EvalQuestion(
            id="FR-083",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the ansible_user for all hosts in the prod inventory?",
            reference_answer="deploy",
            evidence_locations=["ansible-inventory-prod.yaml -> all.vars.ansible_user"],
            reasoning_required="Lookup of Ansible inventory variable",
        ),
        EvalQuestion(
            id="FR-084",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the ExecStart command in systemd-core-api.conf?",
            reference_answer="/opt/ridgeline/core-api/venv/bin/uvicorn core_api.main:app --host 0.0.0.0 --port 8000",
            evidence_locations=["systemd-core-api.conf -> [Service].ExecStart"],
            reasoning_required="Lookup of systemd service command",
        ),
        EvalQuestion(
            id="FR-085",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the experimental setting in docker-daemon-dev.json?",
            reference_answer="true",
            evidence_locations=["docker-daemon-dev.json -> experimental"],
            reasoning_required="Lookup of Docker daemon configuration",
        ),
        EvalQuestion(
            id="FR-086",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the max_connections in pg-migration.conf?",
            reference_answer="20",
            evidence_locations=["pg-migration.conf -> max_connections"],
            reasoning_required="Lookup of PostgreSQL connection parameter",
        ),
        EvalQuestion(
            id="FR-087",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the autovacuum_max_workers in pg-prod-primary.conf?",
            reference_answer="5",
            evidence_locations=["pg-prod-primary.conf -> autovacuum_max_workers"],
            reasoning_required="Lookup of PostgreSQL autovacuum parameter",
        ),
        EvalQuestion(
            id="FR-088",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the tls.options.default.minVersion in traefik-prod-static.yaml?",
            reference_answer="VersionTLS12",
            evidence_locations=["traefik-prod-static.yaml -> tls.options.default.minVersion"],
            reasoning_required="Lookup of Traefik TLS configuration",
        ),
        EvalQuestion(
            id="FR-089",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the fs.file-max value in sysctl-db-tuned.conf?",
            reference_answer="2097152",
            evidence_locations=["sysctl-db-tuned.conf -> fs.file-max"],
            reasoning_required="Lookup of sysctl file system parameter",
        ),
        EvalQuestion(
            id="FR-090",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the log_statement setting in pg-migration.conf?",
            reference_answer="ddl",
            evidence_locations=["pg-migration.conf -> log_statement"],
            reasoning_required="Lookup of PostgreSQL logging parameter",
        ),
        EvalQuestion(
            id="FR-091",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the sync_binlog setting in maria-galera-node.cnf?",
            reference_answer="0",
            evidence_locations=["maria-galera-node.cnf -> sync_binlog"],
            reasoning_required="Lookup of MariaDB replication parameter",
        ),
        EvalQuestion(
            id="FR-092",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the web-app version in package-json-web-app.json?",
            reference_answer="3.0.0",
            evidence_locations=["package-json-web-app.json -> version"],
            reasoning_required="Lookup of package.json version field",
        ),
        EvalQuestion(
            id="FR-093",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the max_wal_senders in pg-migration.conf?",
            reference_answer="0",
            evidence_locations=["pg-migration.conf -> max_wal_senders"],
            reasoning_required="Lookup of PostgreSQL replication parameter",
        ),
        EvalQuestion(
            id="FR-094",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the alertmanager target in prometheus-prod.yaml?",
            reference_answer="mon-prod-01:9093",
            evidence_locations=["prometheus-prod.yaml -> alerting.alertmanagers[0].static_configs[0].targets"],
            reasoning_required="Lookup of Prometheus alerting configuration",
        ),
        EvalQuestion(
            id="FR-095",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the managed_by tag in tfvars-prod.json?",
            reference_answer="opentofu",
            evidence_locations=["tfvars-prod.json -> tags.managed_by"],
            reasoning_required="Lookup of Terraform tags",
        ),
        EvalQuestion(
            id="FR-096",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.MEDIUM,
            question="What is the log_error_verbosity in maria-dev.cnf?",
            reference_answer="3",
            evidence_locations=["maria-dev.cnf -> log_error_verbosity"],
            reasoning_required="Lookup of MariaDB logging parameter",
        ),
        # ── HARD (FR-097 .. FR-120) ──────────────────────────────────────────────
        EvalQuestion(
            id="FR-097",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="How many scrape job targets are defined in prometheus-prod.yaml?",
            reference_answer="15 (prometheus, core-api, auth-service, billing-api, web-app, notification-svc, search-svc, admin-panel, legacy-dashboard, analytics-worker, postgres-primary, postgres-replica, postgres-analytics, mariadb, traefik, node-exporter)",
            evidence_locations=["prometheus-prod.yaml -> scrape_configs"],
            reasoning_required="Count all scrape_configs entries in Prometheus config",
        ),
        EvalQuestion(
            id="FR-098",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What is the archive_command in pg-prod-primary.conf?",
            reference_answer="cp %p /archive/%f",
            evidence_locations=["pg-prod-primary.conf -> archive_command"],
            reasoning_required="Lookup of PostgreSQL archiving command",
        ),
        EvalQuestion(
            id="FR-099",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What is the complete list of hosts in the db_servers group in the prod inventory?",
            reference_answer="db-prod-01, db-prod-02, db-prod-03",
            evidence_locations=["ansible-inventory-prod.yaml -> all.children.db_servers.hosts"],
            reasoning_required="Lookup of Ansible inventory group membership",
        ),
        EvalQuestion(
            id="FR-100",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What is the password_encryption method used in pg-prod-primary.conf?",
            reference_answer="scram-sha-256",
            evidence_locations=["pg-prod-primary.conf -> password_encryption"],
            reasoning_required="Lookup of PostgreSQL authentication method",
        ),
        EvalQuestion(
            id="FR-101",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What packages are installed by the cloud-init-app-server.yaml config?",
            reference_answer="curl, wget, htop, vim, net-tools, python3-pip, docker-ce, docker-compose-plugin",
            evidence_locations=["cloud-init-app-server.yaml -> packages"],
            reasoning_required="Lookup of cloud-init package list",
        ),
        EvalQuestion(
            id="FR-102",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What is the random_page_cost in pg-prod-analytics.conf versus pg-prod-replica.conf?",
            reference_answer="pg-prod-analytics.conf: 1.1, pg-prod-replica.conf: 4.0",
            evidence_locations=["pg-prod-analytics.conf -> random_page_cost", "pg-prod-replica.conf -> random_page_cost"],
            reasoning_required="Lookup and report two PostgreSQL planner parameters",
        ),
        EvalQuestion(
            id="FR-103",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What DNS servers are configured in the prod Ansible inventory?",
            reference_answer="8.8.8.8 and 8.8.4.4",
            evidence_locations=["ansible-inventory-prod.yaml -> all.vars.dns_servers"],
            reasoning_required="Lookup of Ansible inventory variable",
        ),
        EvalQuestion(
            id="FR-104",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What is the hot_standby setting in pg-migration.conf?",
            reference_answer="off",
            evidence_locations=["pg-migration.conf -> hot_standby"],
            reasoning_required="Lookup of PostgreSQL replication parameter on migration instance",
        ),
        EvalQuestion(
            id="FR-105",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="How many subnet CIDRs are in tfvars-prod.json?",
            reference_answer="3 (10.0.1.0/24, 10.0.2.0/24, 10.0.3.0/24)",
            evidence_locations=["tfvars-prod.json -> networking.subnet_cidrs"],
            reasoning_required="Count of Terraform networking subnets",
        ),
        EvalQuestion(
            id="FR-106",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What is the innodb_change_buffering setting in maria-galera-node.cnf?",
            reference_answer="none",
            evidence_locations=["maria-galera-node.cnf -> innodb_change_buffering"],
            reasoning_required="Lookup of MariaDB InnoDB parameter specific to Galera",
        ),
        EvalQuestion(
            id="FR-107",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What are the CORS origins allowed in app-config-auth-jwt.json?",
            reference_answer="https://app.ridgeline.io and https://admin.ridgeline.io",
            evidence_locations=["app-config-auth-jwt.json -> cors.origins"],
            reasoning_required="Lookup of CORS origin list in app config",
        ),
        EvalQuestion(
            id="FR-108",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What is the scrape_interval in prometheus-dev.yaml?",
            reference_answer="1m",
            evidence_locations=["prometheus-dev.yaml -> global.scrape_interval"],
            reasoning_required="Lookup of Prometheus global parameter in dev config",
        ),
        EvalQuestion(
            id="FR-109",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What is the insecure-registries list in docker-daemon-dev.json?",
            reference_answer="registry.ridgeline.internal:5000",
            evidence_locations=["docker-daemon-dev.json -> insecure-registries"],
            reasoning_required="Lookup of Docker daemon insecure registry configuration",
        ),
        EvalQuestion(
            id="FR-110",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What are the feature flags in app-config-search.json?",
            reference_answer="fuzzy_search: true, autocomplete: true, index_cache: true",
            evidence_locations=["app-config-search.json -> features"],
            reasoning_required="Lookup of all feature flags in app config",
        ),
        EvalQuestion(
            id="FR-111",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What is the max_parallel_workers in pg-prod-analytics.conf?",
            reference_answer="16",
            evidence_locations=["pg-prod-analytics.conf -> max_parallel_workers"],
            reasoning_required="Lookup of PostgreSQL parallelism parameter on analytics instance",
        ),
        EvalQuestion(
            id="FR-112",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What service does systemd-legacy-dashboard.conf depend on (After= directive)?",
            reference_answer="network.target and mariadb.service",
            evidence_locations=["systemd-legacy-dashboard.conf -> [Unit].After"],
            reasoning_required="Lookup of systemd unit dependency chain",
        ),
        EvalQuestion(
            id="FR-113",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What is the log_min_duration_statement in pg-prod-analytics.conf?",
            reference_answer="1000",
            evidence_locations=["pg-prod-analytics.conf -> log_min_duration_statement"],
            reasoning_required="Lookup of PostgreSQL logging threshold on analytics instance",
        ),
        EvalQuestion(
            id="FR-114",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What is the Prometheus exporter port for the mariadb target in prometheus-prod.yaml?",
            reference_answer="9104",
            evidence_locations=["prometheus-prod.yaml -> scrape_configs (job_name: mariadb) -> static_configs[0].targets"],
            reasoning_required="Lookup of Prometheus scrape target port for MariaDB",
        ),
        EvalQuestion(
            id="FR-115",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What Grafana image version is used in compose-monitoring-prod.yaml?",
            reference_answer="grafana/grafana:10.4.0",
            evidence_locations=["compose-monitoring-prod.yaml -> services.grafana.image"],
            reasoning_required="Lookup of monitoring stack image version",
        ),
        EvalQuestion(
            id="FR-116",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What is the become setting in ansible-emergency-rollback.yaml?",
            reference_answer="false",
            evidence_locations=["ansible-emergency-rollback.yaml -> become"],
            reasoning_required="Lookup of Ansible playbook privilege escalation setting",
        ),
        EvalQuestion(
            id="FR-117",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What is the jit setting in pg-migration.conf?",
            reference_answer="off",
            evidence_locations=["pg-migration.conf -> jit"],
            reasoning_required="Lookup of PostgreSQL JIT compilation setting on migration instance",
        ),
        EvalQuestion(
            id="FR-118",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What is the maintenance_work_mem in pg-migration.conf?",
            reference_answer="1GB",
            evidence_locations=["pg-migration.conf -> maintenance_work_mem"],
            reasoning_required="Lookup of PostgreSQL memory parameter on migration instance",
        ),
        EvalQuestion(
            id="FR-119",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What Node.js engine version is required by package-json-web-app.json?",
            reference_answer=">=20.0.0",
            evidence_locations=["package-json-web-app.json -> engines.node"],
            reasoning_required="Lookup of Node.js engine requirement in package.json",
        ),
        EvalQuestion(
            id="FR-120",
            question_class=EvalQuestionClass.FACTUAL_RETRIEVAL,
            difficulty=Difficulty.HARD,
            question="What is the general_log setting in maria-dev.cnf?",
            reference_answer="ON",
            evidence_locations=["maria-dev.cnf -> general_log"],
            reasoning_required="Lookup of MariaDB general logging parameter on dev instance",
        ),
    ]


def cross_reference() -> list[EvalQuestion]:
    """Return 120 CROSS_REFERENCE questions (prefix CR)."""
    return [
        # ── EASY (CR-001 .. CR-048) ──────────────────────────────────────────────
        EvalQuestion(
            id="CR-001",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the shared_buffers differ between pg-prod-primary.conf and pg-prod-replica.conf?",
            reference_answer="Primary: 2GB, Replica: 256MB",
            evidence_locations=["pg-prod-primary.conf -> shared_buffers", "pg-prod-replica.conf -> shared_buffers"],
            reasoning_required="Compare PostgreSQL memory setting across two configs",
        ),
        EvalQuestion(
            id="CR-002",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the restart policy differ between compose-app-prod-01.yaml and compose-dev.yaml for the core-api service?",
            reference_answer="Prod: always, Dev: unless-stopped",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.restart", "compose-dev.yaml -> services.core-api.restart"],
            reasoning_required="Compare Docker Compose restart policies across environments",
        ),
        EvalQuestion(
            id="CR-003",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the PasswordAuthentication setting differ between sshd-prod.conf and sshd-staging.conf?",
            reference_answer="Prod: no, Staging: yes",
            evidence_locations=["sshd-prod.conf -> PasswordAuthentication", "sshd-staging.conf -> PasswordAuthentication"],
            reasoning_required="Compare sshd authentication settings across environments",
        ),
        EvalQuestion(
            id="CR-004",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the log level differ between traefik-prod-static.yaml and traefik-staging.yaml?",
            reference_answer="Prod: ERROR, Staging: DEBUG",
            evidence_locations=["traefik-prod-static.yaml -> log.level", "traefik-staging.yaml -> log.level"],
            reasoning_required="Compare Traefik log levels across environments",
        ),
        EvalQuestion(
            id="CR-005",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the max_connections differ between maria-legacy-main.cnf and maria-galera-node.cnf?",
            reference_answer="Legacy-main: 151, Galera: 300",
            evidence_locations=["maria-legacy-main.cnf -> max_connections", "maria-galera-node.cnf -> max_connections"],
            reasoning_required="Compare MariaDB connection limits across instances",
        ),
        EvalQuestion(
            id="CR-006",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the scrape_interval differ between prometheus-prod.yaml and prometheus-staging.yaml?",
            reference_answer="Prod: 15s, Staging: 30s",
            evidence_locations=["prometheus-prod.yaml -> global.scrape_interval", "prometheus-staging.yaml -> global.scrape_interval"],
            reasoning_required="Compare Prometheus scrape intervals across environments",
        ),
        EvalQuestion(
            id="CR-007",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the character_set_server differ between maria-legacy-main.cnf and maria-legacy-billing.cnf?",
            reference_answer="Legacy-main: latin1, Billing: utf8mb4",
            evidence_locations=["maria-legacy-main.cnf -> character_set_server", "maria-legacy-billing.cnf -> character_set_server"],
            reasoning_required="Compare MariaDB character set across legacy instances",
        ),
        EvalQuestion(
            id="CR-008",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the ssl setting differ between pg-prod-primary.conf and pg-staging.conf?",
            reference_answer="Primary: on, Staging: off",
            evidence_locations=["pg-prod-primary.conf -> ssl", "pg-staging.conf -> ssl"],
            reasoning_required="Compare PostgreSQL SSL settings across environments",
        ),
        EvalQuestion(
            id="CR-009",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the Port differ between sshd-prod.conf and sshd-bastion.conf?",
            reference_answer="Prod: 22, Bastion: 2222",
            evidence_locations=["sshd-prod.conf -> Port", "sshd-bastion.conf -> Port"],
            reasoning_required="Compare sshd port across server roles",
        ),
        EvalQuestion(
            id="CR-010",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the MaxSessions differ between sshd-prod.conf and sshd-staging.conf?",
            reference_answer="Prod: 2, Staging: 10",
            evidence_locations=["sshd-prod.conf -> MaxSessions", "sshd-staging.conf -> MaxSessions"],
            reasoning_required="Compare sshd session limits across environments",
        ),
        EvalQuestion(
            id="CR-011",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the work_mem differ between pg-prod-primary.conf and pg-prod-analytics.conf?",
            reference_answer="Primary: 8MB, Analytics: 256MB",
            evidence_locations=["pg-prod-primary.conf -> work_mem", "pg-prod-analytics.conf -> work_mem"],
            reasoning_required="Compare PostgreSQL memory setting across roles",
        ),
        EvalQuestion(
            id="CR-012",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the innodb_buffer_pool_size differ between maria-dev.cnf and maria-galera-node.cnf?",
            reference_answer="Dev: 256M, Galera: 2G",
            evidence_locations=["maria-dev.cnf -> innodb_buffer_pool_size", "maria-galera-node.cnf -> innodb_buffer_pool_size"],
            reasoning_required="Compare MariaDB buffer pool across instances",
        ),
        EvalQuestion(
            id="CR-013",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the core-api image tag differ between compose-app-prod-01.yaml and compose-staging.yaml?",
            reference_answer="Prod: 2.1.0, Staging: 2.1.0-rc1",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.image", "compose-staging.yaml -> services.core-api.image"],
            reasoning_required="Compare Docker image tags across environments",
        ),
        EvalQuestion(
            id="CR-014",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the vm.swappiness differ between sysctl-db-tuned.conf and sysctl-app-server.conf?",
            reference_answer="DB-tuned: 10, App-server: 60",
            evidence_locations=["sysctl-db-tuned.conf -> vm.swappiness", "sysctl-app-server.conf -> vm.swappiness"],
            reasoning_required="Compare sysctl tuning across server roles",
        ),
        EvalQuestion(
            id="CR-015",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the api.insecure setting differ between traefik-prod-static.yaml and traefik-dev.yaml?",
            reference_answer="Prod: false, Dev: true",
            evidence_locations=["traefik-prod-static.yaml -> api.insecure", "traefik-dev.yaml -> api.insecure"],
            reasoning_required="Compare Traefik API security across environments",
        ),
        EvalQuestion(
            id="CR-016",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the LogLevel differ between sshd-prod.conf and sshd-dev.conf?",
            reference_answer="Prod: VERBOSE, Dev: INFO",
            evidence_locations=["sshd-prod.conf -> LogLevel", "sshd-dev.conf -> LogLevel"],
            reasoning_required="Compare sshd logging levels across environments",
        ),
        EvalQuestion(
            id="CR-017",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the slow_query_log differ between maria-legacy-main.cnf and maria-legacy-billing.cnf?",
            reference_answer="Legacy-main: OFF, Billing: ON",
            evidence_locations=["maria-legacy-main.cnf -> slow_query_log", "maria-legacy-billing.cnf -> slow_query_log"],
            reasoning_required="Compare MariaDB slow query logging across instances",
        ),
        EvalQuestion(
            id="CR-018",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the app_instance_type differ between tfvars-prod.json and tfvars-dev.json?",
            reference_answer="Prod: t3.xlarge, Dev: t3.small",
            evidence_locations=["tfvars-prod.json -> compute.app_instance_type", "tfvars-dev.json -> compute.app_instance_type"],
            reasoning_required="Compare Terraform instance types across environments",
        ),
        EvalQuestion(
            id="CR-019",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the log_statement differ between pg-prod-primary.conf and pg-prod-replica.conf?",
            reference_answer="Primary: mod, Replica: none",
            evidence_locations=["pg-prod-primary.conf -> log_statement", "pg-prod-replica.conf -> log_statement"],
            reasoning_required="Compare PostgreSQL logging across roles",
        ),
        EvalQuestion(
            id="CR-020",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the max_connections differ between pg-prod-primary.conf and pg-prod-analytics.conf?",
            reference_answer="Primary: 200, Analytics: 50",
            evidence_locations=["pg-prod-primary.conf -> max_connections", "pg-prod-analytics.conf -> max_connections"],
            reasoning_required="Compare PostgreSQL connection limits across roles",
        ),
        EvalQuestion(
            id="CR-021",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the X11Forwarding differ between sshd-prod.conf and sshd-dev.conf?",
            reference_answer="Prod: no, Dev: yes",
            evidence_locations=["sshd-prod.conf -> X11Forwarding", "sshd-dev.conf -> X11Forwarding"],
            reasoning_required="Compare sshd X11 forwarding across environments",
        ),
        EvalQuestion(
            id="CR-022",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the become setting differ between ansible-base-server.yaml and ansible-deploy-core-api.yaml?",
            reference_answer="base-server: true, deploy-core-api: false",
            evidence_locations=["ansible-base-server.yaml -> become", "ansible-deploy-core-api.yaml -> become"],
            reasoning_required="Compare Ansible privilege escalation across playbooks",
        ),
        EvalQuestion(
            id="CR-023",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the shared_buffers differ between pg-staging.conf and pg-dev.conf?",
            reference_answer="Staging: 512MB, Dev: 128MB",
            evidence_locations=["pg-staging.conf -> shared_buffers", "pg-dev.conf -> shared_buffers"],
            reasoning_required="Compare PostgreSQL memory setting across non-prod environments",
        ),
        EvalQuestion(
            id="CR-024",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the backup_retention_days differ between tfvars-prod.json and tfvars-staging.json?",
            reference_answer="Prod: 30, Staging: 7",
            evidence_locations=["tfvars-prod.json -> features.backup_retention_days", "tfvars-staging.json -> features.backup_retention_days"],
            reasoning_required="Compare Terraform backup retention across environments",
        ),
        EvalQuestion(
            id="CR-025",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the PermitRootLogin differ between sshd-prod.conf and sshd-dev.conf?",
            reference_answer="Prod: no, Dev: prohibit-password",
            evidence_locations=["sshd-prod.conf -> PermitRootLogin", "sshd-dev.conf -> PermitRootLogin"],
            reasoning_required="Compare sshd root login policy across environments",
        ),
        EvalQuestion(
            id="CR-026",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the net.core.somaxconn differ between sysctl-db-tuned.conf and sysctl-dev.conf?",
            reference_answer="DB-tuned: 65535, Dev: 128",
            evidence_locations=["sysctl-db-tuned.conf -> net.core.somaxconn", "sysctl-dev.conf -> net.core.somaxconn"],
            reasoning_required="Compare sysctl network parameter across server roles",
        ),
        EvalQuestion(
            id="CR-027",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the debug setting differ between docker-daemon-prod.json and docker-daemon-dev.json?",
            reference_answer="Prod: false, Dev: true",
            evidence_locations=["docker-daemon-prod.json -> debug", "docker-daemon-dev.json -> debug"],
            reasoning_required="Compare Docker daemon debug mode across environments",
        ),
        EvalQuestion(
            id="CR-028",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the innodb_flush_log_at_trx_commit differ between maria-legacy-main.cnf and maria-galera-node.cnf?",
            reference_answer="Legacy-main: 1, Galera: 2",
            evidence_locations=["maria-legacy-main.cnf -> innodb_flush_log_at_trx_commit", "maria-galera-node.cnf -> innodb_flush_log_at_trx_commit"],
            reasoning_required="Compare MariaDB durability setting across instances",
        ),
        EvalQuestion(
            id="CR-029",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the requests_per_minute rate limit differ between app-config-auth-jwt.json and app-config-billing.json?",
            reference_answer="Auth-JWT: 1000, Billing: 500",
            evidence_locations=["app-config-auth-jwt.json -> rate_limiting.requests_per_minute", "app-config-billing.json -> rate_limiting.requests_per_minute"],
            reasoning_required="Compare rate limits across app services",
        ),
        EvalQuestion(
            id="CR-030",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the archive_mode differ between pg-prod-primary.conf and pg-prod-replica.conf?",
            reference_answer="Primary: on, Replica: off",
            evidence_locations=["pg-prod-primary.conf -> archive_mode", "pg-prod-replica.conf -> archive_mode"],
            reasoning_required="Compare PostgreSQL archiving across roles",
        ),
        EvalQuestion(
            id="CR-031",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the Compression setting differ between sshd-prod.conf and sshd-staging.conf?",
            reference_answer="Prod: no, Staging: yes",
            evidence_locations=["sshd-prod.conf -> Compression", "sshd-staging.conf -> Compression"],
            reasoning_required="Compare sshd compression setting across environments",
        ),
        EvalQuestion(
            id="CR-032",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the max_connections differ between maria-dev.cnf and maria-staging.cnf?",
            reference_answer="Dev: 50, Staging: 100",
            evidence_locations=["maria-dev.cnf -> max_connections", "maria-staging.cnf -> max_connections"],
            reasoning_required="Compare MariaDB connection limits across environments",
        ),
        EvalQuestion(
            id="CR-033",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the insecureSkipVerify differ between traefik-prod-static.yaml and traefik-staging.yaml?",
            reference_answer="Prod: false, Staging: true",
            evidence_locations=["traefik-prod-static.yaml -> serversTransport.insecureSkipVerify", "traefik-staging.yaml -> serversTransport.insecureSkipVerify"],
            reasoning_required="Compare Traefik TLS verification across environments",
        ),
        EvalQuestion(
            id="CR-034",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the core-api image tag differ between compose-dev.yaml and compose-ci.yaml?",
            reference_answer="Dev: dev, CI: ${CI_COMMIT_SHA}",
            evidence_locations=["compose-dev.yaml -> services.core-api.image", "compose-ci.yaml -> services.core-api-test.image"],
            reasoning_required="Compare Docker image tags across environments",
        ),
        EvalQuestion(
            id="CR-035",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the autovacuum setting differ between pg-prod-primary.conf and pg-migration.conf?",
            reference_answer="Primary: on, Migration: off",
            evidence_locations=["pg-prod-primary.conf -> autovacuum", "pg-migration.conf -> autovacuum"],
            reasoning_required="Compare PostgreSQL autovacuum across instances",
        ),
        EvalQuestion(
            id="CR-036",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the enable_backups feature differ between tfvars-prod.json and tfvars-dev.json?",
            reference_answer="Prod: true, Dev: false",
            evidence_locations=["tfvars-prod.json -> features.enable_backups", "tfvars-dev.json -> features.enable_backups"],
            reasoning_required="Compare Terraform feature flags across environments",
        ),
        EvalQuestion(
            id="CR-037",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the AllowTcpForwarding differ between sshd-prod.conf and sshd-bastion.conf?",
            reference_answer="Prod: no, Bastion: yes",
            evidence_locations=["sshd-prod.conf -> AllowTcpForwarding", "sshd-bastion.conf -> AllowTcpForwarding"],
            reasoning_required="Compare sshd forwarding settings across roles",
        ),
        EvalQuestion(
            id="CR-038",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the effective_cache_size differ between pg-prod-primary.conf and pg-prod-replica.conf?",
            reference_answer="Primary: 24GB, Replica: 4GB",
            evidence_locations=["pg-prod-primary.conf -> effective_cache_size", "pg-prod-replica.conf -> effective_cache_size"],
            reasoning_required="Compare PostgreSQL planner cache estimate across roles",
        ),
        EvalQuestion(
            id="CR-039",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the logging level differ between app-config-auth-jwt.json and app-config-search.json?",
            reference_answer="Auth-JWT: info, Search: warning",
            evidence_locations=["app-config-auth-jwt.json -> logging.level", "app-config-search.json -> logging.level"],
            reasoning_required="Compare logging levels across app configs",
        ),
        EvalQuestion(
            id="CR-040",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the Restart policy differ between systemd-core-api.conf and systemd-legacy-dashboard.conf?",
            reference_answer="Core-API: on-failure, Legacy-dashboard: always",
            evidence_locations=["systemd-core-api.conf -> [Service].Restart", "systemd-legacy-dashboard.conf -> [Service].Restart"],
            reasoning_required="Compare systemd restart policies across services",
        ),
        EvalQuestion(
            id="CR-041",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the max_connections differ between pg-prod-primary.conf and pg-migration.conf?",
            reference_answer="Primary: 200, Migration: 20",
            evidence_locations=["pg-prod-primary.conf -> max_connections", "pg-migration.conf -> max_connections"],
            reasoning_required="Compare PostgreSQL connection limits across instances",
        ),
        EvalQuestion(
            id="CR-042",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the slow_query_log differ between maria-staging.cnf and maria-dev.cnf?",
            reference_answer="Staging: OFF, Dev: ON",
            evidence_locations=["maria-staging.cnf -> slow_query_log", "maria-dev.cnf -> slow_query_log"],
            reasoning_required="Compare MariaDB slow query logging across environments",
        ),
        EvalQuestion(
            id="CR-043",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the log_statement differ between pg-dev.conf and pg-migration.conf?",
            reference_answer="Dev: all, Migration: ddl",
            evidence_locations=["pg-dev.conf -> log_statement", "pg-migration.conf -> log_statement"],
            reasoning_required="Compare PostgreSQL logging levels across instances",
        ),
        EvalQuestion(
            id="CR-044",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the app_instance_count differ between tfvars-prod.json and tfvars-staging.json?",
            reference_answer="Prod: 3, Staging: 1",
            evidence_locations=["tfvars-prod.json -> compute.app_instance_count", "tfvars-staging.json -> compute.app_instance_count"],
            reasoning_required="Compare Terraform compute counts across environments",
        ),
        EvalQuestion(
            id="CR-045",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the MaxAuthTries differ between sshd-prod.conf and sshd-staging.conf?",
            reference_answer="Prod: 3, Staging: 6",
            evidence_locations=["sshd-prod.conf -> MaxAuthTries", "sshd-staging.conf -> MaxAuthTries"],
            reasoning_required="Compare sshd auth retry limits across environments",
        ),
        EvalQuestion(
            id="CR-046",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the work_mem differ between pg-prod-primary.conf and pg-migration.conf?",
            reference_answer="Primary: 8MB, Migration: 64MB",
            evidence_locations=["pg-prod-primary.conf -> work_mem", "pg-migration.conf -> work_mem"],
            reasoning_required="Compare PostgreSQL work memory across instances",
        ),
        EvalQuestion(
            id="CR-047",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the start_period differ between compose-app-prod-01.yaml and compose-staging.yaml for core-api?",
            reference_answer="Prod: 10s, Staging: 0s",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.healthcheck.start_period", "compose-staging.yaml -> services.core-api.healthcheck.start_period"],
            reasoning_required="Compare Docker Compose healthcheck settings across environments",
        ),
        EvalQuestion(
            id="CR-048",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.EASY,
            question="How does the long_query_time differ between maria-legacy-main.cnf and maria-galera-node.cnf?",
            reference_answer="Legacy-main: 10, Galera: 1",
            evidence_locations=["maria-legacy-main.cnf -> long_query_time", "maria-galera-node.cnf -> long_query_time"],
            reasoning_required="Compare MariaDB slow query threshold across instances",
        ),
        # ── MEDIUM (CR-049 .. CR-096) ────────────────────────────────────────────
        EvalQuestion(
            id="CR-049",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Which PostgreSQL configs have SSL enabled?",
            reference_answer="pg-prod-primary.conf and pg-prod-analytics.conf (ssl=on); pg-prod-replica.conf, pg-staging.conf, pg-dev.conf, and pg-migration.conf have ssl=off",
            evidence_locations=["pg-prod-primary.conf -> ssl", "pg-prod-analytics.conf -> ssl", "pg-prod-replica.conf -> ssl", "pg-staging.conf -> ssl"],
            reasoning_required="Cross-reference SSL setting across all PostgreSQL configs",
        ),
        EvalQuestion(
            id="CR-050",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Which MariaDB configs have slow_query_log enabled?",
            reference_answer="maria-legacy-billing.cnf, maria-dev.cnf, and maria-galera-node.cnf have slow_query_log=ON; maria-legacy-main.cnf and maria-staging.cnf have it OFF",
            evidence_locations=["maria-legacy-billing.cnf -> slow_query_log", "maria-dev.cnf -> slow_query_log", "maria-galera-node.cnf -> slow_query_log"],
            reasoning_required="Cross-reference slow query log across all MariaDB configs",
        ),
        EvalQuestion(
            id="CR-051",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the number of Prometheus scrape targets compare between prod and staging?",
            reference_answer="Prod has 15 scrape jobs, Staging has 8 scrape jobs",
            evidence_locations=["prometheus-prod.yaml -> scrape_configs", "prometheus-staging.yaml -> scrape_configs"],
            reasoning_required="Count and compare scrape configs across environments",
        ),
        EvalQuestion(
            id="CR-052",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How do the nofile ulimits differ between docker-daemon-prod.json and docker-daemon-dev.json?",
            reference_answer="Prod: Soft=65535, Hard=65535; Dev: Soft=1024, Hard=1024",
            evidence_locations=["docker-daemon-prod.json -> default-ulimits.nofile", "docker-daemon-dev.json -> default-ulimits.nofile"],
            reasoning_required="Compare Docker daemon ulimit settings across environments",
        ),
        EvalQuestion(
            id="CR-053",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Which sshd configs allow password authentication?",
            reference_answer="sshd-staging.conf and sshd-dev.conf have PasswordAuthentication=yes; sshd-prod.conf and sshd-bastion.conf have it set to no",
            evidence_locations=["sshd-staging.conf -> PasswordAuthentication", "sshd-dev.conf -> PasswordAuthentication", "sshd-prod.conf -> PasswordAuthentication"],
            reasoning_required="Cross-reference password authentication across all sshd configs",
        ),
        EvalQuestion(
            id="CR-054",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the number of networks differ between compose-app-prod-01.yaml and compose-dev.yaml?",
            reference_answer="Prod: 2 networks (app-network, monitoring), Dev: 1 network (dev-network)",
            evidence_locations=["compose-app-prod-01.yaml -> networks", "compose-dev.yaml -> networks"],
            reasoning_required="Compare Docker Compose network topology across environments",
        ),
        EvalQuestion(
            id="CR-055",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the shared_buffers value progress from dev to staging to primary PostgreSQL configs?",
            reference_answer="Dev: 128MB, Staging: 512MB, Primary: 2GB",
            evidence_locations=["pg-dev.conf -> shared_buffers", "pg-staging.conf -> shared_buffers", "pg-prod-primary.conf -> shared_buffers"],
            reasoning_required="Track PostgreSQL memory scaling across environments",
        ),
        EvalQuestion(
            id="CR-056",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Which Traefik configs have a file provider configured?",
            reference_answer="Only traefik-prod-static.yaml has the file provider (directory: /etc/traefik/dynamic); traefik-staging.yaml and traefik-dev.yaml only have the docker provider",
            evidence_locations=["traefik-prod-static.yaml -> providers.file", "traefik-staging.yaml -> providers", "traefik-dev.yaml -> providers"],
            reasoning_required="Cross-reference Traefik provider configuration across all configs",
        ),
        EvalQuestion(
            id="CR-057",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the VPC CIDR differ across all three tfvars environments?",
            reference_answer="Prod: 10.0.0.0/16, Staging: 10.1.0.0/16, Dev: 10.2.0.0/16",
            evidence_locations=["tfvars-prod.json -> networking.vpc_cidr", "tfvars-staging.json -> networking.vpc_cidr", "tfvars-dev.json -> networking.vpc_cidr"],
            reasoning_required="Compare Terraform networking across all environments",
        ),
        EvalQuestion(
            id="CR-058",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the log_statement setting differ across all six PostgreSQL configs?",
            reference_answer="Primary: mod, Replica: none, Analytics: none, Staging: none, Dev: all, Migration: ddl",
            evidence_locations=["pg-prod-primary.conf -> log_statement", "pg-prod-replica.conf -> log_statement", "pg-dev.conf -> log_statement", "pg-migration.conf -> log_statement"],
            reasoning_required="Cross-reference PostgreSQL logging across all configs",
        ),
        EvalQuestion(
            id="CR-059",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the innodb_buffer_pool_size vary across all MariaDB configs?",
            reference_answer="Legacy-main: 256M, Billing: 1G, Staging: 512M, Dev: 256M, Galera: 2G",
            evidence_locations=["maria-legacy-main.cnf -> innodb_buffer_pool_size", "maria-legacy-billing.cnf -> innodb_buffer_pool_size", "maria-galera-node.cnf -> innodb_buffer_pool_size"],
            reasoning_required="Cross-reference MariaDB buffer pool across all configs",
        ),
        EvalQuestion(
            id="CR-060",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the database connection differ between app-config-auth-jwt.json and app-config-billing.json?",
            reference_answer="Auth-JWT connects to PostgreSQL on db-prod-01:5432, Billing connects to MariaDB on db-legacy-01:3306",
            evidence_locations=["app-config-auth-jwt.json -> database", "app-config-billing.json -> database"],
            reasoning_required="Compare database backends across app services",
        ),
        EvalQuestion(
            id="CR-061",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How do the Prometheus external_labels differ between prod and staging?",
            reference_answer="Prod: env=prod, cluster=ridgeline-prod; Staging: env=staging, cluster=ridgeline-staging",
            evidence_locations=["prometheus-prod.yaml -> global.external_labels", "prometheus-staging.yaml -> global.external_labels"],
            reasoning_required="Compare Prometheus external labels across environments",
        ),
        EvalQuestion(
            id="CR-062",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the max_parallel_workers_per_gather differ between pg-prod-primary.conf and pg-prod-analytics.conf?",
            reference_answer="Primary: 4, Analytics: 8",
            evidence_locations=["pg-prod-primary.conf -> max_parallel_workers_per_gather", "pg-prod-analytics.conf -> max_parallel_workers_per_gather"],
            reasoning_required="Compare PostgreSQL parallelism settings across roles",
        ),
        EvalQuestion(
            id="CR-063",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the enable_monitoring feature differ across all tfvars environments?",
            reference_answer="Prod: true, Staging: true, Dev: false",
            evidence_locations=["tfvars-prod.json -> features.enable_monitoring", "tfvars-staging.json -> features.enable_monitoring", "tfvars-dev.json -> features.enable_monitoring"],
            reasoning_required="Compare feature flags across all Terraform environments",
        ),
        EvalQuestion(
            id="CR-064",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the kernel.pid_max differ between sysctl-db-tuned.conf and sysctl-app-server.conf?",
            reference_answer="DB-tuned: 131072, App-server: 32768",
            evidence_locations=["sysctl-db-tuned.conf -> kernel.pid_max", "sysctl-app-server.conf -> kernel.pid_max"],
            reasoning_required="Compare sysctl kernel parameter across server roles",
        ),
        EvalQuestion(
            id="CR-065",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Which Docker Compose files use the 'no' restart policy?",
            reference_answer="compose-ci.yaml uses restart: no for all services; compose-dev.yaml uses restart: no for postgres, mariadb, and redis",
            evidence_locations=["compose-ci.yaml -> services.*.restart", "compose-dev.yaml -> services.postgres.restart"],
            reasoning_required="Cross-reference restart policies across Docker Compose files",
        ),
        EvalQuestion(
            id="CR-066",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the checkpoint_timeout differ between pg-prod-primary.conf and pg-prod-analytics.conf?",
            reference_answer="Primary: 15min, Analytics: 30min",
            evidence_locations=["pg-prod-primary.conf -> checkpoint_timeout", "pg-prod-analytics.conf -> checkpoint_timeout"],
            reasoning_required="Compare PostgreSQL WAL parameters across roles",
        ),
        EvalQuestion(
            id="CR-067",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the LoginGraceTime differ between sshd-prod.conf and sshd-staging.conf?",
            reference_answer="Prod: 60, Staging: 120",
            evidence_locations=["sshd-prod.conf -> LoginGraceTime", "sshd-staging.conf -> LoginGraceTime"],
            reasoning_required="Compare sshd login grace time across environments",
        ),
        EvalQuestion(
            id="CR-068",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the jwt_expiry_seconds differ between app-config-auth-jwt.json and app-config-billing.json?",
            reference_answer="Auth-JWT: 3600, Billing: 7200",
            evidence_locations=["app-config-auth-jwt.json -> auth.jwt_expiry_seconds", "app-config-billing.json -> auth.jwt_expiry_seconds"],
            reasoning_required="Compare JWT expiry across app configs",
        ),
        EvalQuestion(
            id="CR-069",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the ClientAliveInterval differ between sshd-prod.conf and sshd-bastion.conf?",
            reference_answer="Prod: 300, Bastion: 120",
            evidence_locations=["sshd-prod.conf -> ClientAliveInterval", "sshd-bastion.conf -> ClientAliveInterval"],
            reasoning_required="Compare sshd keepalive settings across roles",
        ),
        EvalQuestion(
            id="CR-070",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Which Traefik configs have a websecure (port 443) entrypoint?",
            reference_answer="traefik-prod-static.yaml and traefik-staging.yaml have a websecure entrypoint; traefik-dev.yaml does not",
            evidence_locations=["traefik-prod-static.yaml -> entryPoints.websecure", "traefik-staging.yaml -> entryPoints.websecure", "traefik-dev.yaml -> entryPoints"],
            reasoning_required="Cross-reference Traefik entrypoints across all configs",
        ),
        EvalQuestion(
            id="CR-071",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the log-opts max-size differ between docker-daemon-prod.json and docker-daemon-dev.json?",
            reference_answer="Prod: 50m, Dev: 100m",
            evidence_locations=["docker-daemon-prod.json -> log-opts.max-size", "docker-daemon-dev.json -> log-opts.max-size"],
            reasoning_required="Compare Docker daemon log rotation across environments",
        ),
        EvalQuestion(
            id="CR-072",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the max_wal_size differ between pg-prod-primary.conf, pg-prod-analytics.conf, and pg-staging.conf?",
            reference_answer="Primary: 4GB, Analytics: 8GB, Staging: 1GB",
            evidence_locations=["pg-prod-primary.conf -> max_wal_size", "pg-prod-analytics.conf -> max_wal_size", "pg-staging.conf -> max_wal_size"],
            reasoning_required="Compare PostgreSQL WAL sizes across instances",
        ),
        EvalQuestion(
            id="CR-073",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Which MariaDB configs use utf8mb4 character set?",
            reference_answer="maria-legacy-billing.cnf, maria-staging.cnf, maria-dev.cnf, and maria-galera-node.cnf all use utf8mb4; only maria-legacy-main.cnf uses latin1",
            evidence_locations=["maria-legacy-billing.cnf -> character_set_server", "maria-legacy-main.cnf -> character_set_server"],
            reasoning_required="Cross-reference character set across all MariaDB configs",
        ),
        EvalQuestion(
            id="CR-074",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the scrape_interval compare across all three Prometheus configs?",
            reference_answer="Prod: 15s, Staging: 30s, Dev: 1m",
            evidence_locations=["prometheus-prod.yaml -> global.scrape_interval", "prometheus-staging.yaml -> global.scrape_interval", "prometheus-dev.yaml -> global.scrape_interval"],
            reasoning_required="Compare Prometheus scrape intervals across all environments",
        ),
        EvalQuestion(
            id="CR-075",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the default_statistics_target differ between pg-prod-primary.conf and pg-prod-analytics.conf?",
            reference_answer="Primary: 200, Analytics: 500",
            evidence_locations=["pg-prod-primary.conf -> default_statistics_target", "pg-prod-analytics.conf -> default_statistics_target"],
            reasoning_required="Compare PostgreSQL planner statistics across roles",
        ),
        EvalQuestion(
            id="CR-076",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the userland-proxy setting differ between docker-daemon-prod.json and docker-daemon-dev.json?",
            reference_answer="Prod: false, Dev: true",
            evidence_locations=["docker-daemon-prod.json -> userland-proxy", "docker-daemon-dev.json -> userland-proxy"],
            reasoning_required="Compare Docker daemon proxy settings across environments",
        ),
        EvalQuestion(
            id="CR-077",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the log_connections setting differ between pg-prod-primary.conf and pg-prod-replica.conf?",
            reference_answer="Primary: on, Replica: off",
            evidence_locations=["pg-prod-primary.conf -> log_connections", "pg-prod-replica.conf -> log_connections"],
            reasoning_required="Compare PostgreSQL connection logging across roles",
        ),
        EvalQuestion(
            id="CR-078",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the db_instance_count differ between tfvars-prod.json and tfvars-staging.json?",
            reference_answer="Prod: 3, Staging: 1",
            evidence_locations=["tfvars-prod.json -> compute.db_instance_count", "tfvars-staging.json -> compute.db_instance_count"],
            reasoning_required="Compare Terraform DB instance count across environments",
        ),
        EvalQuestion(
            id="CR-079",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the AllowAgentForwarding differ between sshd-prod.conf and sshd-bastion.conf?",
            reference_answer="Prod: no, Bastion: yes",
            evidence_locations=["sshd-prod.conf -> AllowAgentForwarding", "sshd-bastion.conf -> AllowAgentForwarding"],
            reasoning_required="Compare sshd agent forwarding across roles",
        ),
        EvalQuestion(
            id="CR-080",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the max_worker_processes differ between pg-prod-primary.conf and pg-prod-analytics.conf?",
            reference_answer="Primary: 8, Analytics: 16",
            evidence_locations=["pg-prod-primary.conf -> max_worker_processes", "pg-prod-analytics.conf -> max_worker_processes"],
            reasoning_required="Compare PostgreSQL worker configuration across roles",
        ),
        EvalQuestion(
            id="CR-081",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the innodb_io_capacity differ between maria-legacy-main.cnf and maria-legacy-billing.cnf?",
            reference_answer="Legacy-main: 200, Billing: 400",
            evidence_locations=["maria-legacy-main.cnf -> innodb_io_capacity", "maria-legacy-billing.cnf -> innodb_io_capacity"],
            reasoning_required="Compare MariaDB InnoDB IO capacity across instances",
        ),
        EvalQuestion(
            id="CR-082",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the accessLog.addInternals setting differ between traefik-prod-static.yaml and traefik-dev.yaml?",
            reference_answer="Prod: false, Dev: true",
            evidence_locations=["traefik-prod-static.yaml -> accessLog.addInternals", "traefik-dev.yaml -> accessLog.addInternals"],
            reasoning_required="Compare Traefik access log configuration across environments",
        ),
        EvalQuestion(
            id="CR-083",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the synchronous_commit differ between pg-prod-primary.conf and pg-migration.conf?",
            reference_answer="Primary: on, Migration: off",
            evidence_locations=["pg-prod-primary.conf -> synchronous_commit", "pg-migration.conf -> synchronous_commit"],
            reasoning_required="Compare PostgreSQL durability across instances",
        ),
        EvalQuestion(
            id="CR-084",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the fs.file-max value differ between sysctl-db-tuned.conf and sysctl-app-server.conf?",
            reference_answer="DB-tuned: 2097152, App-server: 1048576",
            evidence_locations=["sysctl-db-tuned.conf -> fs.file-max", "sysctl-app-server.conf -> fs.file-max"],
            reasoning_required="Compare sysctl file descriptor limits across roles",
        ),
        EvalQuestion(
            id="CR-085",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the mem_limit for core-api differ across prod, staging, and CI compose files?",
            reference_answer="Prod: 2g, Staging: 1g, CI: 2g (core-api-test)",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.mem_limit", "compose-staging.yaml -> services.core-api.mem_limit", "compose-ci.yaml -> services.core-api-test.mem_limit"],
            reasoning_required="Compare Docker Compose resource limits across environments",
        ),
        EvalQuestion(
            id="CR-086",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Which PostgreSQL configs have logging_collector enabled?",
            reference_answer="pg-prod-primary.conf, pg-prod-analytics.conf, pg-dev.conf, and pg-migration.conf have logging_collector=on; pg-prod-replica.conf and pg-staging.conf have it off",
            evidence_locations=["pg-prod-primary.conf -> logging_collector", "pg-prod-replica.conf -> logging_collector"],
            reasoning_required="Cross-reference PostgreSQL logging collector across all configs",
        ),
        EvalQuestion(
            id="CR-087",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the ignore_errors setting differ between ansible-base-server.yaml and ansible-emergency-rollback.yaml?",
            reference_answer="Base-server: false, Emergency-rollback: true",
            evidence_locations=["ansible-base-server.yaml -> ignore_errors", "ansible-emergency-rollback.yaml -> ignore_errors"],
            reasoning_required="Compare Ansible error handling across playbooks",
        ),
        EvalQuestion(
            id="CR-088",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the maintenance_work_mem differ between pg-prod-primary.conf and pg-prod-analytics.conf?",
            reference_answer="Primary: 512MB, Analytics: 2GB",
            evidence_locations=["pg-prod-primary.conf -> maintenance_work_mem", "pg-prod-analytics.conf -> maintenance_work_mem"],
            reasoning_required="Compare PostgreSQL maintenance memory across roles",
        ),
        EvalQuestion(
            id="CR-089",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the domain differ across all three tfvars environments?",
            reference_answer="Prod: ridgeline.io, Staging: staging.ridgeline.io, Dev: dev.ridgeline.internal",
            evidence_locations=["tfvars-prod.json -> networking.domain", "tfvars-staging.json -> networking.domain", "tfvars-dev.json -> networking.domain"],
            reasoning_required="Compare domain names across all Terraform environments",
        ),
        EvalQuestion(
            id="CR-090",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the AddressFamily differ between sshd-prod.conf and sshd-bastion.conf?",
            reference_answer="Prod: any, Bastion: inet",
            evidence_locations=["sshd-prod.conf -> AddressFamily", "sshd-bastion.conf -> AddressFamily"],
            reasoning_required="Compare sshd network family across roles",
        ),
        EvalQuestion(
            id="CR-091",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the server_id differ across all MariaDB configs?",
            reference_answer="Legacy-main: 1, Billing: 2, Staging: 10, Dev: 20, Galera: 100",
            evidence_locations=["maria-legacy-main.cnf -> server_id", "maria-legacy-billing.cnf -> server_id", "maria-galera-node.cnf -> server_id"],
            reasoning_required="Cross-reference MariaDB server IDs across all configs",
        ),
        EvalQuestion(
            id="CR-092",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the log_timezone differ between pg-prod-primary.conf and pg-prod-replica.conf?",
            reference_answer="Primary: America/New_York, Replica: GMT",
            evidence_locations=["pg-prod-primary.conf -> log_timezone", "pg-prod-replica.conf -> log_timezone"],
            reasoning_required="Compare PostgreSQL timezone configuration across roles",
        ),
        EvalQuestion(
            id="CR-093",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the hash_mem_multiplier differ between pg-prod-primary.conf and pg-prod-analytics.conf?",
            reference_answer="Primary: 2.0, Analytics: 4.0",
            evidence_locations=["pg-prod-primary.conf -> hash_mem_multiplier", "pg-prod-analytics.conf -> hash_mem_multiplier"],
            reasoning_required="Compare PostgreSQL hash memory multiplier across roles",
        ),
        EvalQuestion(
            id="CR-094",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Which sshd configs set an AllowUsers directive?",
            reference_answer="sshd-prod.conf (deploy ops admins) and sshd-bastion.conf (deploy ops); sshd-staging.conf and sshd-dev.conf do not have AllowUsers",
            evidence_locations=["sshd-prod.conf -> AllowUsers", "sshd-bastion.conf -> AllowUsers"],
            reasoning_required="Cross-reference sshd access control across all configs",
        ),
        EvalQuestion(
            id="CR-095",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the vm.nr_hugepages setting compare between sysctl-db-tuned.conf and sysctl-app-server.conf?",
            reference_answer="DB-tuned: 1024, App-server: not set",
            evidence_locations=["sysctl-db-tuned.conf -> vm.nr_hugepages", "sysctl-app-server.conf"],
            reasoning_required="Compare sysctl hugepage settings across server roles",
        ),
        EvalQuestion(
            id="CR-096",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the log-opts max-file setting differ between docker-daemon-prod.json and docker-daemon-dev.json?",
            reference_answer="Prod: 5, Dev: 3",
            evidence_locations=["docker-daemon-prod.json -> log-opts.max-file", "docker-daemon-dev.json -> log-opts.max-file"],
            reasoning_required="Compare Docker daemon log rotation file counts across environments",
        ),
        # ── HARD (CR-097 .. CR-120) ──────────────────────────────────────────────
        EvalQuestion(
            id="CR-097",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="Which PostgreSQL config has the highest effective_cache_size and which has the lowest among production instances?",
            reference_answer="Highest: pg-prod-analytics.conf (48GB), Lowest: pg-prod-replica.conf (4GB)",
            evidence_locations=["pg-prod-analytics.conf -> effective_cache_size", "pg-prod-replica.conf -> effective_cache_size", "pg-prod-primary.conf -> effective_cache_size"],
            reasoning_required="Compare and rank PostgreSQL planner cache estimates across all prod configs",
        ),
        EvalQuestion(
            id="CR-098",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="Which config files reference db-prod-01 as a host or target?",
            reference_answer="app-config-auth-jwt.json (database.host), systemd-core-api.conf (DATABASE_URL env), prometheus-prod.yaml (postgres-primary scrape target at db-prod-01:9187), ansible-inventory-prod.yaml (db_servers group)",
            evidence_locations=["app-config-auth-jwt.json -> database.host", "systemd-core-api.conf -> Environment", "prometheus-prod.yaml -> scrape_configs", "ansible-inventory-prod.yaml -> db_servers"],
            reasoning_required="Cross-reference all files that reference a specific host",
        ),
        EvalQuestion(
            id="CR-099",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How do the shared_buffers values across all six PostgreSQL configs rank from highest to lowest?",
            reference_answer="Analytics: 16GB > Primary: 2GB > Staging: 512MB > Replica: 256MB > Dev: 128MB = Migration: 128MB",
            evidence_locations=["pg-prod-analytics.conf -> shared_buffers", "pg-prod-primary.conf -> shared_buffers", "pg-staging.conf -> shared_buffers", "pg-prod-replica.conf -> shared_buffers", "pg-dev.conf -> shared_buffers", "pg-migration.conf -> shared_buffers"],
            reasoning_required="Rank PostgreSQL shared_buffers across all configs",
        ),
        EvalQuestion(
            id="CR-100",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="Which MariaDB config has the highest innodb_buffer_pool_size and which has the lowest?",
            reference_answer="Highest: maria-galera-node.cnf (2G), Lowest: maria-legacy-main.cnf and maria-dev.cnf (both 256M)",
            evidence_locations=["maria-galera-node.cnf -> innodb_buffer_pool_size", "maria-legacy-main.cnf -> innodb_buffer_pool_size", "maria-dev.cnf -> innodb_buffer_pool_size"],
            reasoning_required="Rank MariaDB buffer pool sizes across all configs",
        ),
        EvalQuestion(
            id="CR-101",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How do the sshd hardening levels compare across all four environments based on PasswordAuthentication, MaxAuthTries, MaxSessions, and AllowTcpForwarding?",
            reference_answer="Prod is most hardened (PwdAuth=no, MaxAuth=3, MaxSess=2, TcpFwd=no). Bastion is restrictive but allows forwarding (PwdAuth=no, MaxAuth=3, MaxSess=1, TcpFwd=yes). Staging/Dev are permissive (PwdAuth=yes, MaxAuth=6, MaxSess=10, TcpFwd=yes)",
            evidence_locations=["sshd-prod.conf", "sshd-bastion.conf", "sshd-staging.conf", "sshd-dev.conf"],
            reasoning_required="Multi-attribute comparison across all sshd configs to assess hardening",
        ),
        EvalQuestion(
            id="CR-102",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How do the memory resource limits compare across prod, staging, and CI compose files for equivalent services?",
            reference_answer="Core-API: prod=2g, staging=1g, CI=2g. Auth-service: prod=1g, staging=512m. Redis: prod=512m. Postgres: staging=1g, CI=512m",
            evidence_locations=["compose-app-prod-01.yaml -> services.*.mem_limit", "compose-staging.yaml -> services.*.mem_limit", "compose-ci.yaml -> services.*.mem_limit"],
            reasoning_required="Multi-service memory comparison across Docker Compose environments",
        ),
        EvalQuestion(
            id="CR-103",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="Which config files contain references to the redis service or redis URL?",
            reference_answer="compose-app-prod-01.yaml (redis service), compose-dev.yaml (redis service), compose-ci.yaml (redis service), app-config-auth-jwt.json (redis.url), app-config-billing.json (redis.url), app-config-search.json (redis.url), ansible-vars-prod.yaml (redis_password)",
            evidence_locations=["compose-app-prod-01.yaml -> services.redis", "app-config-auth-jwt.json -> redis", "app-config-billing.json -> redis"],
            reasoning_required="Cross-reference all files mentioning Redis across the fleet",
        ),
        EvalQuestion(
            id="CR-104",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How do the Prometheus scrape targets differ between dev and staging in terms of which services are monitored?",
            reference_answer="Dev monitors 3 services (prometheus, core-api, web-app, node-exporter); Staging monitors 8 services (prometheus, core-api, auth-service, web-app, legacy-dashboard, postgres, mariadb, traefik, node-exporter)",
            evidence_locations=["prometheus-dev.yaml -> scrape_configs", "prometheus-staging.yaml -> scrape_configs"],
            reasoning_required="Compare monitored service sets across Prometheus configs",
        ),
        EvalQuestion(
            id="CR-105",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How does the Ansible become (privilege escalation) setting compare across ansible-base-server.yaml, ansible-deploy-core-api.yaml, and ansible-emergency-rollback.yaml?",
            reference_answer="base-server: become=true globally, deploy-core-api: become=false globally with become=false per-task, emergency-rollback: become=false globally (potential issue as rollback may need privileges)",
            evidence_locations=["ansible-base-server.yaml -> become", "ansible-deploy-core-api.yaml -> become", "ansible-emergency-rollback.yaml -> become"],
            reasoning_required="Compare privilege escalation strategies across Ansible playbooks",
        ),
        EvalQuestion(
            id="CR-106",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How do the PostgreSQL WAL and replication settings compare between the primary and migration configs?",
            reference_answer="Primary: max_wal_senders=10, max_replication_slots=10, archive_mode=on, synchronous_commit=on, hot_standby=on. Migration: max_wal_senders=0, max_replication_slots=0, archive_mode=off, synchronous_commit=off, hot_standby=off",
            evidence_locations=["pg-prod-primary.conf -> max_wal_senders", "pg-migration.conf -> max_wal_senders"],
            reasoning_required="Multi-parameter WAL/replication comparison between primary and migration",
        ),
        EvalQuestion(
            id="CR-107",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="Which config files reference port 8000?",
            reference_answer="compose-app-prod-01.yaml (core-api port mapping), compose-staging.yaml (core-api port mapping), compose-dev.yaml (core-api port mapping), systemd-core-api.conf (uvicorn --port 8000), ansible-deploy-core-api.yaml (app_port variable and wait_for), prometheus-prod.yaml (core-api target app-prod-01:8000), prometheus-staging.yaml (core-api target), prometheus-dev.yaml (core-api target)",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.ports", "systemd-core-api.conf -> ExecStart", "prometheus-prod.yaml -> scrape_configs"],
            reasoning_required="Cross-reference all files referencing a specific port number",
        ),
        EvalQuestion(
            id="CR-108",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How do the MariaDB replication-related settings (sync_binlog and innodb_flush_log_at_trx_commit) compare across all MariaDB configs?",
            reference_answer="Legacy-main: sync_binlog=1, flush=1; Billing: sync_binlog=1, flush=1; Staging: sync_binlog=1, flush=1; Dev: sync_binlog=1, flush=1; Galera: sync_binlog=0, flush=2 (relaxed for Galera replication)",
            evidence_locations=["maria-legacy-main.cnf -> sync_binlog", "maria-galera-node.cnf -> sync_binlog", "maria-galera-node.cnf -> innodb_flush_log_at_trx_commit"],
            reasoning_required="Multi-parameter replication comparison across all MariaDB configs",
        ),
        EvalQuestion(
            id="CR-109",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How do the Terraform instance types scale down from prod to staging to dev?",
            reference_answer="App: t3.xlarge (prod) -> t3.medium (staging) -> t3.small (dev). DB: r6g.2xlarge (prod) -> r6g.large (staging) -> t3.medium (dev). Edge: t3.medium (prod) -> t3.small (staging) -> not defined (dev)",
            evidence_locations=["tfvars-prod.json -> compute", "tfvars-staging.json -> compute", "tfvars-dev.json -> compute"],
            reasoning_required="Multi-attribute instance type comparison across all environments",
        ),
        EvalQuestion(
            id="CR-110",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How do the Traefik security settings (api.insecure, api.debug, insecureSkipVerify) compare across prod, staging, and dev?",
            reference_answer="Prod: insecure=false, debug=false, skipVerify=false. Staging: insecure=true, debug=true, skipVerify=true. Dev: insecure=true, debug=true, skipVerify=true",
            evidence_locations=["traefik-prod-static.yaml -> api", "traefik-staging.yaml -> api", "traefik-dev.yaml -> api"],
            reasoning_required="Multi-attribute security comparison across all Traefik configs",
        ),
        EvalQuestion(
            id="CR-111",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How do the PostgreSQL parallelism settings (max_worker_processes, max_parallel_workers, max_parallel_workers_per_gather) compare between primary, replica, and analytics?",
            reference_answer="Primary: workers=8, parallel=8, per_gather=4. Replica: workers=8, parallel=8, per_gather=2. Analytics: workers=16, parallel=16, per_gather=8",
            evidence_locations=["pg-prod-primary.conf -> max_worker_processes", "pg-prod-replica.conf -> max_worker_processes", "pg-prod-analytics.conf -> max_worker_processes"],
            reasoning_required="Multi-parameter parallelism comparison across PostgreSQL roles",
        ),
        EvalQuestion(
            id="CR-112",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="Which app-config JSON files have a database section, and what database type does each connect to?",
            reference_answer="app-config-auth-jwt.json connects to PostgreSQL (port 5432, host db-prod-01). app-config-billing.json connects to MariaDB (port 3306, host db-legacy-01). app-config-search.json has no database section (uses Redis only)",
            evidence_locations=["app-config-auth-jwt.json -> database", "app-config-billing.json -> database", "app-config-search.json"],
            reasoning_required="Cross-reference database connectivity across all app configs",
        ),
        EvalQuestion(
            id="CR-113",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How do the sysctl network tuning settings (net.core.somaxconn, net.ipv4.tcp_max_syn_backlog, net.ipv4.tcp_fin_timeout) compare between db-tuned and app-server?",
            reference_answer="DB-tuned: somaxconn=65535, syn_backlog=4096, fin_timeout=15. App-server: somaxconn=128, syn_backlog=128, fin_timeout=60",
            evidence_locations=["sysctl-db-tuned.conf", "sysctl-app-server.conf"],
            reasoning_required="Multi-parameter network tuning comparison across server roles",
        ),
        EvalQuestion(
            id="CR-114",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How does the cpus resource limit for core-api compare across prod, staging, and CI compose files?",
            reference_answer="Prod: 1.0, Staging: 0.5, CI: 1.0",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.cpus", "compose-staging.yaml -> services.core-api.cpus", "compose-ci.yaml -> services.core-api-test.cpus"],
            reasoning_required="Compare CPU allocation across environments",
        ),
        EvalQuestion(
            id="CR-115",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="Which Ansible inventory groups exist in the prod inventory and how many hosts are in each?",
            reference_answer="app_servers: 3 hosts, web_servers: 1 host, db_servers: 3 hosts, legacy_db_servers: 1 host, edge_servers: 1 host, monitoring_servers: 1 host, worker_servers: 1 host",
            evidence_locations=["ansible-inventory-prod.yaml -> all.children"],
            reasoning_required="Count and enumerate all Ansible inventory groups and their members",
        ),
        EvalQuestion(
            id="CR-116",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="Which Redis DB index does each app config use?",
            reference_answer="app-config-auth-jwt.json uses DB 0, app-config-billing.json uses DB 1, app-config-search.json uses DB 2",
            evidence_locations=["app-config-auth-jwt.json -> redis.url", "app-config-billing.json -> redis.url", "app-config-search.json -> redis.url"],
            reasoning_required="Cross-reference Redis DB selection across app configs",
        ),
        EvalQuestion(
            id="CR-117",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How do the PostgreSQL logging parameters (log_connections, log_disconnections, log_lock_waits) compare between primary and dev configs?",
            reference_answer="Primary: connections=on, disconnections=on, lock_waits=on. Dev: connections=on, disconnections=on, lock_waits=off",
            evidence_locations=["pg-prod-primary.conf -> log_connections", "pg-dev.conf -> log_connections"],
            reasoning_required="Multi-parameter logging comparison between production and development",
        ),
        EvalQuestion(
            id="CR-118",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How does the number of scrape jobs change from dev to staging to prod Prometheus configs?",
            reference_answer="Dev: 4 jobs (prometheus, core-api, web-app, node-exporter). Staging: 9 jobs (adds auth-service, legacy-dashboard, postgres, mariadb, traefik). Prod: 16 jobs (adds billing-api, notification-svc, search-svc, admin-panel, analytics-worker, and separate postgres-primary/replica/analytics)",
            evidence_locations=["prometheus-dev.yaml -> scrape_configs", "prometheus-staging.yaml -> scrape_configs", "prometheus-prod.yaml -> scrape_configs"],
            reasoning_required="Track scrape target growth across all Prometheus environments",
        ),
        EvalQuestion(
            id="CR-119",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How do the Docker Compose image tagging strategies differ across prod, staging, dev, and CI?",
            reference_answer="Prod uses exact versions (e.g. 2.1.0), Staging uses release candidates (e.g. 2.1.0-rc1), Dev uses 'dev' tag, CI uses ${CI_COMMIT_SHA} for commit-based tagging",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.image", "compose-staging.yaml -> services.core-api.image", "compose-dev.yaml -> services.core-api.image", "compose-ci.yaml -> services.core-api-test.image"],
            reasoning_required="Compare image tagging conventions across all environments",
        ),
        EvalQuestion(
            id="CR-120",
            question_class=EvalQuestionClass.CROSS_REFERENCE,
            difficulty=Difficulty.HARD,
            question="How do the number of subnet CIDRs scale from dev to staging to prod in the tfvars files?",
            reference_answer="Dev: 1 subnet (10.2.1.0/24), Staging: 1 subnet (10.1.1.0/24), Prod: 3 subnets (10.0.1.0/24, 10.0.2.0/24, 10.0.3.0/24)",
            evidence_locations=["tfvars-dev.json -> networking.subnet_cidrs", "tfvars-staging.json -> networking.subnet_cidrs", "tfvars-prod.json -> networking.subnet_cidrs"],
            reasoning_required="Compare network subnet allocation across all Terraform environments",
        ),
    ]


def operational_inference() -> list[EvalQuestion]:
    """Return 120 OPERATIONAL_INFERENCE questions (prefix OI)."""
    return [
        # ── EASY (OI-001 .. OI-048) ─────────────────────────────────────────────
        EvalQuestion(
            id="OI-001",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="If the PostgreSQL primary crashes, can the replica take over as a hot standby?",
            reference_answer="Yes, the replica has hot_standby=on, so it can serve read queries. However it has archive_mode=off and SSL=off, so it is not configured identically to the primary.",
            evidence_locations=["pg-prod-replica.conf -> hot_standby", "pg-prod-replica.conf -> archive_mode", "pg-prod-replica.conf -> ssl"],
            reasoning_required="Infer failover capability from replication settings",
        ),
        EvalQuestion(
            id="OI-002",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Would the emergency rollback playbook be able to restart system services that require root?",
            reference_answer="No, ansible-emergency-rollback.yaml has become=false globally and no per-task become overrides, so it cannot restart services requiring root privileges.",
            evidence_locations=["ansible-emergency-rollback.yaml -> become"],
            reasoning_required="Infer operational impact from Ansible privilege escalation settings",
        ),
        EvalQuestion(
            id="OI-003",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Can users SSH into the staging server using password authentication?",
            reference_answer="Yes, sshd-staging.conf has PasswordAuthentication=yes.",
            evidence_locations=["sshd-staging.conf -> PasswordAuthentication"],
            reasoning_required="Infer SSH access method from sshd settings",
        ),
        EvalQuestion(
            id="OI-004",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="If the Traefik staging instance is compromised, could an attacker use it to reach backend services with invalid TLS certificates?",
            reference_answer="Yes, traefik-staging.yaml has serversTransport.insecureSkipVerify=true, which means Traefik will accept any backend certificate.",
            evidence_locations=["traefik-staging.yaml -> serversTransport.insecureSkipVerify"],
            reasoning_required="Infer TLS security posture from Traefik configuration",
        ),
        EvalQuestion(
            id="OI-005",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Will the legacy dashboard service automatically restart if it crashes?",
            reference_answer="Yes, systemd-legacy-dashboard.conf has Restart=always, so systemd will restart it regardless of exit status.",
            evidence_locations=["systemd-legacy-dashboard.conf -> [Service].Restart"],
            reasoning_required="Infer service reliability from systemd restart policy",
        ),
        EvalQuestion(
            id="OI-006",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the MariaDB legacy-main instance susceptible to encoding issues with non-Latin characters?",
            reference_answer="Yes, maria-legacy-main.cnf uses character_set_server=latin1, which cannot properly store non-Latin characters (e.g. CJK, Cyrillic).",
            evidence_locations=["maria-legacy-main.cnf -> character_set_server"],
            reasoning_required="Infer data integrity risk from character set configuration",
        ),
        EvalQuestion(
            id="OI-007",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Will the Docker Compose dev environment core-api service start if the postgres service is not running?",
            reference_answer="No, compose-dev.yaml has a depends_on for postgres with condition=service_started, so core-api will wait for postgres to start.",
            evidence_locations=["compose-dev.yaml -> services.core-api.depends_on"],
            reasoning_required="Infer service startup dependency from Docker Compose configuration",
        ),
        EvalQuestion(
            id="OI-008",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Are database backups scheduled automatically in the Ridgeline fleet?",
            reference_answer="Yes, systemd-backup-timer.conf schedules backups daily at 03:00 via OnCalendar=*-*-* 03:00:00.",
            evidence_locations=["systemd-backup-timer.conf -> [Timer].OnCalendar"],
            reasoning_required="Infer backup automation from systemd timer configuration",
        ),
        EvalQuestion(
            id="OI-009",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Can the dev sshd server be used for X11 forwarding (GUI tunneling)?",
            reference_answer="Yes, sshd-dev.conf has X11Forwarding=yes.",
            evidence_locations=["sshd-dev.conf -> X11Forwarding"],
            reasoning_required="Infer remote GUI capability from sshd forwarding settings",
        ),
        EvalQuestion(
            id="OI-010",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Will the CI compose environment persist data between test runs?",
            reference_answer="No, compose-ci.yaml does not define any volumes and services use restart=no, so no data persists between runs.",
            evidence_locations=["compose-ci.yaml"],
            reasoning_required="Infer data persistence from Docker Compose volume configuration",
        ),
        EvalQuestion(
            id="OI-011",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the PostgreSQL analytics instance optimized for read-heavy workloads?",
            reference_answer="Yes, pg-prod-analytics.conf has very high shared_buffers (16GB), work_mem (256MB), max_parallel_workers (16), and max_parallel_workers_per_gather (8), indicating optimization for heavy analytical queries.",
            evidence_locations=["pg-prod-analytics.conf -> shared_buffers", "pg-prod-analytics.conf -> work_mem", "pg-prod-analytics.conf -> max_parallel_workers_per_gather"],
            reasoning_required="Infer workload optimization from PostgreSQL memory and parallelism settings",
        ),
        EvalQuestion(
            id="OI-012",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Can the dev Docker daemon pull images from the internal Ridgeline registry without TLS?",
            reference_answer="Yes, docker-daemon-dev.json has registry.ridgeline.internal:5000 in insecure-registries.",
            evidence_locations=["docker-daemon-dev.json -> insecure-registries"],
            reasoning_required="Infer registry access from Docker daemon configuration",
        ),
        EvalQuestion(
            id="OI-013",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the prod Prometheus instance sending metrics to a remote storage backend?",
            reference_answer="Yes, prometheus-prod.yaml has a remote_write section sending to http://remote-write.ridgeline.io/api/v1/write.",
            evidence_locations=["prometheus-prod.yaml -> remote_write"],
            reasoning_required="Infer metric storage architecture from Prometheus remote_write config",
        ),
        EvalQuestion(
            id="OI-014",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Would the PostgreSQL migration instance be suitable for production traffic?",
            reference_answer="No, pg-migration.conf has max_connections=20, listen_addresses=localhost, autovacuum=off, and synchronous_commit=off, making it unsuitable for production workloads.",
            evidence_locations=["pg-migration.conf -> max_connections", "pg-migration.conf -> listen_addresses", "pg-migration.conf -> autovacuum"],
            reasoning_required="Infer production suitability from PostgreSQL configuration parameters",
        ),
        EvalQuestion(
            id="OI-015",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the bastion host configured to allow SSH tunneling for port forwarding?",
            reference_answer="Yes, sshd-bastion.conf has AllowTcpForwarding=yes and AllowAgentForwarding=yes.",
            evidence_locations=["sshd-bastion.conf -> AllowTcpForwarding", "sshd-bastion.conf -> AllowAgentForwarding"],
            reasoning_required="Infer tunneling capability from sshd forwarding settings",
        ),
        EvalQuestion(
            id="OI-016",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Would slow queries be caught in the MariaDB legacy-main instance?",
            reference_answer="No, maria-legacy-main.cnf has slow_query_log=OFF, so slow queries are not being recorded.",
            evidence_locations=["maria-legacy-main.cnf -> slow_query_log"],
            reasoning_required="Infer query performance monitoring from MariaDB logging settings",
        ),
        EvalQuestion(
            id="OI-017",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Can the PostgreSQL primary accept remote connections?",
            reference_answer="Yes, pg-prod-primary.conf has listen_addresses='*', which means it listens on all interfaces.",
            evidence_locations=["pg-prod-primary.conf -> listen_addresses"],
            reasoning_required="Infer network accessibility from PostgreSQL connection settings",
        ),
        EvalQuestion(
            id="OI-018",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the core-api service in the prod compose configuration discoverable by Traefik?",
            reference_answer="Yes, compose-app-prod-01.yaml has the label traefik.enable=true on the core-api service.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.labels"],
            reasoning_required="Infer service discovery from Docker labels and Traefik configuration",
        ),
        EvalQuestion(
            id="OI-019",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the sysctl-db-tuned.conf optimized for database workloads compared to a default configuration?",
            reference_answer="Yes, it has vm.swappiness=10 (reduced swap pressure), net.core.somaxconn=65535 (high connection backlog), and vm.nr_hugepages=1024 (preallocated huge pages for database shared memory).",
            evidence_locations=["sysctl-db-tuned.conf -> vm.swappiness", "sysctl-db-tuned.conf -> net.core.somaxconn", "sysctl-db-tuned.conf -> vm.nr_hugepages"],
            reasoning_required="Infer database optimization from sysctl tuning parameters",
        ),
        EvalQuestion(
            id="OI-020",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Will the staging compose environment automatically recover services after a host reboot?",
            reference_answer="Yes, compose-staging.yaml uses restart=unless-stopped for all services, so they will restart after a reboot unless explicitly stopped beforehand.",
            evidence_locations=["compose-staging.yaml -> services.core-api.restart"],
            reasoning_required="Infer post-reboot behaviour from Docker restart policy",
        ),
        EvalQuestion(
            id="OI-021",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the auth-jwt service protected against brute-force API attacks?",
            reference_answer="Yes, app-config-auth-jwt.json has rate_limiting enabled with 1000 requests_per_minute and burst=50, plus mfa_required=true in features.",
            evidence_locations=["app-config-auth-jwt.json -> rate_limiting", "app-config-auth-jwt.json -> features.mfa_required"],
            reasoning_required="Infer security posture from rate limiting and MFA settings",
        ),
        EvalQuestion(
            id="OI-022",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Would a developer using the dev compose environment be able to hot-reload code changes?",
            reference_answer="Yes, compose-dev.yaml mounts local directories as volumes (./core-api:/app, ./web-app:/app) and has stdin_open=true and tty=true, enabling hot-reload.",
            evidence_locations=["compose-dev.yaml -> services.core-api.volumes", "compose-dev.yaml -> services.core-api.stdin_open"],
            reasoning_required="Infer development workflow from Docker Compose volume mounts and terminal settings",
        ),
        EvalQuestion(
            id="OI-023",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the Galera node's innodb_flush_log_at_trx_commit=2 a concern for data durability?",
            reference_answer="Yes, setting innodb_flush_log_at_trx_commit=2 means the log buffer is flushed once per second rather than at each commit. This improves performance but risks losing up to 1 second of transactions on crash. Standard non-Galera production databases use value 1.",
            evidence_locations=["maria-galera-node.cnf -> innodb_flush_log_at_trx_commit"],
            reasoning_required="Infer data durability risk from InnoDB flush configuration",
        ),
        EvalQuestion(
            id="OI-024",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Can the PostgreSQL dev instance be used to debug query performance issues?",
            reference_answer="Yes, pg-dev.conf has log_statement=all and log_min_duration_statement=0, which logs all queries with their durations.",
            evidence_locations=["pg-dev.conf -> log_statement", "pg-dev.conf -> log_min_duration_statement"],
            reasoning_required="Infer debugging capability from PostgreSQL logging configuration",
        ),
        EvalQuestion(
            id="OI-025",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Would the core-api systemd service restart if it exits with a zero exit code?",
            reference_answer="No, systemd-core-api.conf has Restart=on-failure, which only restarts on non-zero exit codes. A clean exit (code 0) would not trigger a restart.",
            evidence_locations=["systemd-core-api.conf -> [Service].Restart"],
            reasoning_required="Infer restart behaviour from systemd Restart policy semantics",
        ),
        EvalQuestion(
            id="OI-026",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is cloud-init configured to automatically update packages on the app server?",
            reference_answer="Yes, cloud-init-app-server.yaml has package_update=true and package_upgrade=true.",
            evidence_locations=["cloud-init-app-server.yaml -> package_update", "cloud-init-app-server.yaml -> package_upgrade"],
            reasoning_required="Infer package management behaviour from cloud-init settings",
        ),
        EvalQuestion(
            id="OI-027",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the prod Docker daemon configured for live restoring containers during daemon restarts?",
            reference_answer="Yes, docker-daemon-prod.json has live-restore=true.",
            evidence_locations=["docker-daemon-prod.json -> live-restore"],
            reasoning_required="Infer container availability during daemon upgrades from Docker daemon config",
        ),
        EvalQuestion(
            id="OI-028",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Will the PostgreSQL primary log slow queries that take more than 500ms?",
            reference_answer="Yes, pg-prod-primary.conf has log_min_duration_statement=500, so any query taking 500ms or more will be logged.",
            evidence_locations=["pg-prod-primary.conf -> log_min_duration_statement"],
            reasoning_required="Infer slow query logging from PostgreSQL log threshold",
        ),
        EvalQuestion(
            id="OI-029",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the search service able to handle more API requests per minute than the auth service?",
            reference_answer="Yes, app-config-search.json allows 2000 requests_per_minute while app-config-auth-jwt.json allows 1000.",
            evidence_locations=["app-config-search.json -> rate_limiting.requests_per_minute", "app-config-auth-jwt.json -> rate_limiting.requests_per_minute"],
            reasoning_required="Compare rate limits to infer throughput capacity",
        ),
        EvalQuestion(
            id="OI-030",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Does the Prometheus prod config have alerting configured?",
            reference_answer="Yes, prometheus-prod.yaml has an alerting section with an alertmanager target at mon-prod-01:9093.",
            evidence_locations=["prometheus-prod.yaml -> alerting"],
            reasoning_required="Infer alerting capability from Prometheus alerting configuration",
        ),
        EvalQuestion(
            id="OI-031",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the billing API rate limited more strictly than the auth service?",
            reference_answer="Yes, app-config-billing.json allows only 500 requests_per_minute while app-config-auth-jwt.json allows 1000.",
            evidence_locations=["app-config-billing.json -> rate_limiting.requests_per_minute", "app-config-auth-jwt.json -> rate_limiting.requests_per_minute"],
            reasoning_required="Compare rate limits across services",
        ),
        EvalQuestion(
            id="OI-032",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the production sshd configuration enforcing key-based authentication only?",
            reference_answer="Yes, sshd-prod.conf has PasswordAuthentication=no and PubkeyAuthentication=yes, enforcing key-based access.",
            evidence_locations=["sshd-prod.conf -> PasswordAuthentication", "sshd-prod.conf -> PubkeyAuthentication"],
            reasoning_required="Infer authentication method from sshd settings",
        ),
        EvalQuestion(
            id="OI-033",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Can the Traefik prod API dashboard be accessed without authentication?",
            reference_answer="No, traefik-prod-static.yaml has api.insecure=false, meaning the dashboard requires proper authentication.",
            evidence_locations=["traefik-prod-static.yaml -> api.insecure"],
            reasoning_required="Infer dashboard access control from Traefik API settings",
        ),
        EvalQuestion(
            id="OI-034",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Will the backup service run as a long-lived daemon or as a one-time job?",
            reference_answer="One-time job. systemd-backup-svc.conf has Type=oneshot, meaning it runs its ExecStart command once and then exits. It is triggered by a timer.",
            evidence_locations=["systemd-backup-svc.conf -> [Service].Type"],
            reasoning_required="Infer service execution model from systemd Type setting",
        ),
        EvalQuestion(
            id="OI-035",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is Docker started automatically on app servers provisioned by cloud-init?",
            reference_answer="Yes, cloud-init-app-server.yaml has runcmd directives to enable and start docker.",
            evidence_locations=["cloud-init-app-server.yaml -> runcmd"],
            reasoning_required="Infer service startup from cloud-init run commands",
        ),
        EvalQuestion(
            id="OI-036",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the PostgreSQL migration instance optimized for bulk data operations?",
            reference_answer="Yes, pg-migration.conf has high work_mem (64MB), high maintenance_work_mem (1GB), autovacuum=off (to avoid interference), and synchronous_commit=off (for faster writes).",
            evidence_locations=["pg-migration.conf -> work_mem", "pg-migration.conf -> maintenance_work_mem", "pg-migration.conf -> autovacuum", "pg-migration.conf -> synchronous_commit"],
            reasoning_required="Infer bulk-load optimization from PostgreSQL parameters",
        ),
        EvalQuestion(
            id="OI-037",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the Traefik dashboard accessible insecurely in staging?",
            reference_answer="Yes, traefik-staging.yaml has api.insecure=true and api.debug=true, exposing the dashboard without authentication.",
            evidence_locations=["traefik-staging.yaml -> api.insecure", "traefik-staging.yaml -> api.debug"],
            reasoning_required="Infer dashboard exposure from Traefik API settings",
        ),
        EvalQuestion(
            id="OI-038",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Are Docker container logs rotated in the prod environment?",
            reference_answer="Yes, docker-daemon-prod.json configures json-file log driver with max-size=50m and max-file=5.",
            evidence_locations=["docker-daemon-prod.json -> log-driver", "docker-daemon-prod.json -> log-opts"],
            reasoning_required="Infer log rotation from Docker daemon log configuration",
        ),
        EvalQuestion(
            id="OI-039",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Does the Prometheus dev config have alerting configured?",
            reference_answer="No, prometheus-dev.yaml has no alerting section, so no alerts are configured.",
            evidence_locations=["prometheus-dev.yaml"],
            reasoning_required="Infer alerting capability from absence of Prometheus alerting configuration",
        ),
        EvalQuestion(
            id="OI-040",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Would the bastion SSH server allow more than one concurrent session from a user?",
            reference_answer="No, sshd-bastion.conf has MaxSessions=1, limiting each user to a single session.",
            evidence_locations=["sshd-bastion.conf -> MaxSessions"],
            reasoning_required="Infer session behaviour from sshd MaxSessions setting",
        ),
        EvalQuestion(
            id="OI-041",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Would a prod compose core-api container still be healthy during a 5-second startup delay?",
            reference_answer="Yes, compose-app-prod-01.yaml has healthcheck.start_period=10s, giving the container 10 seconds before health checks begin.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.healthcheck.start_period"],
            reasoning_required="Infer health check grace period from Docker Compose healthcheck config",
        ),
        EvalQuestion(
            id="OI-042",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the MariaDB Galera node's sync_binlog=0 a concern for data integrity?",
            reference_answer="Yes, sync_binlog=0 means the binary log is not synced to disk on each commit. In a Galera cluster this is a common trade-off for performance, but a crash could lose binlog events.",
            evidence_locations=["maria-galera-node.cnf -> sync_binlog"],
            reasoning_required="Infer data integrity risk from MariaDB sync_binlog setting",
        ),
        EvalQuestion(
            id="OI-043",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Can the search service handle fuzzy search queries?",
            reference_answer="Yes, app-config-search.json has features.fuzzy_search=true.",
            evidence_locations=["app-config-search.json -> features.fuzzy_search"],
            reasoning_required="Infer service capability from feature flag configuration",
        ),
        EvalQuestion(
            id="OI-044",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Will the web-app dev container run in development mode with hot reload?",
            reference_answer="Yes, compose-dev.yaml specifies command: npm run dev for the web-app service with source code mounted via volume.",
            evidence_locations=["compose-dev.yaml -> services.web-app.command", "compose-dev.yaml -> services.web-app.volumes"],
            reasoning_required="Infer development workflow from Docker Compose command and volume configuration",
        ),
        EvalQuestion(
            id="OI-045",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the prod Docker daemon configured to expose metrics for monitoring?",
            reference_answer="Yes, docker-daemon-prod.json has metrics-addr=0.0.0.0:9323.",
            evidence_locations=["docker-daemon-prod.json -> metrics-addr"],
            reasoning_required="Infer metrics exposure from Docker daemon configuration",
        ),
        EvalQuestion(
            id="OI-046",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Would a PostgreSQL primary connection attempt be rejected after 200 active connections?",
            reference_answer="Yes, pg-prod-primary.conf has max_connections=200, so the 201st non-superuser connection would be rejected.",
            evidence_locations=["pg-prod-primary.conf -> max_connections"],
            reasoning_required="Infer connection limit behaviour from PostgreSQL setting",
        ),
        EvalQuestion(
            id="OI-047",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Is the billing app using a MariaDB database on the legacy server?",
            reference_answer="Yes, app-config-billing.json connects to db-legacy-01 on port 3306 (MariaDB).",
            evidence_locations=["app-config-billing.json -> database.host", "app-config-billing.json -> database.port"],
            reasoning_required="Infer database backend from app config connection details",
        ),
        EvalQuestion(
            id="OI-048",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.EASY,
            question="Will Traefik in production automatically discover Docker containers?",
            reference_answer="Yes, traefik-prod-static.yaml has providers.docker.exposedByDefault=true and watch=true.",
            evidence_locations=["traefik-prod-static.yaml -> providers.docker"],
            reasoning_required="Infer service discovery from Traefik Docker provider configuration",
        ),
        # ── MEDIUM (OI-049 .. OI-096) ────────────────────────────────────────────
        EvalQuestion(
            id="OI-049",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="If the PostgreSQL primary fails and the replica is promoted, what operational issues might arise?",
            reference_answer="The replica has SSL=off (primary has SSL=on), archive_mode=off, lower maintenance_work_mem (64MB vs 512MB), and log_statement=none. Clients expecting SSL connections would fail, WAL archiving would stop, and query logging would be lost.",
            evidence_locations=["pg-prod-primary.conf -> ssl", "pg-prod-replica.conf -> ssl", "pg-prod-replica.conf -> archive_mode"],
            reasoning_required="Infer multi-dimensional failover risks from configuration differences",
        ),
        EvalQuestion(
            id="OI-050",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="What would happen if the Galera node crashes mid-transaction?",
            reference_answer="With innodb_flush_log_at_trx_commit=2 and sync_binlog=0, up to 1 second of committed transactions could be lost. However, Galera's wsrep replication provides cluster-level consistency, so other nodes would have the data.",
            evidence_locations=["maria-galera-node.cnf -> innodb_flush_log_at_trx_commit", "maria-galera-node.cnf -> sync_binlog"],
            reasoning_required="Infer crash recovery implications from MariaDB durability and Galera replication settings",
        ),
        EvalQuestion(
            id="OI-051",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Would running the emergency-rollback playbook work correctly to restart services on the app servers?",
            reference_answer="No, the playbook has become=false globally with no per-task become overrides. The systemd task to restart services typically requires root privileges, which the playbook cannot escalate to.",
            evidence_locations=["ansible-emergency-rollback.yaml -> become", "ansible-emergency-rollback.yaml -> tasks"],
            reasoning_required="Infer privilege escalation failure from Ansible become settings and task requirements",
        ),
        EvalQuestion(
            id="OI-052",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="If the PostgreSQL analytics server experiences heavy query load, how well can it parallelize queries compared to the primary?",
            reference_answer="Much better. Analytics has max_parallel_workers=16 and max_parallel_workers_per_gather=8, while primary has max_parallel_workers=8 and max_parallel_workers_per_gather=4. Analytics can use twice as many parallel workers per query.",
            evidence_locations=["pg-prod-analytics.conf -> max_parallel_workers_per_gather", "pg-prod-primary.conf -> max_parallel_workers_per_gather"],
            reasoning_required="Compare parallelism capacity from PostgreSQL worker settings",
        ),
        EvalQuestion(
            id="OI-053",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="What is the operational risk of having PasswordAuthentication=yes in the staging sshd config?",
            reference_answer="Password authentication is vulnerable to brute-force attacks and credential stuffing. Combined with MaxAuthTries=6 (double the prod value of 3) and no AllowUsers restriction, the staging server has a larger attack surface than production.",
            evidence_locations=["sshd-staging.conf -> PasswordAuthentication", "sshd-staging.conf -> MaxAuthTries"],
            reasoning_required="Assess security risk from sshd authentication and access control settings",
        ),
        EvalQuestion(
            id="OI-054",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the sysctl tuning on db-tuned servers improve PostgreSQL performance compared to default app-server settings?",
            reference_answer="vm.swappiness=10 (vs 60) reduces swap thrashing for large shared_buffers. net.core.somaxconn=65535 (vs 128) handles connection surges. vm.nr_hugepages=1024 pre-allocates huge pages for PostgreSQL shared memory. kernel.shmmax=68GB supports large shared_buffers. tcp_fin_timeout=15 (vs 60) recycles connections faster.",
            evidence_locations=["sysctl-db-tuned.conf", "sysctl-app-server.conf"],
            reasoning_required="Infer performance benefits from sysctl parameter differences",
        ),
        EvalQuestion(
            id="OI-055",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="If the MariaDB legacy-main instance needs to store customer names in Chinese characters, would it work correctly?",
            reference_answer="No, maria-legacy-main.cnf uses character_set_server=latin1 and collation_server=latin1_swedish_ci, which cannot properly encode Chinese characters. Data would be corrupted or rejected. A migration to utf8mb4 would be required.",
            evidence_locations=["maria-legacy-main.cnf -> character_set_server", "maria-legacy-main.cnf -> collation_server"],
            reasoning_required="Infer character encoding limitations from MariaDB character set configuration",
        ),
        EvalQuestion(
            id="OI-056",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Could the Traefik staging configuration be exploited to access internal services?",
            reference_answer="Yes, the staging Traefik has api.insecure=true (dashboard exposed without auth), api.debug=true (detailed error info), and serversTransport.insecureSkipVerify=true (accepts any TLS cert). An attacker could view the dashboard, enumerate routes, and potentially MITM backend services.",
            evidence_locations=["traefik-staging.yaml -> api.insecure", "traefik-staging.yaml -> api.debug", "traefik-staging.yaml -> serversTransport.insecureSkipVerify"],
            reasoning_required="Assess attack surface from Traefik security settings",
        ),
        EvalQuestion(
            id="OI-057",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="What would happen if the backup service takes longer than its timer interval to complete?",
            reference_answer="The systemd-backup-svc.conf has TimeoutStartSec=600 (10 minutes). The timer fires daily at 03:00. If the backup exceeds 10 minutes, systemd will kill it. Since it is Type=oneshot with RemainAfterExit=yes, a new invocation would not overlap but the backup might be incomplete.",
            evidence_locations=["systemd-backup-svc.conf -> [Service].TimeoutStartSec", "systemd-backup-timer.conf -> [Timer].OnCalendar"],
            reasoning_required="Infer backup reliability from systemd timer and service timeout settings",
        ),
        EvalQuestion(
            id="OI-058",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="What is the risk of having the PostgreSQL replica configured without SSL while the primary has SSL enabled?",
            reference_answer="Applications connecting to the replica would transmit data in plaintext, including query results and credentials. If the network is untrusted, this is a data exposure risk. It also means applications cannot use a single SSL-required connection string for both primary and replica.",
            evidence_locations=["pg-prod-primary.conf -> ssl", "pg-prod-replica.conf -> ssl"],
            reasoning_required="Assess security risk from inconsistent SSL configuration across PostgreSQL roles",
        ),
        EvalQuestion(
            id="OI-059",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="What is the impact of having autovacuum disabled on the migration instance?",
            reference_answer="Disabling autovacuum (pg-migration.conf: autovacuum=off) prevents automatic cleanup of dead tuples. This is appropriate for bulk data loads (avoids I/O overhead) but if the instance is used for ongoing queries, table bloat and performance degradation would occur. Autovacuum should be re-enabled after migration completes.",
            evidence_locations=["pg-migration.conf -> autovacuum"],
            reasoning_required="Infer maintenance implications from PostgreSQL autovacuum setting",
        ),
        EvalQuestion(
            id="OI-060",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How does the Docker dev environment differ from prod in terms of debugging capability?",
            reference_answer="Dev has stdin_open=true and tty=true (allows interactive debugging), source code mounted as volumes (hot reload), DEBUG=true environment variable, and uses dev image tags. Prod has none of these, using fixed image versions and no volume mounts.",
            evidence_locations=["compose-dev.yaml -> services.core-api", "compose-app-prod-01.yaml -> services.core-api"],
            reasoning_required="Compare development vs production container configurations for debugging",
        ),
        EvalQuestion(
            id="OI-061",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="What would be the impact of a disk failure on the PostgreSQL primary's WAL archives?",
            reference_answer="pg-prod-primary.conf archives WAL with archive_command='cp %p /archive/%f'. If /archive is on the same disk that fails, both the database and archives would be lost. The replica has archive_mode=off, so it cannot provide archived WAL segments for point-in-time recovery.",
            evidence_locations=["pg-prod-primary.conf -> archive_command", "pg-prod-primary.conf -> archive_mode", "pg-prod-replica.conf -> archive_mode"],
            reasoning_required="Infer disaster recovery risk from WAL archiving configuration",
        ),
        EvalQuestion(
            id="OI-062",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Can the CI compose environment properly test the full application stack including the billing API?",
            reference_answer="Partially. compose-ci.yaml only includes core-api-test, postgres, mariadb, and redis services. It does not include auth-service, web-app, billing-api, or other services. The billing API and its MariaDB dependency exist but the billing API service itself is not defined in the CI compose.",
            evidence_locations=["compose-ci.yaml -> services"],
            reasoning_required="Assess test coverage from CI compose service definitions",
        ),
        EvalQuestion(
            id="OI-063",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How would the health check behaviour of core-api differ between prod and staging during container startup?",
            reference_answer="In prod, the healthcheck has start_period=10s, giving the container 10 seconds before health checks begin. In staging, start_period=0s, meaning health checks start immediately. A slow startup in staging could cause the container to be marked unhealthy prematurely.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.healthcheck.start_period", "compose-staging.yaml -> services.core-api.healthcheck.start_period"],
            reasoning_required="Compare health check timing and infer startup reliability implications",
        ),
        EvalQuestion(
            id="OI-064",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the dev Prometheus configuration sufficient for identifying performance issues in the fleet?",
            reference_answer="No, prometheus-dev.yaml only monitors 3 services (core-api, web-app, node-exporter) with a 1-minute scrape interval. There is no alerting, no remote_write for long-term storage, and it misses auth-service, billing, databases, and other services that are monitored in prod.",
            evidence_locations=["prometheus-dev.yaml -> scrape_configs", "prometheus-dev.yaml -> global.scrape_interval"],
            reasoning_required="Assess monitoring adequacy from Prometheus scrape configuration and compare to prod",
        ),
        EvalQuestion(
            id="OI-065",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Would a rolling update of the core-api in the prod compose file cause downtime?",
            reference_answer="Minimal downtime. compose-app-prod-01.yaml has deploy.replicas=2 with update_config.parallelism=1 and order=stop-first. During a rolling update, one replica is stopped then updated, while the other continues serving. However, stop-first means there is a brief period with only 1 replica.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.deploy"],
            reasoning_required="Infer update strategy impact from Docker Compose deploy configuration",
        ),
        EvalQuestion(
            id="OI-066",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="If the billing API experiences high load, could its MariaDB connection pool become a bottleneck?",
            reference_answer="Potentially. app-config-billing.json has a pool_size of 10, connecting to maria-legacy-billing.cnf which allows max_connections=200. The pool is relatively small but the MariaDB side has capacity. The billing API rate limit of 500/min may protect against pool exhaustion.",
            evidence_locations=["app-config-billing.json -> database.pool_size", "maria-legacy-billing.cnf -> max_connections", "app-config-billing.json -> rate_limiting"],
            reasoning_required="Assess connection pool capacity from app config and database settings",
        ),
        EvalQuestion(
            id="OI-067",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="What is the likely reason the PostgreSQL analytics config uses huge_pages=on instead of the default 'try'?",
            reference_answer="With 16GB shared_buffers, huge pages provide significant performance benefits by reducing TLB misses and page table overhead. Setting huge_pages=on (rather than try) ensures the instance fails to start if huge pages are unavailable, preventing the performance degradation that would result from using standard pages for such a large allocation.",
            evidence_locations=["pg-prod-analytics.conf -> huge_pages", "pg-prod-analytics.conf -> shared_buffers"],
            reasoning_required="Infer performance optimization rationale from PostgreSQL memory settings",
        ),
        EvalQuestion(
            id="OI-068",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="What operational risks exist with the current Prometheus monitoring gap between staging and prod?",
            reference_answer="Staging monitors 8 scrape targets while prod monitors 15. Services like billing-api, notification-svc, search-svc, admin-panel, and analytics-worker are only monitored in prod. Issues with these services cannot be detected in staging before promotion to prod.",
            evidence_locations=["prometheus-staging.yaml -> scrape_configs", "prometheus-prod.yaml -> scrape_configs"],
            reasoning_required="Identify monitoring coverage gaps between environments",
        ),
        EvalQuestion(
            id="OI-069",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Would the PostgreSQL primary's WAL archiving protect against data loss during a full server failure?",
            reference_answer="Partially. pg-prod-primary.conf has archive_mode=on with archive_command='cp %p /archive/%f'. If /archive is on a separate disk/server, point-in-time recovery is possible. However, the archive_command uses a simple cp, which does not verify integrity or copy to remote storage, making it vulnerable to local disk failures.",
            evidence_locations=["pg-prod-primary.conf -> archive_mode", "pg-prod-primary.conf -> archive_command"],
            reasoning_required="Assess disaster recovery capability from WAL archive configuration",
        ),
        EvalQuestion(
            id="OI-070",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Why would the legacy dashboard systemd service use Restart=always instead of Restart=on-failure?",
            reference_answer="Restart=always restarts the service regardless of exit code, including clean exits. For a legacy Flask application that might exit cleanly due to bugs or memory issues, Restart=always ensures maximum uptime. This is more aggressive than the Restart=on-failure used by the modern core-api service.",
            evidence_locations=["systemd-legacy-dashboard.conf -> [Service].Restart", "systemd-core-api.conf -> [Service].Restart"],
            reasoning_required="Compare systemd restart policies and infer rationale for legacy service handling",
        ),
        EvalQuestion(
            id="OI-071",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the app-config-auth-jwt.json service's database connection secured with TLS?",
            reference_answer="No, app-config-auth-jwt.json has database.ssl=false. Although the PostgreSQL primary has SSL enabled, the auth service is not configured to use it, leaving database connections unencrypted.",
            evidence_locations=["app-config-auth-jwt.json -> database.ssl", "pg-prod-primary.conf -> ssl"],
            reasoning_required="Cross-reference app config and database TLS to identify security gap",
        ),
        EvalQuestion(
            id="OI-072",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="What is the impact of the MariaDB legacy-main having slow_query_log=OFF on performance troubleshooting?",
            reference_answer="Without slow query logging, it is impossible to identify queries exceeding the long_query_time threshold (10s). Performance issues on the legacy database would need to be diagnosed through other means (application logs, process list). This is a monitoring blind spot for the oldest database in the fleet.",
            evidence_locations=["maria-legacy-main.cnf -> slow_query_log", "maria-legacy-main.cnf -> long_query_time"],
            reasoning_required="Assess troubleshooting capability from MariaDB logging settings",
        ),
        EvalQuestion(
            id="OI-073",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="What would happen if the CI compose environment is used to run load tests?",
            reference_answer="compose-ci.yaml has core-api-test with mem_limit=2g and cpus=1.0, which matches prod. However, the postgres service only has mem_limit=512m and cpus=0.5, far less than the prod PostgreSQL. Load test results would be unreliable due to the under-provisioned database.",
            evidence_locations=["compose-ci.yaml -> services.core-api-test.mem_limit", "compose-ci.yaml -> services.postgres.mem_limit"],
            reasoning_required="Assess load testing validity from CI resource limits versus production",
        ),
        EvalQuestion(
            id="OI-074",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Can the production bastion host be used to jump to any server in the fleet?",
            reference_answer="Yes, sshd-bastion.conf allows TCP forwarding (AllowTcpForwarding=yes) and agent forwarding (AllowAgentForwarding=yes). With the AllowUsers limited to deploy and ops, authorized users can tunnel through the bastion to reach internal servers.",
            evidence_locations=["sshd-bastion.conf -> AllowTcpForwarding", "sshd-bastion.conf -> AllowAgentForwarding", "sshd-bastion.conf -> AllowUsers"],
            reasoning_required="Infer bastion jump host capability from sshd forwarding and access settings",
        ),
        EvalQuestion(
            id="OI-075",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Why does the PostgreSQL analytics instance have a higher default_statistics_target than the primary?",
            reference_answer="The analytics instance (default_statistics_target=500) runs complex analytical queries that benefit from more accurate statistics for query planning. The primary (default_statistics_target=200) handles OLTP workloads where the overhead of collecting more statistics is less justified.",
            evidence_locations=["pg-prod-analytics.conf -> default_statistics_target", "pg-prod-primary.conf -> default_statistics_target"],
            reasoning_required="Infer query optimization rationale from PostgreSQL statistics configuration",
        ),
        EvalQuestion(
            id="OI-076",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="What is the operational impact of the PostgreSQL primary logging to America/New_York timezone while the replica uses GMT?",
            reference_answer="Log correlation between primary and replica becomes more difficult because timestamps are in different timezones. During incident investigation, engineers must mentally convert timezones or use tools to normalize timestamps.",
            evidence_locations=["pg-prod-primary.conf -> log_timezone", "pg-prod-replica.conf -> log_timezone"],
            reasoning_required="Infer operational impact from inconsistent timezone configuration",
        ),
        EvalQuestion(
            id="OI-077",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the current infrastructure setup ready for a multi-region deployment based on the tfvars configuration?",
            reference_answer="No, all three tfvars (prod, staging, dev) use eu-central-1 as the region. There are no tfvars files for other regions. The networking uses a single VPC CIDR per environment, which would need expansion for multi-region.",
            evidence_locations=["tfvars-prod.json -> region", "tfvars-staging.json -> region", "tfvars-dev.json -> region"],
            reasoning_required="Assess multi-region readiness from Terraform configuration",
        ),
        EvalQuestion(
            id="OI-078",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="How would the billing service be affected if the MariaDB legacy server (db-legacy-01) goes down?",
            reference_answer="The billing API (app-config-billing.json) connects directly to db-legacy-01:3306. The systemd-legacy-dashboard.conf also depends on mariadb.service. Both would fail. The billing service has no failover configuration, no read replicas, and the legacy MariaDB has no replication (it is standalone).",
            evidence_locations=["app-config-billing.json -> database.host", "systemd-legacy-dashboard.conf -> [Unit].After"],
            reasoning_required="Trace service dependency chain to assess failure impact",
        ),
        EvalQuestion(
            id="OI-079",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Would the Prometheus prod config catch a complete outage of the search service?",
            reference_answer="Yes, prometheus-prod.yaml has a search-svc scrape job targeting app-prod-02:8004. If the search service goes down, the scrape target would become unreachable, which Prometheus records as a down target. With alerting configured, an alert would fire.",
            evidence_locations=["prometheus-prod.yaml -> scrape_configs (job_name: search-svc)", "prometheus-prod.yaml -> alerting"],
            reasoning_required="Infer monitoring coverage from Prometheus scrape targets and alerting configuration",
        ),
        EvalQuestion(
            id="OI-080",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="What is the risk of the dev Docker daemon having experimental=true enabled?",
            reference_answer="Experimental features in Docker are not considered stable and may change or be removed. Using docker-daemon-dev.json with experimental=true in dev is acceptable for testing new features, but it means dev may behave differently from prod (experimental=false), potentially causing works-in-dev-but-not-in-prod issues.",
            evidence_locations=["docker-daemon-dev.json -> experimental", "docker-daemon-prod.json -> experimental"],
            reasoning_required="Assess environment parity risk from Docker daemon feature flags",
        ),
        EvalQuestion(
            id="OI-081",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="If the PostgreSQL analytics instance runs a very large analytical query, what is the maximum memory it could use for sorting?",
            reference_answer="Each sort/hash operation can use up to work_mem=256MB, and with hash_mem_multiplier=4.0, hash operations can use up to 1024MB. With max_parallel_workers_per_gather=8, a parallel query could use 256MB * (8+1) = 2.3GB just for sorting across all workers.",
            evidence_locations=["pg-prod-analytics.conf -> work_mem", "pg-prod-analytics.conf -> hash_mem_multiplier", "pg-prod-analytics.conf -> max_parallel_workers_per_gather"],
            reasoning_required="Calculate maximum query memory from PostgreSQL work_mem and parallelism settings",
        ),
        EvalQuestion(
            id="OI-082",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the infrastructure managed by a consistent IaC tool across all environments?",
            reference_answer="Yes, all three tfvars files (prod, staging, dev) have tags.managed_by=opentofu, indicating consistent use of OpenTofu (Terraform fork) across all environments.",
            evidence_locations=["tfvars-prod.json -> tags.managed_by", "tfvars-staging.json -> tags.managed_by", "tfvars-dev.json -> tags.managed_by"],
            reasoning_required="Infer IaC consistency from Terraform tags across environments",
        ),
        EvalQuestion(
            id="OI-083",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Could the prod compose environment handle a traffic spike that requires more than 2 core-api replicas?",
            reference_answer="The compose-app-prod-01.yaml has deploy.replicas=2 with mem_limit=2g and cpus=1.0 per replica. Scaling beyond 2 would require modifying the compose file. The auth-service has replicas=1. Horizontal scaling is limited by the single-node compose architecture.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.deploy.replicas", "compose-app-prod-01.yaml -> services.core-api.mem_limit"],
            reasoning_required="Assess horizontal scalability from Docker Compose deploy configuration",
        ),
        EvalQuestion(
            id="OI-084",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Would the Ansible deploy-core-api playbook work for a fresh deployment where no service exists yet?",
            reference_answer="The playbook installs Python dependencies, deploys config, starts the service, waits for the port, and verifies health. However, become=false means it cannot install system packages, create system users, or manage systemd services that require root. It would need the base-server playbook (become=true) to run first.",
            evidence_locations=["ansible-deploy-core-api.yaml -> become", "ansible-deploy-core-api.yaml -> tasks", "ansible-base-server.yaml -> become"],
            reasoning_required="Assess deployment workflow dependencies from Ansible playbook privilege settings",
        ),
        EvalQuestion(
            id="OI-085",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the billing service's invoice_batching feature currently active?",
            reference_answer="No, app-config-billing.json has features.invoice_batching=false. The auto_retry feature is enabled, but batch processing of invoices is disabled.",
            evidence_locations=["app-config-billing.json -> features.invoice_batching"],
            reasoning_required="Infer feature status from app config feature flags",
        ),
        EvalQuestion(
            id="OI-086",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="What is the maximum number of concurrent database connections available across the entire production PostgreSQL fleet?",
            reference_answer="Primary: 200, Replica: 200, Analytics: 50, Migration: 20. Total: 470 connections across all PostgreSQL instances (though migration is localhost-only).",
            evidence_locations=["pg-prod-primary.conf -> max_connections", "pg-prod-replica.conf -> max_connections", "pg-prod-analytics.conf -> max_connections", "pg-migration.conf -> max_connections"],
            reasoning_required="Sum connection limits across all PostgreSQL instances",
        ),
        EvalQuestion(
            id="OI-087",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the Traefik metrics endpoint in prod accessible from external networks?",
            reference_answer="The metrics are exposed on the traefik entrypoint at port 8080 (traefik-prod-static.yaml). Whether it is externally accessible depends on firewall rules, but the entrypoint itself listens on :8080 with no authentication requirement since api.insecure=false only protects the dashboard.",
            evidence_locations=["traefik-prod-static.yaml -> metrics.prometheus.entryPoint", "traefik-prod-static.yaml -> entryPoints.traefik.address"],
            reasoning_required="Infer metrics exposure from Traefik entrypoint and metrics configuration",
        ),
        EvalQuestion(
            id="OI-088",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="What would be the impact on the fleet if the bastion server's SSH port (2222) is blocked by a firewall?",
            reference_answer="All SSH jump access through the bastion would be blocked. Since prod sshd only allows deploy, ops, and admins users and has AllowTcpForwarding=no, the bastion is the expected path for forwarded connections. Engineers would lose remote access to internal servers if direct SSH is not available.",
            evidence_locations=["sshd-bastion.conf -> Port", "sshd-prod.conf -> AllowTcpForwarding"],
            reasoning_required="Assess access path dependency from sshd forwarding and bastion configuration",
        ),
        EvalQuestion(
            id="OI-089",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="If the PostgreSQL primary's disk fills up, will WAL archiving help preserve data?",
            reference_answer="WAL archiving (archive_command='cp %p /archive/%f') copies WAL to /archive. If /archive is on a separate filesystem, recent transactions would be preserved. However, if the pg_wal directory fills up, PostgreSQL will stop accepting writes regardless of archive status.",
            evidence_locations=["pg-prod-primary.conf -> archive_command", "pg-prod-primary.conf -> max_wal_size"],
            reasoning_required="Infer disk space failure scenario from PostgreSQL WAL and archiving settings",
        ),
        EvalQuestion(
            id="OI-090",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Are the cloud-init app servers configured with proper user isolation for security?",
            reference_answer="Yes, cloud-init-app-server.yaml creates separate deploy and ops users with sudo access, disables root login (disable_root=true), and deletes default SSH keys (ssh_deletekeys=true). Both users have NOPASSWD sudo, which is convenient but means any compromise of these accounts gives full root access.",
            evidence_locations=["cloud-init-app-server.yaml -> users", "cloud-init-app-server.yaml -> disable_root"],
            reasoning_required="Assess user security from cloud-init user and SSH configuration",
        ),
        EvalQuestion(
            id="OI-091",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the PostgreSQL primary configured to detect long-running lock waits?",
            reference_answer="Yes, pg-prod-primary.conf has log_lock_waits=on, which logs a message when a session waits longer than deadlock_timeout for a lock. This helps identify lock contention issues in the application.",
            evidence_locations=["pg-prod-primary.conf -> log_lock_waits"],
            reasoning_required="Infer lock monitoring capability from PostgreSQL logging settings",
        ),
        EvalQuestion(
            id="OI-092",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Why does the PostgreSQL migration instance have JIT disabled?",
            reference_answer="pg-migration.conf has jit=off. During bulk data migrations, JIT compilation adds overhead to compile queries that are typically simple bulk inserts/updates and do not benefit from JIT optimization. Disabling JIT reduces CPU usage during migrations.",
            evidence_locations=["pg-migration.conf -> jit"],
            reasoning_required="Infer migration optimization rationale from PostgreSQL JIT setting",
        ),
        EvalQuestion(
            id="OI-093",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="If the MariaDB billing database experiences a surge of slow queries, would the operations team be notified?",
            reference_answer="The slow_query_log is ON in maria-legacy-billing.cnf with long_query_time=2, so slow queries are logged. Prometheus prod monitors mariadb at db-legacy-01:9104 with alerting enabled. However, alerts depend on what rules are defined in /etc/prometheus/rules/*.yaml, which we do not see in the configs.",
            evidence_locations=["maria-legacy-billing.cnf -> slow_query_log", "prometheus-prod.yaml -> scrape_configs (job_name: mariadb)", "prometheus-prod.yaml -> alerting"],
            reasoning_required="Trace monitoring and alerting chain from MariaDB logging through Prometheus",
        ),
        EvalQuestion(
            id="OI-094",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the staging compose environment a good replica of production for integration testing?",
            reference_answer="It is a reasonable but reduced replica. Staging has all core services (core-api, auth-service, web-app, legacy-dashboard, postgres, mariadb) with -rc1 image tags. However, it uses a single network vs prod's two, lower mem_limits, start_period=0s, and lacks the monitoring stack and separate Redis. It is suitable for integration testing but not for performance testing.",
            evidence_locations=["compose-staging.yaml -> services", "compose-app-prod-01.yaml -> services"],
            reasoning_required="Compare staging vs prod compose configurations to assess test fidelity",
        ),
        EvalQuestion(
            id="OI-095",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the fleet have consistent NTP configuration across all servers?",
            reference_answer="The cloud-init-app-server.yaml configures NTP using 0.pool.ntp.org and 1.pool.ntp.org, while the Ansible inventory uses ntp_server=ntp.ubuntu.com. This inconsistency means different servers may use different NTP sources.",
            evidence_locations=["cloud-init-app-server.yaml -> ntp.servers", "ansible-inventory-prod.yaml -> all.vars.ntp_server"],
            reasoning_required="Identify NTP configuration inconsistency across provisioning methods",
        ),
        EvalQuestion(
            id="OI-096",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.MEDIUM,
            question="If the core-api container in prod exceeds its 2g memory limit, what happens?",
            reference_answer="Docker will OOM-kill the container. With restart=always, Docker will immediately restart it. The deploy.restart_policy condition=any ensures restart even after OOM. However, if the memory leak persists, the container will enter a crash loop.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.mem_limit", "compose-app-prod-01.yaml -> services.core-api.restart"],
            reasoning_required="Infer OOM behaviour from Docker Compose memory and restart settings",
        ),
        # ── HARD (OI-097 .. OI-120) ──────────────────────────────────────────────
        EvalQuestion(
            id="OI-097",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What is the complete chain of services that would be affected if db-prod-01 becomes unreachable?",
            reference_answer="db-prod-01 hosts the PostgreSQL primary. Affected: core-api (compose and systemd connect to db-prod-01:5432), auth-service (compose connects to db-prod-01:5432), app-config-auth-jwt.json (database.host=db-prod-01), Prometheus postgres-primary scrape target (db-prod-01:9187). The Ansible inventory lists it in db_servers. The legacy dashboard and billing API use different hosts and would not be affected.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.environment", "systemd-core-api.conf -> Environment", "app-config-auth-jwt.json -> database.host", "prometheus-prod.yaml -> scrape_configs"],
            reasoning_required="Trace complete dependency chain from a single database host failure",
        ),
        EvalQuestion(
            id="OI-098",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What is the disaster recovery posture of the entire database layer?",
            reference_answer="PostgreSQL: Primary has archive_mode=on but archives to local disk. Replica exists but has SSL off and no archiving. Analytics and migration have no replication/archiving. MariaDB: Legacy-main, billing, and staging are standalone with no replicas. Galera has cluster-level redundancy but relaxed durability (flush=2, sync_binlog=0). Backups run daily at 03:00 via systemd timer. Prod tfvars has backup_retention_days=30, but dev has 0. Overall: partial DR with significant gaps.",
            evidence_locations=["pg-prod-primary.conf -> archive_mode", "pg-prod-replica.conf -> archive_mode", "maria-galera-node.cnf -> innodb_flush_log_at_trx_commit", "systemd-backup-timer.conf", "tfvars-prod.json -> features"],
            reasoning_required="Synthesize DR posture from database, backup, and replication configurations across the entire fleet",
        ),
        EvalQuestion(
            id="OI-099",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What would be the sequence of steps to safely perform a PostgreSQL major version upgrade on the primary?",
            reference_answer="1) Use the migration instance (pg-migration.conf) with its optimized settings (autovacuum=off, synchronous_commit=off, high work_mem/maintenance_work_mem). 2) Stop archiving on primary. 3) Promote replica for read traffic. 4) Run pg_upgrade on primary. 5) Re-enable archiving and autovacuum. 6) Re-sync replica. Key concern: the replica lacks SSL configuration parity with the primary.",
            evidence_locations=["pg-migration.conf", "pg-prod-primary.conf", "pg-prod-replica.conf"],
            reasoning_required="Plan upgrade procedure from PostgreSQL configuration details across all instances",
        ),
        EvalQuestion(
            id="OI-100",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="If an attacker compromises the staging environment, what lateral movement paths exist to production?",
            reference_answer="Staging sshd allows password auth and has no AllowUsers restriction. If compromised: 1) Staging Traefik has insecureSkipVerify=true and debug mode, revealing backend routing. 2) Staging compose secrets use weaker values but share patterns with prod. 3) No network segmentation is evident between staging and prod (same eu-central-1 region). 4) The Ansible vars files contain prod credentials. Key mitigations needed: network isolation, separate credential sets, stricter staging sshd.",
            evidence_locations=["sshd-staging.conf", "traefik-staging.yaml -> serversTransport.insecureSkipVerify", "ansible-vars-prod.yaml"],
            reasoning_required="Assess lateral movement risk from cross-environment configuration analysis",
        ),
        EvalQuestion(
            id="OI-101",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What is the total maximum log storage consumption rate across all PostgreSQL instances?",
            reference_answer="Primary: log_rotation_size=10MB, log_statement=mod, log_min_duration_statement=500ms, logging_collector=on. Analytics: same rotation, log_statement=none, log_min_duration_statement=1000ms. Replica: logging_collector=off. Dev: log_statement=all, log_min_duration_statement=0 (logs everything). Migration: log_statement=ddl. The dev instance is the highest risk for disk consumption due to logging all queries.",
            evidence_locations=["pg-prod-primary.conf -> log_statement", "pg-dev.conf -> log_statement", "pg-dev.conf -> log_min_duration_statement"],
            reasoning_required="Assess log storage impact from logging configuration across all PostgreSQL instances",
        ),
        EvalQuestion(
            id="OI-102",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What are the security implications of the current secret management approach across the fleet?",
            reference_answer="Secrets are stored in plaintext across multiple locations: compose files (DATABASE_URL, API_KEY, JWT_SECRET), systemd units (DATABASE_URL, SECRET_KEY, BACKUP_ENCRYPTION_KEY), tfvars (database.password, api_secret, ssh_private_key), ansible-vars (db_password, deploy_key, vault_password), app-config JSONs (jwt_secret, api_key, encryption_key). The ansible-vars file even warns about vault encryption. No config uses external secret stores (Vault, AWS Secrets Manager).",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.environment", "systemd-core-api.conf -> Environment", "tfvars-prod.json -> secrets", "ansible-vars-prod.yaml"],
            reasoning_required="Audit secret management practices across the entire configuration fleet",
        ),
        EvalQuestion(
            id="OI-103",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="If the auth-jwt service experiences a spike in authentication requests, what are all the downstream capacity limits?",
            reference_answer="Auth-JWT rate limit: 1000 req/min with burst=50. DB pool: 10 connections to PostgreSQL primary (max_connections=200 shared with other services). Redis pool: 5 connections on DB 0. JWT expiry: 3600s means token cache is effective for 1 hour. The PostgreSQL primary also serves core-api, making the 200 connection limit a shared bottleneck.",
            evidence_locations=["app-config-auth-jwt.json -> rate_limiting", "app-config-auth-jwt.json -> database.pool_size", "pg-prod-primary.conf -> max_connections"],
            reasoning_required="Trace capacity limits across service, database, and cache layers",
        ),
        EvalQuestion(
            id="OI-104",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What would be the operational impact of promoting the staging environment to production?",
            reference_answer="Multiple issues: 1) sshd allows password auth (security gap). 2) Traefik has insecure API and insecureSkipVerify=true. 3) PostgreSQL has SSL=off, lower shared_buffers (512MB), no archiving. 4) MariaDB staging has slow_query_log=OFF. 5) Compose uses -rc1 image tags. 6) Single network topology vs prod dual-network. 7) No Prometheus alerting or remote_write. 8) Lower Terraform instance types. All of these would need remediation before production use.",
            evidence_locations=["sshd-staging.conf", "traefik-staging.yaml", "pg-staging.conf", "compose-staging.yaml", "prometheus-staging.yaml", "tfvars-staging.json"],
            reasoning_required="Comprehensive gap analysis between staging and production configurations",
        ),
        EvalQuestion(
            id="OI-105",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="How would you estimate the total memory footprint of the prod compose application stack?",
            reference_answer="From compose-app-prod-01.yaml: core-api=2g, auth-service=1g, redis=512m. From compose-monitoring-prod.yaml: prometheus=2g, grafana is also defined. The total from a single app compose is approximately 3.5GB. With monitoring, at least 5.5GB. This does not include PostgreSQL and MariaDB which run as native services (not in compose). The t3.xlarge instance type has 16GB RAM, which should accommodate this.",
            evidence_locations=["compose-app-prod-01.yaml -> services.*.mem_limit", "compose-monitoring-prod.yaml -> services.prometheus.mem_limit", "tfvars-prod.json -> compute.app_instance_type"],
            reasoning_required="Calculate total memory from compose resource limits and validate against infrastructure capacity",
        ),
        EvalQuestion(
            id="OI-106",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What are the risks of running the MariaDB Galera node alongside standalone MariaDB instances?",
            reference_answer="The Galera node (server_id=100) has different durability settings (flush=2, sync_binlog=0) and different buffer settings (innodb_change_buffering=none, innodb_max_dirty_pages_pct=75 vs 90) compared to standalone instances. If applications switch between Galera and standalone, they may experience different durability guarantees and performance characteristics. The Galera node also has 300 max_connections vs 151/200 on others, so connection pool sizing must differ.",
            evidence_locations=["maria-galera-node.cnf", "maria-legacy-main.cnf", "maria-legacy-billing.cnf"],
            reasoning_required="Assess heterogeneous MariaDB fleet risks from configuration differences",
        ),
        EvalQuestion(
            id="OI-107",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What is the monitoring blind spot in the current Prometheus configuration?",
            reference_answer="1) Dev Prometheus has no alerting and only 4 scrape targets. 2) Staging lacks alerting and remote_write. 3) Neither staging nor dev Prometheus configs monitor backup services, systemd timers, sysctl parameters, or Docker daemon metrics. 4) The MariaDB exporter (port 9104) only monitors db-legacy-01 in prod but not billing, staging, or dev MariaDB instances. 5) No separate monitoring for the Galera cluster health.",
            evidence_locations=["prometheus-prod.yaml -> scrape_configs", "prometheus-staging.yaml -> scrape_configs", "prometheus-dev.yaml -> scrape_configs"],
            reasoning_required="Identify comprehensive monitoring gaps across all Prometheus configurations",
        ),
        EvalQuestion(
            id="OI-108",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="If you needed to run the emergency rollback playbook at 3 AM during an incident, what problems would you encounter?",
            reference_answer="1) become=false prevents restarting systemd services that require root. 2) ignore_errors=true at play level means individual task failures would be silently ignored. 3) gather_facts=false means the playbook does not know the current state of targets. 4) The playbook has no notification tasks to alert the team. 5) The deploy_key is hardcoded as plaintext in vars. 6) There is no rollback version logic, just rollback_version='previous'. The playbook is fundamentally broken for its intended purpose.",
            evidence_locations=["ansible-emergency-rollback.yaml"],
            reasoning_required="Comprehensive assessment of emergency rollback playbook reliability and correctness",
        ),
        EvalQuestion(
            id="OI-109",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What would be the performance impact of migrating the MariaDB legacy-main from latin1 to utf8mb4?",
            reference_answer="1) Storage increase: utf8mb4 uses up to 4 bytes per character vs 1 for latin1. Indexes on varchar columns would grow significantly. 2) With innodb_buffer_pool_size=256M (smallest in fleet), the buffer pool may need to be increased. 3) The InnoDB row size limit may be exceeded for tables with many varchar columns. 4) Collation change from latin1_swedish_ci to utf8mb4_0900_ai_ci affects sort order and index usage. 5) Application queries may need to be updated for new collation behaviour.",
            evidence_locations=["maria-legacy-main.cnf -> character_set_server", "maria-legacy-main.cnf -> innodb_buffer_pool_size", "maria-legacy-main.cnf -> collation_server"],
            reasoning_required="Assess migration impact from character set, storage, and buffer pool interactions",
        ),
        EvalQuestion(
            id="OI-110",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What is the blast radius of a compromised deploy user SSH key?",
            reference_answer="The deploy user is in AllowUsers for sshd-prod.conf and sshd-bastion.conf, ansible-inventory-prod.yaml sets ansible_user=deploy for all hosts, cloud-init creates deploy user with NOPASSWD sudo on app servers. A compromised deploy key gives: 1) SSH access to all prod and bastion servers. 2) Full sudo on app servers. 3) Through bastion forwarding, access to all internal hosts. 4) With sudo, access to all service secrets in env vars and config files.",
            evidence_locations=["sshd-prod.conf -> AllowUsers", "sshd-bastion.conf -> AllowUsers", "ansible-inventory-prod.yaml -> all.vars.ansible_user", "cloud-init-app-server.yaml -> users"],
            reasoning_required="Trace access scope of a compromised credential across the entire fleet",
        ),
        EvalQuestion(
            id="OI-111",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="How would you design a configuration change to improve the PostgreSQL replica's production readiness for failover?",
            reference_answer="Changes needed: 1) Enable SSL (ssl=on with cert/key paths matching primary). 2) Enable archive_mode=on with a remote archive_command. 3) Increase maintenance_work_mem from 64MB to match primary's 512MB. 4) Set log_statement=mod to match primary's logging. 5) Enable logging_collector=on. 6) Match log_timezone to primary (America/New_York). 7) Consider enabling log_connections and log_lock_waits to match primary monitoring. 8) Increase effective_cache_size from 4GB to better reflect actual memory.",
            evidence_locations=["pg-prod-primary.conf", "pg-prod-replica.conf"],
            reasoning_required="Design configuration improvements by comparing replica to primary settings",
        ),
        EvalQuestion(
            id="OI-112",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What are the network connectivity requirements for the complete prod application stack?",
            reference_answer="Core-api needs: PostgreSQL (db-prod-01:5432), Redis (:6379). Auth-service needs: PostgreSQL (db-prod-01:5432). Billing needs: MariaDB (db-legacy-01:3306), Redis (:6379). Search needs: Redis (:6379). Prometheus needs: all app ports (8000-8005, 5000), DB exporters (9187, 9104), Traefik (8080), node-exporter (9100), Alertmanager (mon-prod-01:9093), remote-write. Traefik needs: all service ports. Bastion needs: SSH to all internal hosts.",
            evidence_locations=["compose-app-prod-01.yaml", "app-config-auth-jwt.json", "app-config-billing.json", "prometheus-prod.yaml", "sshd-bastion.conf"],
            reasoning_required="Map complete network topology from all service configurations",
        ),
        EvalQuestion(
            id="OI-113",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="If you needed to perform zero-downtime database schema migration on the primary, what PostgreSQL settings would help or hinder?",
            reference_answer="Helpful: archive_mode=on preserves WAL for recovery. max_wal_senders=10 allows replication during migration. log_lock_waits=on helps detect blocking. Hindering: log_min_duration_statement=500ms will generate high log volume during migration. max_connections=200 limits concurrent migration tools. The migration instance (pg-migration.conf) with synchronous_commit=off and high work_mem would be better for running the actual DDL, then replicating changes.",
            evidence_locations=["pg-prod-primary.conf", "pg-migration.conf"],
            reasoning_required="Assess migration feasibility from PostgreSQL settings across instances",
        ),
        EvalQuestion(
            id="OI-114",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What is the complete deployment pipeline based on the compose file naming and image tagging strategy?",
            reference_answer="1) Developers work with compose-dev.yaml using :dev tagged images and local source mounts. 2) CI runs compose-ci.yaml with :${CI_COMMIT_SHA} tags for testing. 3) Staging uses compose-staging.yaml with -rc1 suffixed versions. 4) Production uses compose-app-prod-01/02/03.yaml with exact version tags (e.g. 2.1.0). The progression is: dev -> CI (commit SHA) -> staging (release candidate) -> prod (release). There is a load-test compose and DR compose for additional environments.",
            evidence_locations=["compose-dev.yaml", "compose-ci.yaml", "compose-staging.yaml", "compose-app-prod-01.yaml"],
            reasoning_required="Reconstruct deployment pipeline from compose file naming and image tagging conventions",
        ),
        EvalQuestion(
            id="OI-115",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What observability gaps exist in the dev environment compared to production?",
            reference_answer="1) Prometheus: 4 scrape targets vs 15 in prod, no alerting, no remote_write, 1m scrape vs 15s. 2) Docker: experimental mode may change metric availability. 3) PostgreSQL: log_statement=all generates noise but captures everything. 4) MariaDB dev: general_log=ON but slow_query_log threshold is 0 (logs everything, useful but disk-heavy). 5) No Grafana or alertmanager. 6) No Traefik metrics endpoint. 7) sshd LogLevel=INFO vs prod VERBOSE. Overall, the dev env has verbose logging but poor metric collection and no alerting.",
            evidence_locations=["prometheus-dev.yaml", "docker-daemon-dev.json", "pg-dev.conf", "maria-dev.cnf", "sshd-dev.conf"],
            reasoning_required="Comprehensive observability comparison between dev and prod environments",
        ),
        EvalQuestion(
            id="OI-116",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What configuration changes would be needed to add a new microservice to the fleet?",
            reference_answer="1) Create a Docker Compose service definition for each environment (dev, staging, prod, CI). 2) Add a systemd unit for native deployment. 3) Add a Prometheus scrape target in all three Prometheus configs. 4) Create an app-config JSON with database, Redis, rate limiting, and auth settings. 5) Add the service's hosts to the Ansible inventory. 6) Create or update Ansible deployment playbook. 7) Add Traefik routing labels. 8) Update cloud-init if new packages are needed. 9) Configure Grafana dashboards. 10) Update tfvars if new infrastructure is needed.",
            evidence_locations=["compose-app-prod-01.yaml", "systemd-core-api.conf", "prometheus-prod.yaml", "app-config-auth-jwt.json", "ansible-inventory-prod.yaml"],
            reasoning_required="Derive required configuration changes from existing fleet patterns",
        ),
        EvalQuestion(
            id="OI-117",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What is the maximum memory the PostgreSQL analytics instance could use under heavy load?",
            reference_answer="shared_buffers=16GB (fixed allocation). work_mem=256MB * max_connections(50) = up to 12.8GB in worst case. maintenance_work_mem=2GB for vacuum/index operations. temp_buffers=32MB * connections = 1.6GB. effective_cache_size=48GB is a planner hint, not allocation. With huge_pages=on and vm.nr_hugepages from sysctl, the total could exceed 32GB under extreme concurrent query load. The r6g.2xlarge (64GB RAM) should accommodate this.",
            evidence_locations=["pg-prod-analytics.conf -> shared_buffers", "pg-prod-analytics.conf -> work_mem", "pg-prod-analytics.conf -> max_connections", "tfvars-prod.json -> compute.db_instance_type"],
            reasoning_required="Calculate worst-case memory consumption from PostgreSQL configuration parameters",
        ),
        EvalQuestion(
            id="OI-118",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="How resilient is the fleet to a DNS failure affecting 8.8.8.8 and 8.8.4.4?",
            reference_answer="The Ansible inventory configures dns_servers=[8.8.8.8, 8.8.4.4] for all hosts. Docker daemon prod also uses dns=[8.8.8.8, 8.8.4.4]. A failure of both Google DNS servers would affect: all host-level DNS resolution, all Docker container DNS resolution in prod, and any service that needs external DNS. Internal services using .ridgeline.internal hostnames would also fail if managed by these resolvers. Mitigation: no local DNS cache or fallback resolvers are configured.",
            evidence_locations=["ansible-inventory-prod.yaml -> all.vars.dns_servers", "docker-daemon-prod.json -> dns"],
            reasoning_required="Trace DNS dependency across the fleet to assess single point of failure",
        ),
        EvalQuestion(
            id="OI-119",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What are the implications of the Ridgeline fleet's lack of configuration management convergence between cloud-init and Ansible?",
            reference_answer="cloud-init runs once at provisioning time (app server config), while Ansible playbooks are run ad-hoc. NTP servers differ (pool.ntp.org vs ntp.ubuntu.com). Cloud-init installs packages and creates users at boot, but Ansible base-server also installs packages and creates users. There is no mechanism to ensure cloud-init and Ansible produce the same end state. Drift can occur between servers provisioned at different times or with different playbook runs.",
            evidence_locations=["cloud-init-app-server.yaml", "ansible-base-server.yaml", "ansible-inventory-prod.yaml -> all.vars.ntp_server"],
            reasoning_required="Identify configuration management inconsistencies between provisioning tools",
        ),
        EvalQuestion(
            id="OI-120",
            question_class=EvalQuestionClass.OPERATIONAL_INFERENCE,
            difficulty=Difficulty.HARD,
            question="What is the end-to-end latency impact of the current Prometheus monitoring architecture?",
            reference_answer="Prod scrapes every 15s with evaluation_interval=15s, meaning alert detection latency is ~30s best case. The alertmanager is at mon-prod-01:9093. remote_write has queue_config with batch_send_deadline=5s. Total metric visibility latency: ~15-30s for scrape, ~5s for remote write. For staging (30s scrape), detection latency doubles to ~60s. For dev (1m scrape), detection latency is ~2 minutes. No alerting in staging/dev means issues are only detected when someone checks dashboards.",
            evidence_locations=["prometheus-prod.yaml -> global.scrape_interval", "prometheus-prod.yaml -> remote_write.queue_config", "prometheus-staging.yaml -> global.scrape_interval", "prometheus-dev.yaml -> global.scrape_interval"],
            reasoning_required="Calculate end-to-end monitoring latency from Prometheus scrape, evaluation, and remote write settings",
        ),
    ]


def design_compliance() -> list[EvalQuestion]:
    """Return 120 DESIGN_COMPLIANCE questions (prefix DC)."""
    return [
        # ── EASY (DC-001 .. DC-048) ──────────────────────────────────────────────
        EvalQuestion(
            id="DC-001",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the production sshd config follow the standard of disabling password authentication?",
            reference_answer="Yes, sshd-prod.conf has PasswordAuthentication=no, following the security standard.",
            evidence_locations=["sshd-prod.conf -> PasswordAuthentication"],
            reasoning_required="Check sshd password authentication standard compliance",
        ),
        EvalQuestion(
            id="DC-002",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the staging sshd config follow the same password authentication standard as production?",
            reference_answer="No, sshd-staging.conf has PasswordAuthentication=yes, deviating from the production standard of PasswordAuthentication=no.",
            evidence_locations=["sshd-staging.conf -> PasswordAuthentication", "sshd-prod.conf -> PasswordAuthentication"],
            reasoning_required="Compare staging sshd to production standard",
        ),
        EvalQuestion(
            id="DC-003",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all MariaDB instances use utf8mb4 character set?",
            reference_answer="No, maria-legacy-main.cnf uses latin1 instead of utf8mb4, deviating from the standard used by all other MariaDB instances.",
            evidence_locations=["maria-legacy-main.cnf -> character_set_server", "maria-legacy-billing.cnf -> character_set_server"],
            reasoning_required="Check character set consistency across MariaDB fleet",
        ),
        EvalQuestion(
            id="DC-004",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all production PostgreSQL instances have SSL enabled?",
            reference_answer="No, pg-prod-replica.conf has ssl=off, while pg-prod-primary.conf and pg-prod-analytics.conf have ssl=on.",
            evidence_locations=["pg-prod-replica.conf -> ssl", "pg-prod-primary.conf -> ssl"],
            reasoning_required="Check SSL consistency across production PostgreSQL instances",
        ),
        EvalQuestion(
            id="DC-005",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the prod Traefik config follow the standard of disabling insecure API access?",
            reference_answer="Yes, traefik-prod-static.yaml has api.insecure=false and api.debug=false.",
            evidence_locations=["traefik-prod-static.yaml -> api.insecure", "traefik-prod-static.yaml -> api.debug"],
            reasoning_required="Check Traefik API security standard compliance",
        ),
        EvalQuestion(
            id="DC-006",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all systemd service units use Restart=on-failure as the standard policy?",
            reference_answer="No, systemd-legacy-dashboard.conf uses Restart=always instead of the on-failure standard used by other services like systemd-core-api.conf.",
            evidence_locations=["systemd-legacy-dashboard.conf -> [Service].Restart", "systemd-core-api.conf -> [Service].Restart"],
            reasoning_required="Check systemd restart policy consistency",
        ),
        EvalQuestion(
            id="DC-007",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the production compose file use exact version tags for images?",
            reference_answer="Yes, compose-app-prod-01.yaml uses exact version tags like ridgeline/core-api:2.1.0 and ridgeline/auth-service:1.8.0.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.image", "compose-app-prod-01.yaml -> services.auth-service.image"],
            reasoning_required="Check Docker image tagging standard compliance",
        ),
        EvalQuestion(
            id="DC-008",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the prod sshd config disable root login?",
            reference_answer="Yes, sshd-prod.conf has PermitRootLogin=no.",
            evidence_locations=["sshd-prod.conf -> PermitRootLogin"],
            reasoning_required="Check root login restriction compliance",
        ),
        EvalQuestion(
            id="DC-009",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the dev sshd config follow the same root login policy as production?",
            reference_answer="No, sshd-dev.conf has PermitRootLogin=prohibit-password, which is less restrictive than production's PermitRootLogin=no.",
            evidence_locations=["sshd-dev.conf -> PermitRootLogin", "sshd-prod.conf -> PermitRootLogin"],
            reasoning_required="Compare dev sshd root login policy to production standard",
        ),
        EvalQuestion(
            id="DC-010",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all PostgreSQL instances have autovacuum enabled?",
            reference_answer="No, pg-migration.conf has autovacuum=off. All other PostgreSQL instances have autovacuum=on.",
            evidence_locations=["pg-migration.conf -> autovacuum", "pg-prod-primary.conf -> autovacuum"],
            reasoning_required="Check autovacuum standard across PostgreSQL fleet",
        ),
        EvalQuestion(
            id="DC-011",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the production Docker daemon disable experimental features?",
            reference_answer="Yes, docker-daemon-prod.json has experimental=false.",
            evidence_locations=["docker-daemon-prod.json -> experimental"],
            reasoning_required="Check Docker daemon experimental mode compliance",
        ),
        EvalQuestion(
            id="DC-012",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the prod Traefik config disable insecureSkipVerify?",
            reference_answer="Yes, traefik-prod-static.yaml has serversTransport.insecureSkipVerify=false.",
            evidence_locations=["traefik-prod-static.yaml -> serversTransport.insecureSkipVerify"],
            reasoning_required="Check Traefik TLS verification standard compliance",
        ),
        EvalQuestion(
            id="DC-013",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all production compose services use the 'always' restart policy?",
            reference_answer="No, the redis service in compose-app-prod-01.yaml uses restart=no, while core-api and auth-service use restart=always.",
            evidence_locations=["compose-app-prod-01.yaml -> services.redis.restart", "compose-app-prod-01.yaml -> services.core-api.restart"],
            reasoning_required="Check restart policy consistency within production compose",
        ),
        EvalQuestion(
            id="DC-014",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all MariaDB instances have innodb_flush_log_at_trx_commit=1 for ACID compliance?",
            reference_answer="No, maria-galera-node.cnf has innodb_flush_log_at_trx_commit=2, deviating from the standard value of 1 used by all other MariaDB instances.",
            evidence_locations=["maria-galera-node.cnf -> innodb_flush_log_at_trx_commit", "maria-legacy-main.cnf -> innodb_flush_log_at_trx_commit"],
            reasoning_required="Check MariaDB ACID compliance across fleet",
        ),
        EvalQuestion(
            id="DC-015",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the production sshd config disable X11 forwarding?",
            reference_answer="Yes, sshd-prod.conf has X11Forwarding=no.",
            evidence_locations=["sshd-prod.conf -> X11Forwarding"],
            reasoning_required="Check sshd X11 forwarding security standard",
        ),
        EvalQuestion(
            id="DC-016",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the production sshd config disable TCP forwarding?",
            reference_answer="Yes, sshd-prod.conf has AllowTcpForwarding=no.",
            evidence_locations=["sshd-prod.conf -> AllowTcpForwarding"],
            reasoning_required="Check sshd TCP forwarding security standard",
        ),
        EvalQuestion(
            id="DC-017",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all Ansible playbooks set become appropriately for their tasks?",
            reference_answer="No, ansible-emergency-rollback.yaml has become=false but includes a systemd restart task that typically requires root. This is likely a misconfiguration.",
            evidence_locations=["ansible-emergency-rollback.yaml -> become", "ansible-emergency-rollback.yaml -> tasks"],
            reasoning_required="Check Ansible privilege escalation consistency with task requirements",
        ),
        EvalQuestion(
            id="DC-018",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all PostgreSQL instances use scram-sha-256 for password encryption?",
            reference_answer="Yes, all PostgreSQL configs have password_encryption=scram-sha-256.",
            evidence_locations=["pg-prod-primary.conf -> password_encryption", "pg-dev.conf -> password_encryption"],
            reasoning_required="Check PostgreSQL authentication method consistency",
        ),
        EvalQuestion(
            id="DC-019",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the Docker daemon prod config use overlay2 storage driver?",
            reference_answer="Yes, docker-daemon-prod.json has storage-driver=overlay2.",
            evidence_locations=["docker-daemon-prod.json -> storage-driver"],
            reasoning_required="Check Docker storage driver standard compliance",
        ),
        EvalQuestion(
            id="DC-020",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all production sshd configs disable PermitEmptyPasswords?",
            reference_answer="Yes, sshd-prod.conf has PermitEmptyPasswords=no.",
            evidence_locations=["sshd-prod.conf -> PermitEmptyPasswords"],
            reasoning_required="Check sshd empty password security standard",
        ),
        EvalQuestion(
            id="DC-021",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the Traefik staging config follow the same insecureSkipVerify standard as production?",
            reference_answer="No, traefik-staging.yaml has serversTransport.insecureSkipVerify=true, deviating from the production standard of false.",
            evidence_locations=["traefik-staging.yaml -> serversTransport.insecureSkipVerify", "traefik-prod-static.yaml -> serversTransport.insecureSkipVerify"],
            reasoning_required="Compare Traefik TLS verification between staging and production",
        ),
        EvalQuestion(
            id="DC-022",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all Terraform environments tag resources with the 'team' and 'project' tags?",
            reference_answer="Yes, all three tfvars files (prod, staging, dev) include team=platform and project=ridgeline tags.",
            evidence_locations=["tfvars-prod.json -> tags", "tfvars-staging.json -> tags", "tfvars-dev.json -> tags"],
            reasoning_required="Check Terraform resource tagging standard compliance",
        ),
        EvalQuestion(
            id="DC-023",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the Docker daemon prod config have live-restore enabled?",
            reference_answer="Yes, docker-daemon-prod.json has live-restore=true.",
            evidence_locations=["docker-daemon-prod.json -> live-restore"],
            reasoning_required="Check Docker daemon live-restore standard compliance",
        ),
        EvalQuestion(
            id="DC-024",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the PostgreSQL primary have logging_collector enabled?",
            reference_answer="Yes, pg-prod-primary.conf has logging_collector=on.",
            evidence_locations=["pg-prod-primary.conf -> logging_collector"],
            reasoning_required="Check PostgreSQL logging standard compliance",
        ),
        EvalQuestion(
            id="DC-025",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all sshd configs use SHA256 fingerprint hashing?",
            reference_answer="Yes, all sshd configs (prod, bastion, staging, dev) have FingerprintHash=sha256.",
            evidence_locations=["sshd-prod.conf -> FingerprintHash", "sshd-dev.conf -> FingerprintHash"],
            reasoning_required="Check sshd fingerprint hash standard across fleet",
        ),
        EvalQuestion(
            id="DC-026",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the Traefik prod config enforce a minimum TLS version of 1.2?",
            reference_answer="Yes, traefik-prod-static.yaml has tls.options.default.minVersion=VersionTLS12.",
            evidence_locations=["traefik-prod-static.yaml -> tls.options.default.minVersion"],
            reasoning_required="Check Traefik TLS version standard compliance",
        ),
        EvalQuestion(
            id="DC-027",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all PostgreSQL instances use the replica WAL level?",
            reference_answer="Yes, all PostgreSQL configs have wal_level=replica.",
            evidence_locations=["pg-prod-primary.conf -> wal_level", "pg-dev.conf -> wal_level"],
            reasoning_required="Check PostgreSQL WAL level consistency",
        ),
        EvalQuestion(
            id="DC-028",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the production Docker daemon disable debug mode?",
            reference_answer="Yes, docker-daemon-prod.json has debug=false.",
            evidence_locations=["docker-daemon-prod.json -> debug"],
            reasoning_required="Check Docker daemon debug mode standard compliance",
        ),
        EvalQuestion(
            id="DC-029",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all MariaDB instances use InnoDB as the default storage engine?",
            reference_answer="Yes, all MariaDB configs have default_storage_engine=InnoDB.",
            evidence_locations=["maria-legacy-main.cnf -> default_storage_engine", "maria-galera-node.cnf -> default_storage_engine"],
            reasoning_required="Check MariaDB storage engine standard",
        ),
        EvalQuestion(
            id="DC-030",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the cloud-init config disable root login on app servers?",
            reference_answer="Yes, cloud-init-app-server.yaml has disable_root=true.",
            evidence_locations=["cloud-init-app-server.yaml -> disable_root"],
            reasoning_required="Check cloud-init root access standard compliance",
        ),
        EvalQuestion(
            id="DC-031",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all Prometheus configs define rule_files?",
            reference_answer="No, prometheus-dev.yaml does not define a rule_files section. Prod and staging both define rule_files pointing to /etc/prometheus/rules/*.yaml.",
            evidence_locations=["prometheus-prod.yaml -> rule_files", "prometheus-staging.yaml -> rule_files", "prometheus-dev.yaml"],
            reasoning_required="Check Prometheus rule files standard across environments",
        ),
        EvalQuestion(
            id="DC-032",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all sshd configs have StrictModes enabled?",
            reference_answer="Yes, all four sshd configs (prod, bastion, staging, dev) have StrictModes=yes.",
            evidence_locations=["sshd-prod.conf -> StrictModes", "sshd-dev.conf -> StrictModes"],
            reasoning_required="Check sshd strict modes standard across fleet",
        ),
        EvalQuestion(
            id="DC-033",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the production sshd config set a banner?",
            reference_answer="Yes, sshd-prod.conf has Banner=/etc/ssh/banner.txt.",
            evidence_locations=["sshd-prod.conf -> Banner"],
            reasoning_required="Check sshd login banner standard compliance",
        ),
        EvalQuestion(
            id="DC-034",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all app-config JSON files have rate limiting enabled?",
            reference_answer="Yes, all three app configs (auth-jwt, billing, search) have rate_limiting.enabled=true.",
            evidence_locations=["app-config-auth-jwt.json -> rate_limiting.enabled", "app-config-billing.json -> rate_limiting.enabled", "app-config-search.json -> rate_limiting.enabled"],
            reasoning_required="Check rate limiting standard across app configs",
        ),
        EvalQuestion(
            id="DC-035",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all app-config JSON files have CORS enabled?",
            reference_answer="Yes, all three app configs (auth-jwt, billing, search) have cors.enabled=true.",
            evidence_locations=["app-config-auth-jwt.json -> cors.enabled", "app-config-billing.json -> cors.enabled", "app-config-search.json -> cors.enabled"],
            reasoning_required="Check CORS standard across app configs",
        ),
        EvalQuestion(
            id="DC-036",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all MariaDB instances have innodb_file_per_table enabled?",
            reference_answer="Yes, all MariaDB configs have innodb_file_per_table=ON.",
            evidence_locations=["maria-legacy-main.cnf -> innodb_file_per_table", "maria-galera-node.cnf -> innodb_file_per_table"],
            reasoning_required="Check MariaDB InnoDB file-per-table standard",
        ),
        EvalQuestion(
            id="DC-037",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the PostgreSQL primary have log_checkpoints enabled?",
            reference_answer="Yes, pg-prod-primary.conf has log_checkpoints=on.",
            evidence_locations=["pg-prod-primary.conf -> log_checkpoints"],
            reasoning_required="Check PostgreSQL checkpoint logging standard",
        ),
        EvalQuestion(
            id="DC-038",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all PostgreSQL instances use fsync=on?",
            reference_answer="Yes, all PostgreSQL configs have fsync=on for data integrity.",
            evidence_locations=["pg-prod-primary.conf -> fsync", "pg-dev.conf -> fsync"],
            reasoning_required="Check PostgreSQL data integrity standard",
        ),
        EvalQuestion(
            id="DC-039",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all Docker Compose files use json-file logging driver?",
            reference_answer="Yes, all compose services that specify a logging driver use driver: json-file.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.logging.driver", "compose-dev.yaml -> services.core-api.logging.driver"],
            reasoning_required="Check Docker Compose logging driver standard",
        ),
        EvalQuestion(
            id="DC-040",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the Traefik prod config have prometheus metrics enabled?",
            reference_answer="Yes, traefik-prod-static.yaml has a metrics.prometheus section with addEntryPointsLabels=true and addServicesLabels=true.",
            evidence_locations=["traefik-prod-static.yaml -> metrics.prometheus"],
            reasoning_required="Check Traefik metrics standard compliance",
        ),
        EvalQuestion(
            id="DC-041",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all PostgreSQL instances disable PermitEmptyPasswords equivalent (via password_encryption)?",
            reference_answer="Yes, all PostgreSQL instances use password_encryption=scram-sha-256, which enforces password authentication standards.",
            evidence_locations=["pg-prod-primary.conf -> password_encryption"],
            reasoning_required="Check PostgreSQL password encryption standard",
        ),
        EvalQuestion(
            id="DC-042",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all sshd configs disable HostbasedAuthentication?",
            reference_answer="Yes, all four sshd configs have HostbasedAuthentication=no.",
            evidence_locations=["sshd-prod.conf -> HostbasedAuthentication", "sshd-dev.conf -> HostbasedAuthentication"],
            reasoning_required="Check sshd host-based authentication standard",
        ),
        EvalQuestion(
            id="DC-043",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the production sshd config use AUTH syslog facility?",
            reference_answer="Yes, sshd-prod.conf has SyslogFacility=AUTH.",
            evidence_locations=["sshd-prod.conf -> SyslogFacility"],
            reasoning_required="Check sshd syslog standard compliance",
        ),
        EvalQuestion(
            id="DC-044",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all MariaDB instances disable local_infile?",
            reference_answer="Yes, all MariaDB configs have local_infile=OFF.",
            evidence_locations=["maria-legacy-main.cnf -> local_infile", "maria-dev.cnf -> local_infile"],
            reasoning_required="Check MariaDB local file access security standard",
        ),
        EvalQuestion(
            id="DC-045",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all Docker Compose prod services set privileged=false?",
            reference_answer="Yes, all services in compose-app-prod-01.yaml have privileged=false.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.privileged"],
            reasoning_required="Check Docker Compose privilege escalation standard",
        ),
        EvalQuestion(
            id="DC-046",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all sshd configs ignore rhosts?",
            reference_answer="Yes, all four sshd configs have IgnoreRhosts=yes.",
            evidence_locations=["sshd-prod.conf -> IgnoreRhosts", "sshd-dev.conf -> IgnoreRhosts"],
            reasoning_required="Check sshd rhosts security standard",
        ),
        EvalQuestion(
            id="DC-047",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Does the PostgreSQL primary enforce SSL with minimum TLS 1.2?",
            reference_answer="Yes, pg-prod-primary.conf has ssl=on and ssl_min_protocol_version=TLSv1.2.",
            evidence_locations=["pg-prod-primary.conf -> ssl", "pg-prod-primary.conf -> ssl_min_protocol_version"],
            reasoning_required="Check PostgreSQL TLS standard compliance",
        ),
        EvalQuestion(
            id="DC-048",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.EASY,
            question="Do all MariaDB instances have performance_schema enabled?",
            reference_answer="Yes, all MariaDB configs have performance_schema=ON.",
            evidence_locations=["maria-legacy-main.cnf -> performance_schema", "maria-dev.cnf -> performance_schema"],
            reasoning_required="Check MariaDB performance monitoring standard",
        ),
        # ── MEDIUM (DC-049 .. DC-096) ────────────────────────────────────────────
        EvalQuestion(
            id="DC-049",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Are all production sshd instances consistently hardened with the same MaxAuthTries limit?",
            reference_answer="Yes, both sshd-prod.conf and sshd-bastion.conf use MaxAuthTries=3. However, staging and dev use MaxAuthTries=6, which is less strict.",
            evidence_locations=["sshd-prod.conf -> MaxAuthTries", "sshd-bastion.conf -> MaxAuthTries", "sshd-staging.conf -> MaxAuthTries"],
            reasoning_required="Check MaxAuthTries consistency across production sshd configs",
        ),
        EvalQuestion(
            id="DC-050",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the MariaDB Galera node compliant with the fleet-wide sync_binlog=1 standard?",
            reference_answer="No, maria-galera-node.cnf has sync_binlog=0, deviating from the standard of sync_binlog=1 used by all other MariaDB instances. This is a Galera-specific optimization but weakens binary log durability.",
            evidence_locations=["maria-galera-node.cnf -> sync_binlog", "maria-legacy-main.cnf -> sync_binlog"],
            reasoning_required="Check binary log sync standard compliance for Galera node",
        ),
        EvalQuestion(
            id="DC-051",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all non-dev PostgreSQL instances have consistent checkpoint_completion_target?",
            reference_answer="Yes, all PostgreSQL instances have checkpoint_completion_target=0.9.",
            evidence_locations=["pg-prod-primary.conf -> checkpoint_completion_target", "pg-staging.conf -> checkpoint_completion_target"],
            reasoning_required="Check PostgreSQL checkpoint consistency across fleet",
        ),
        EvalQuestion(
            id="DC-052",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Are all Traefik instances configured with a consistent TLS minimum version?",
            reference_answer="Yes, all Traefik configs (prod, staging, dev) have tls.options.default.minVersion=VersionTLS12.",
            evidence_locations=["traefik-prod-static.yaml -> tls.options", "traefik-staging.yaml -> tls.options", "traefik-dev.yaml -> tls.options"],
            reasoning_required="Check TLS version standard across all Traefik configs",
        ),
        EvalQuestion(
            id="DC-053",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Are all Docker Compose healthchecks using consistent intervals across environments?",
            reference_answer="Yes, all compose files use interval=30s and timeout=30s consistently across environments. The start_period varies: prod uses 10s while staging, dev, and CI use 0s.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.healthcheck", "compose-staging.yaml -> services.core-api.healthcheck"],
            reasoning_required="Check healthcheck parameter consistency across compose files",
        ),
        EvalQuestion(
            id="DC-054",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all PostgreSQL production instances have consistent full_page_writes settings?",
            reference_answer="Yes, all PostgreSQL instances have full_page_writes=on for data integrity.",
            evidence_locations=["pg-prod-primary.conf -> full_page_writes", "pg-prod-replica.conf -> full_page_writes"],
            reasoning_required="Check PostgreSQL data integrity setting consistency",
        ),
        EvalQuestion(
            id="DC-055",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the fleet follow a consistent naming convention for Ansible inventory hostnames?",
            reference_answer="Yes, the inventory follows a pattern: role-environment-number (e.g. app-prod-01, db-prod-02, edge-prod-01, mon-prod-01, worker-prod-01). The exception is db-legacy-01 which uses 'legacy' instead of 'prod'.",
            evidence_locations=["ansible-inventory-prod.yaml -> all.children"],
            reasoning_required="Check naming convention consistency in Ansible inventory",
        ),
        EvalQuestion(
            id="DC-056",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Are all Terraform environments using consistent tagging?",
            reference_answer="Yes, all tfvars (prod, staging, dev) have consistent team=platform, project=ridgeline, and managed_by=opentofu tags. The environment tag correctly matches the respective environment.",
            evidence_locations=["tfvars-prod.json -> tags", "tfvars-staging.json -> tags", "tfvars-dev.json -> tags"],
            reasoning_required="Check Terraform tagging consistency across environments",
        ),
        EvalQuestion(
            id="DC-057",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all MariaDB instances have consistent innodb_strict_mode settings?",
            reference_answer="Yes, all MariaDB configs have innodb_strict_mode=ON.",
            evidence_locations=["maria-legacy-main.cnf -> innodb_strict_mode", "maria-galera-node.cnf -> innodb_strict_mode"],
            reasoning_required="Check MariaDB strict mode consistency",
        ),
        EvalQuestion(
            id="DC-058",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the fleet maintain consistent Docker Compose deploy strategies across environments?",
            reference_answer="Yes, all compose files use deploy.update_config with parallelism=1 and order=stop-first. They also all have rollback_config with the same settings. However, replicas differ (prod core-api: 2, all others: 1).",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.deploy", "compose-staging.yaml -> services.core-api.deploy"],
            reasoning_required="Check deploy strategy consistency across compose files",
        ),
        EvalQuestion(
            id="DC-059",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Are all PostgreSQL instances using consistent geqo settings?",
            reference_answer="Yes, all PostgreSQL configs have consistent GEQO settings: geqo=on, geqo_threshold=12, geqo_effort=5.",
            evidence_locations=["pg-prod-primary.conf -> geqo", "pg-dev.conf -> geqo"],
            reasoning_required="Check PostgreSQL genetic query optimiser consistency",
        ),
        EvalQuestion(
            id="DC-060",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all Traefik configs have consistent entrypoint addresses for common ports?",
            reference_answer="Mostly. All Traefik configs define web (:80) and traefik (:8080) entrypoints. However, traefik-dev.yaml is missing the websecure (:443) entrypoint that exists in prod and staging.",
            evidence_locations=["traefik-prod-static.yaml -> entryPoints", "traefik-staging.yaml -> entryPoints", "traefik-dev.yaml -> entryPoints"],
            reasoning_required="Check Traefik entrypoint consistency across environments",
        ),
        EvalQuestion(
            id="DC-061",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the fleet follow a standard for PostgreSQL log rotation?",
            reference_answer="Yes, all PostgreSQL instances with logging_collector=on use log_rotation_age=24h and log_rotation_size=10MB consistently.",
            evidence_locations=["pg-prod-primary.conf -> log_rotation_age", "pg-prod-primary.conf -> log_rotation_size"],
            reasoning_required="Check PostgreSQL log rotation standard compliance",
        ),
        EvalQuestion(
            id="DC-062",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Are all systemd service units configured with consistent file descriptor limits?",
            reference_answer="Yes, all systemd service units set LimitNOFILE=65535 and LimitNPROC=4096.",
            evidence_locations=["systemd-core-api.conf -> LimitNOFILE", "systemd-legacy-dashboard.conf -> LimitNOFILE"],
            reasoning_required="Check systemd resource limit consistency",
        ),
        EvalQuestion(
            id="DC-063",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all app-config JSON files use consistent logging format and output?",
            reference_answer="Yes, all app configs use format=json and output=stdout. The level varies: auth-jwt uses info, billing uses info, search uses warning.",
            evidence_locations=["app-config-auth-jwt.json -> logging", "app-config-billing.json -> logging", "app-config-search.json -> logging"],
            reasoning_required="Check logging configuration standard across app configs",
        ),
        EvalQuestion(
            id="DC-064",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all PostgreSQL instances use a consistent ssl_min_protocol_version?",
            reference_answer="Yes, all PostgreSQL configs that define ssl_min_protocol_version use TLSv1.2.",
            evidence_locations=["pg-prod-primary.conf -> ssl_min_protocol_version", "pg-prod-replica.conf -> ssl_min_protocol_version"],
            reasoning_required="Check PostgreSQL TLS version standard consistency",
        ),
        EvalQuestion(
            id="DC-065",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the PostgreSQL replica's logging configuration consistent with the primary?",
            reference_answer="No, significant differences: Primary has logging_collector=on, log_connections=on, log_disconnections=on, log_statement=mod, log_lock_waits=on. Replica has logging_collector=off, log_connections=off, log_disconnections=off, log_statement=none, log_lock_waits=off. The replica has much less logging.",
            evidence_locations=["pg-prod-primary.conf -> logging_collector", "pg-prod-replica.conf -> logging_collector"],
            reasoning_required="Compare logging standards between PostgreSQL primary and replica",
        ),
        EvalQuestion(
            id="DC-066",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all Prometheus configs follow a consistent naming convention for external_labels?",
            reference_answer="Yes, all Prometheus configs define env and cluster external labels. Prod: env=prod, cluster=ridgeline-prod. Staging: env=staging, cluster=ridgeline-staging. Dev: env=dev, cluster=ridgeline-dev.",
            evidence_locations=["prometheus-prod.yaml -> global.external_labels", "prometheus-staging.yaml -> global.external_labels", "prometheus-dev.yaml -> global.external_labels"],
            reasoning_required="Check Prometheus label naming standard across environments",
        ),
        EvalQuestion(
            id="DC-067",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Are all MariaDB instances configured with consistent autocommit settings?",
            reference_answer="Yes, all MariaDB configs have autocommit=ON.",
            evidence_locations=["maria-legacy-main.cnf -> autocommit", "maria-galera-node.cnf -> autocommit"],
            reasoning_required="Check MariaDB transaction standard consistency",
        ),
        EvalQuestion(
            id="DC-068",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the fleet follow a consistent practice for Docker Compose network isolation?",
            reference_answer="Mostly. Each environment uses a dedicated named network (app-network/monitoring in prod, staging-network in staging, dev-network in dev, ci-network in CI). However, prod uses two networks for separation between app and monitoring, while other environments use only one.",
            evidence_locations=["compose-app-prod-01.yaml -> networks", "compose-staging.yaml -> networks", "compose-dev.yaml -> networks"],
            reasoning_required="Check Docker Compose network isolation standard across environments",
        ),
        EvalQuestion(
            id="DC-069",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Are all sshd configs compliant with disabling Kerberos authentication?",
            reference_answer="Yes, all four sshd configs have KerberosAuthentication=no.",
            evidence_locations=["sshd-prod.conf -> KerberosAuthentication", "sshd-dev.conf -> KerberosAuthentication"],
            reasoning_required="Check sshd Kerberos authentication standard",
        ),
        EvalQuestion(
            id="DC-070",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the fleet follow a consistent standard for Redis image versions?",
            reference_answer="Yes, all compose files that include Redis use redis:7-alpine consistently.",
            evidence_locations=["compose-app-prod-01.yaml -> services.redis.image", "compose-dev.yaml -> services.redis.image", "compose-ci.yaml -> services.redis.image"],
            reasoning_required="Check Redis image version standard across compose files",
        ),
        EvalQuestion(
            id="DC-071",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the fleet use consistent MariaDB versions across compose files?",
            reference_answer="Yes, compose-staging.yaml, compose-dev.yaml, and compose-ci.yaml all use mariadb:10.11.",
            evidence_locations=["compose-staging.yaml -> services.mariadb.image", "compose-dev.yaml -> services.mariadb.image", "compose-ci.yaml -> services.mariadb.image"],
            reasoning_required="Check MariaDB image version consistency in compose files",
        ),
        EvalQuestion(
            id="DC-072",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all sshd configs consistently disable KbdInteractiveAuthentication in production?",
            reference_answer="Yes, sshd-prod.conf and sshd-bastion.conf have KbdInteractiveAuthentication=no. However, staging and dev have it set to yes.",
            evidence_locations=["sshd-prod.conf -> KbdInteractiveAuthentication", "sshd-staging.conf -> KbdInteractiveAuthentication"],
            reasoning_required="Check keyboard-interactive authentication standard across environments",
        ),
        EvalQuestion(
            id="DC-073",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Are all PostgreSQL configs using consistent constraint_exclusion settings?",
            reference_answer="Yes, all PostgreSQL configs have constraint_exclusion=partition.",
            evidence_locations=["pg-prod-primary.conf -> constraint_exclusion", "pg-dev.conf -> constraint_exclusion"],
            reasoning_required="Check PostgreSQL planner standard consistency",
        ),
        EvalQuestion(
            id="DC-074",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the fleet follow a standard for Ansible become_user when privilege escalation is used?",
            reference_answer="Yes, ansible-base-server.yaml uses become_user=root as the standard. The other playbooks that have become=false do not need become_user.",
            evidence_locations=["ansible-base-server.yaml -> become_user"],
            reasoning_required="Check Ansible privilege escalation user standard",
        ),
        EvalQuestion(
            id="DC-075",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all Traefik configs use Docker as a provider?",
            reference_answer="Yes, all Traefik configs (prod, staging, dev) define providers.docker with endpoint unix:///var/run/docker.sock and watch=true.",
            evidence_locations=["traefik-prod-static.yaml -> providers.docker", "traefik-staging.yaml -> providers.docker", "traefik-dev.yaml -> providers.docker"],
            reasoning_required="Check Traefik provider standard across configs",
        ),
        EvalQuestion(
            id="DC-076",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Are all MariaDB instances configured with consistent replica settings (log_replica_updates, relay_log_purge)?",
            reference_answer="Yes, all MariaDB configs have log_replica_updates=ON and relay_log_purge=ON.",
            evidence_locations=["maria-legacy-main.cnf -> log_replica_updates", "maria-galera-node.cnf -> log_replica_updates"],
            reasoning_required="Check MariaDB replication standard consistency",
        ),
        EvalQuestion(
            id="DC-077",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all PostgreSQL instances have consistent recovery_target_timeline settings?",
            reference_answer="Yes, all PostgreSQL configs have recovery_target_timeline=latest.",
            evidence_locations=["pg-prod-primary.conf -> recovery_target_timeline", "pg-migration.conf -> recovery_target_timeline"],
            reasoning_required="Check PostgreSQL recovery target standard",
        ),
        EvalQuestion(
            id="DC-078",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the Docker daemon configuration consistent regarding userland-proxy across environments?",
            reference_answer="No, docker-daemon-prod.json has userland-proxy=false for better performance, while docker-daemon-dev.json has userland-proxy=true.",
            evidence_locations=["docker-daemon-prod.json -> userland-proxy", "docker-daemon-dev.json -> userland-proxy"],
            reasoning_required="Check Docker daemon network proxy consistency",
        ),
        EvalQuestion(
            id="DC-079",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all app-config services use consistent redis pool_size and timeout?",
            reference_answer="Yes, all three app configs (auth-jwt, billing, search) use pool_size=5 and timeout=5 for Redis.",
            evidence_locations=["app-config-auth-jwt.json -> redis.pool_size", "app-config-billing.json -> redis.pool_size", "app-config-search.json -> redis.pool_size"],
            reasoning_required="Check Redis pool configuration standard across app configs",
        ),
        EvalQuestion(
            id="DC-080",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Are all Prometheus scrape configs using consistent metrics_path and scheme?",
            reference_answer="Yes, all scrape jobs across all environments use metrics_path=/metrics and scheme=http.",
            evidence_locations=["prometheus-prod.yaml -> scrape_configs[0].metrics_path", "prometheus-staging.yaml -> scrape_configs[0].scheme"],
            reasoning_required="Check Prometheus scrape standard across all configs",
        ),
        EvalQuestion(
            id="DC-081",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the compose-dev.yaml environment parity with prod adequate for testing?",
            reference_answer="Partially. Dev uses the same database engines (postgres:16-alpine, mariadb:10.11, redis:7-alpine) as staging/CI. However, dev lacks mem_limit/cpus on core-api and web-app, uses dev image tags, and has a single network. The database services use the same images, providing schema compatibility but not performance parity.",
            evidence_locations=["compose-dev.yaml -> services", "compose-app-prod-01.yaml -> services"],
            reasoning_required="Assess dev-prod parity from Docker Compose configurations",
        ),
        EvalQuestion(
            id="DC-082",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Are all systemd service units running as non-root users where appropriate?",
            reference_answer="Application services run as ridgeline user (core-api, legacy-dashboard), which is good. However, systemd-backup-svc.conf runs as root (User=root, Group=root), which has broader system access than may be necessary for a backup script.",
            evidence_locations=["systemd-core-api.conf -> [Service].User", "systemd-legacy-dashboard.conf -> [Service].User", "systemd-backup-svc.conf -> [Service].User"],
            reasoning_required="Check systemd user privilege standard across service units",
        ),
        EvalQuestion(
            id="DC-083",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all PostgreSQL production instances have consistent max_replication_slots?",
            reference_answer="Primary, replica, and analytics all have max_replication_slots=10. Migration has max_replication_slots=0, which is appropriate for its purpose.",
            evidence_locations=["pg-prod-primary.conf -> max_replication_slots", "pg-prod-replica.conf -> max_replication_slots", "pg-migration.conf -> max_replication_slots"],
            reasoning_required="Check replication slot standard across PostgreSQL instances",
        ),
        EvalQuestion(
            id="DC-084",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the Prometheus scrape timeout consistently set across all environments?",
            reference_answer="Yes, all three Prometheus configs (prod, staging, dev) use scrape_timeout=10s at both the global and per-job level.",
            evidence_locations=["prometheus-prod.yaml -> global.scrape_timeout", "prometheus-staging.yaml -> global.scrape_timeout", "prometheus-dev.yaml -> global.scrape_timeout"],
            reasoning_required="Check Prometheus scrape timeout standard across environments",
        ),
        EvalQuestion(
            id="DC-085",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all PostgreSQL instances have consistent authentication_timeout settings?",
            reference_answer="Yes, all PostgreSQL configs have authentication_timeout=1min.",
            evidence_locations=["pg-prod-primary.conf -> authentication_timeout", "pg-dev.conf -> authentication_timeout"],
            reasoning_required="Check PostgreSQL auth timeout standard consistency",
        ),
        EvalQuestion(
            id="DC-086",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Are all Docker Compose services using consistent database images (postgres:16-alpine)?",
            reference_answer="Yes, all compose files that include PostgreSQL use postgres:16-alpine consistently.",
            evidence_locations=["compose-staging.yaml -> services.postgres.image", "compose-dev.yaml -> services.postgres.image", "compose-ci.yaml -> services.postgres.image"],
            reasoning_required="Check PostgreSQL Docker image version standard across compose files",
        ),
        EvalQuestion(
            id="DC-087",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all sshd production configs use VERBOSE log level?",
            reference_answer="Yes, both sshd-prod.conf and sshd-bastion.conf use LogLevel=VERBOSE. Staging and dev use LogLevel=INFO.",
            evidence_locations=["sshd-prod.conf -> LogLevel", "sshd-bastion.conf -> LogLevel"],
            reasoning_required="Check sshd logging level standard for production servers",
        ),
        EvalQuestion(
            id="DC-088",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the Traefik provider throttle duration consistent across all environments?",
            reference_answer="Yes, all Traefik configs use providersThrottleDuration=2s.",
            evidence_locations=["traefik-prod-static.yaml -> providers.providersThrottleDuration", "traefik-staging.yaml -> providers.providersThrottleDuration"],
            reasoning_required="Check Traefik provider throttle standard",
        ),
        EvalQuestion(
            id="DC-089",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all PostgreSQL instances have consistent vacuum_cost settings?",
            reference_answer="Yes, all PostgreSQL configs use vacuum_cost_delay=0, vacuum_cost_page_hit=1, vacuum_cost_page_miss=2, vacuum_cost_page_dirty=20, vacuum_cost_limit=200.",
            evidence_locations=["pg-prod-primary.conf -> vacuum_cost_delay", "pg-staging.conf -> vacuum_cost_delay"],
            reasoning_required="Check PostgreSQL vacuum cost standard consistency",
        ),
        EvalQuestion(
            id="DC-090",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the fleet follow a standard for app-config database pool settings?",
            reference_answer="Yes, both app configs with database sections (auth-jwt and billing) use pool_size=10 and pool_timeout=30.",
            evidence_locations=["app-config-auth-jwt.json -> database.pool_size", "app-config-billing.json -> database.pool_size"],
            reasoning_required="Check database pool standard across app configs",
        ),
        EvalQuestion(
            id="DC-091",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Are all MariaDB instances using consistent binlog_row_image settings?",
            reference_answer="Yes, all MariaDB configs have binlog_row_image=FULL.",
            evidence_locations=["maria-legacy-main.cnf -> binlog_row_image", "maria-galera-node.cnf -> binlog_row_image"],
            reasoning_required="Check MariaDB binary log standard consistency",
        ),
        EvalQuestion(
            id="DC-092",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the Traefik prod config have a ping endpoint for health checks?",
            reference_answer="Yes, traefik-prod-static.yaml has a ping section with entryPoint=traefik and terminatingStatusCode=503.",
            evidence_locations=["traefik-prod-static.yaml -> ping"],
            reasoning_required="Check Traefik health check standard compliance",
        ),
        EvalQuestion(
            id="DC-093",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the systemd RestartSec consistent across all service units?",
            reference_answer="Yes, all systemd service units that define RestartSec use 5 seconds. The backup service does not define RestartSec because it is Type=oneshot.",
            evidence_locations=["systemd-core-api.conf -> [Service].RestartSec", "systemd-legacy-dashboard.conf -> [Service].RestartSec"],
            reasoning_required="Check systemd restart interval standard across services",
        ),
        EvalQuestion(
            id="DC-094",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Do all PostgreSQL instances use consistent bgwriter settings?",
            reference_answer="Yes, all PostgreSQL configs have bgwriter_delay=200ms, bgwriter_lru_maxpages=100, bgwriter_lru_multiplier=2.0.",
            evidence_locations=["pg-prod-primary.conf -> bgwriter_delay", "pg-dev.conf -> bgwriter_delay"],
            reasoning_required="Check PostgreSQL background writer standard consistency",
        ),
        EvalQuestion(
            id="DC-095",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the fleet follow a standard for sshd ClientAliveCountMax?",
            reference_answer="Mostly. Prod, staging, and dev all use ClientAliveCountMax=3. The bastion uses ClientAliveCountMax=2, which is slightly more aggressive in disconnecting idle sessions.",
            evidence_locations=["sshd-prod.conf -> ClientAliveCountMax", "sshd-bastion.conf -> ClientAliveCountMax"],
            reasoning_required="Check sshd keepalive standard across fleet",
        ),
        EvalQuestion(
            id="DC-096",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.MEDIUM,
            question="Are all MariaDB instances using consistent connection timeout settings?",
            reference_answer="Yes, all MariaDB configs use connect_timeout=10, wait_timeout=28800, interactive_timeout=28800, net_read_timeout=30, net_write_timeout=60.",
            evidence_locations=["maria-legacy-main.cnf -> connect_timeout", "maria-galera-node.cnf -> connect_timeout"],
            reasoning_required="Check MariaDB timeout standard consistency",
        ),
        # ── HARD (DC-097 .. DC-120) ──────────────────────────────────────────────
        EvalQuestion(
            id="DC-097",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with a consistent SSL/TLS encryption standard across all services?",
            reference_answer="No. PostgreSQL: primary and analytics have SSL=on but replica has SSL=off. App configs: auth-jwt and billing have database.ssl=false. Traefik: prod has insecureSkipVerify=false but staging/dev have true. TLS 1.2 minimum is consistent where configured. The fleet has an inconsistent TLS posture with several gaps.",
            evidence_locations=["pg-prod-primary.conf -> ssl", "pg-prod-replica.conf -> ssl", "app-config-auth-jwt.json -> database.ssl", "traefik-prod-static.yaml -> serversTransport.insecureSkipVerify"],
            reasoning_required="Audit TLS/SSL compliance across all service types in the fleet",
        ),
        EvalQuestion(
            id="DC-098",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet follow a consistent secret management standard?",
            reference_answer="No. Secrets are stored in plaintext in multiple locations: compose env vars, systemd Environment directives, tfvars JSON, Ansible vars YAML, app-config JSON. The ansible-vars-prod.yaml acknowledges this with a comment about vault encryption but is not actually encrypted. No external secret store (Vault, AWS SM) is used. Some secrets (api_key, jwt_secret) appear in multiple files. This violates best practices for secret management.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.environment", "ansible-vars-prod.yaml", "tfvars-prod.json -> secrets"],
            reasoning_required="Comprehensive secret management compliance audit across all config types",
        ),
        EvalQuestion(
            id="DC-099",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with a consistent monitoring and observability standard?",
            reference_answer="No. Prod has comprehensive monitoring (15 scrape targets, alerting, remote_write). Staging has 8 targets and no alerting/remote_write. Dev has only 4 targets, no alerting, no remote_write. Not all database instances are individually monitored (MariaDB only monitored at db-legacy-01). Docker daemon metrics are only configured in prod. The observability degrades significantly from prod to dev.",
            evidence_locations=["prometheus-prod.yaml", "prometheus-staging.yaml", "prometheus-dev.yaml", "docker-daemon-prod.json -> metrics-addr"],
            reasoning_required="Audit monitoring coverage compliance across all environments",
        ),
        EvalQuestion(
            id="DC-100",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet follow a consistent standard for database backup and disaster recovery?",
            reference_answer="Inconsistent. PostgreSQL primary has archive_mode=on but uses local cp for archiving. Replica/analytics/migration have no archiving. MariaDB instances have no explicit backup configuration. The systemd backup timer runs daily at 03:00. Terraform prod has backup_retention=30 days, staging has 7, dev has 0. There is no cross-region replication or off-site backup evidence. DR posture is weak.",
            evidence_locations=["pg-prod-primary.conf -> archive_mode", "systemd-backup-timer.conf", "tfvars-prod.json -> features.backup_retention_days", "tfvars-dev.json -> features.backup_retention_days"],
            reasoning_required="Comprehensive DR compliance assessment across all data stores",
        ),
        EvalQuestion(
            id="DC-101",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with principle of least privilege for SSH access?",
            reference_answer="Partially. Prod and bastion have AllowUsers restrictions and PasswordAuthentication=no. However, staging and dev have no AllowUsers restriction and allow password auth. The bastion allows TCP and agent forwarding (needed for its role). Dev allows X11Forwarding and root login via prohibit-password. The fleet progressively relaxes SSH security from prod to dev, but staging's permissiveness is concerning for a pre-production environment.",
            evidence_locations=["sshd-prod.conf", "sshd-bastion.conf", "sshd-staging.conf", "sshd-dev.conf"],
            reasoning_required="Audit SSH access control compliance across all sshd configs",
        ),
        EvalQuestion(
            id="DC-102",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with a consistent logging standard across all components?",
            reference_answer="Inconsistent. PostgreSQL: primary logs mod+connections+lock_waits, replica logs almost nothing, dev logs everything. MariaDB: slow_query_log varies (OFF on legacy-main and staging, ON on billing/dev/galera). sshd: prod/bastion use VERBOSE, staging/dev use INFO. Traefik: prod uses ERROR, staging/dev use DEBUG. Docker: all use json-file driver. The logging strategy lacks standardization across components and environments.",
            evidence_locations=["pg-prod-primary.conf -> log_statement", "pg-prod-replica.conf -> log_statement", "maria-legacy-main.cnf -> slow_query_log", "sshd-prod.conf -> LogLevel", "traefik-prod-static.yaml -> log.level"],
            reasoning_required="Comprehensive logging compliance audit across all service types and environments",
        ),
        EvalQuestion(
            id="DC-103",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with environment parity between staging and production for pre-release validation?",
            reference_answer="Significant gaps. Network: staging uses 1 network vs prod 2. Resources: staging has lower mem_limits and CPU. Database: staging PG has lower shared_buffers (512MB vs 2GB), no SSL, no archiving. sshd: staging allows passwords. Traefik: staging has insecure API and skip TLS verify. Prometheus: staging has 8 vs 15 targets, no alerting. Terraform: staging uses smaller instances (t3.medium vs t3.xlarge). The staging environment is not a reliable proxy for production behaviour.",
            evidence_locations=["compose-staging.yaml", "compose-app-prod-01.yaml", "pg-staging.conf", "pg-prod-primary.conf", "sshd-staging.conf", "traefik-staging.yaml"],
            reasoning_required="Comprehensive environment parity audit between staging and production",
        ),
        EvalQuestion(
            id="DC-104",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet follow a consistent standard for rate limiting and API protection?",
            reference_answer="Partially. All three app-config services have rate_limiting.enabled=true. However, limits vary widely: auth-jwt=1000/min, billing=500/min, search=2000/min. The Traefik reverse proxy does not have rate limiting configured at the edge level. There are no rate limits on database connections or SSH attempts beyond MaxAuthTries. The fleet lacks a unified rate limiting strategy at the infrastructure layer.",
            evidence_locations=["app-config-auth-jwt.json -> rate_limiting", "app-config-billing.json -> rate_limiting", "app-config-search.json -> rate_limiting", "traefik-prod-static.yaml"],
            reasoning_required="Audit rate limiting compliance across all service types",
        ),
        EvalQuestion(
            id="DC-105",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with a standard for configuration-as-code and infrastructure reproducibility?",
            reference_answer="Mostly. Terraform (OpenTofu) manages infrastructure with consistent tagging. Ansible manages configuration with inventory and playbooks. Docker Compose defines service stacks. Cloud-init handles initial provisioning. However, there are reproducibility gaps: cloud-init and Ansible overlap in user/package management, NTP sources differ, secrets are not managed via a central store, and the emergency-rollback playbook has a privilege escalation bug.",
            evidence_locations=["tfvars-prod.json -> tags.managed_by", "ansible-inventory-prod.yaml", "cloud-init-app-server.yaml", "ansible-base-server.yaml", "ansible-emergency-rollback.yaml"],
            reasoning_required="Assess IaC reproducibility compliance across all provisioning and configuration tools",
        ),
        EvalQuestion(
            id="DC-106",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with CIS benchmark recommendations for PostgreSQL database hardening?",
            reference_answer="Partial compliance. Good: scram-sha-256 password encryption, ssl_min_protocol_version=TLSv1.2, ssl_prefer_server_ciphers=on on primary. Gaps: replica has SSL off, log_statement is not consistent, some instances lack logging_collector, the migration instance has synchronous_commit=off and hot_standby=off, and pg-dev has listen_addresses='*' with log_statement=all which could expose sensitive data in logs.",
            evidence_locations=["pg-prod-primary.conf", "pg-prod-replica.conf", "pg-migration.conf", "pg-dev.conf"],
            reasoning_required="Assess CIS benchmark compliance for PostgreSQL across all instances",
        ),
        EvalQuestion(
            id="DC-107",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with a consistent resource allocation standard across Docker Compose environments?",
            reference_answer="No consistent standard. Prod core-api: mem_limit=2g, cpus=1.0. Staging core-api: mem_limit=1g, cpus=0.5. Dev core-api: no mem_limit, no cpus set. CI core-api-test: mem_limit=2g, cpus=1.0. Supporting services (postgres, mariadb, redis) have varying limits. The dev environment lacks any resource constraints, which could mask resource-related issues that would appear in production.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api", "compose-staging.yaml -> services.core-api", "compose-dev.yaml -> services.core-api", "compose-ci.yaml -> services.core-api-test"],
            reasoning_required="Audit resource allocation standard across all Docker Compose environments",
        ),
        EvalQuestion(
            id="DC-108",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with a consistent network security standard?",
            reference_answer="Inconsistent. Production: sshd restricts users, disables forwarding/X11, uses VERBOSE logging. Bastion: allows forwarding (by design). Staging: allows passwords, no user restrictions. Traefik: prod disables insecure API, staging/dev enable it. Docker daemon: prod disables userland-proxy, dev enables it. sysctl: db-tuned has extensive TCP hardening, app-server uses defaults. The fleet lacks a documented network security baseline.",
            evidence_locations=["sshd-prod.conf", "sshd-staging.conf", "traefik-prod-static.yaml", "traefik-staging.yaml", "sysctl-db-tuned.conf", "sysctl-app-server.conf"],
            reasoning_required="Comprehensive network security compliance audit across all service types",
        ),
        EvalQuestion(
            id="DC-109",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with database connection security standards (encrypted connections, credential rotation)?",
            reference_answer="Poor compliance. PostgreSQL: SSL on for primary/analytics but off for replica. App configs: both auth-jwt and billing have database.ssl=false despite PostgreSQL primary having SSL enabled. Credentials are static (no rotation mechanism visible). The same database password appears in multiple config files. No connection pooler (PgBouncer/ProxySQL) is deployed. MariaDB legacy connections are unencrypted.",
            evidence_locations=["pg-prod-primary.conf -> ssl", "pg-prod-replica.conf -> ssl", "app-config-auth-jwt.json -> database.ssl", "app-config-billing.json -> database.ssl"],
            reasoning_required="Audit database connection security across all application and database configs",
        ),
        EvalQuestion(
            id="DC-110",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with a standard for health check and readiness probe configuration?",
            reference_answer="Partial compliance. All Docker Compose services define healthcheck with consistent interval/timeout (30s). However: prod has appropriate start_period (10s) while staging/dev/CI use 0s. The backup and report systemd services are Type=oneshot with no health checking. The Ansible deploy-core-api playbook includes a health endpoint verification (URI check for /health), which is good. Traefik prod has a ping endpoint for health checking. Not all services have equivalent health monitoring.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.healthcheck", "compose-staging.yaml -> services.core-api.healthcheck", "ansible-deploy-core-api.yaml -> tasks", "traefik-prod-static.yaml -> ping"],
            reasoning_required="Audit health check standard compliance across all service types",
        ),
        EvalQuestion(
            id="DC-111",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with the principle of least privilege for service accounts and users?",
            reference_answer="Mixed compliance. Good: systemd services run as non-root user (ridgeline). sshd prod restricts to deploy/ops/admins. Cloud-init creates dedicated users. Bad: backup service runs as root. Cloud-init users have NOPASSWD sudo. Ansible vars contain plaintext credentials. The deploy user has broad SSH access to all hosts. MariaDB legacy has a root password comment in the config file. The fleet lacks fine-grained service account isolation.",
            evidence_locations=["systemd-core-api.conf -> [Service].User", "systemd-backup-svc.conf -> [Service].User", "cloud-init-app-server.yaml -> users", "sshd-prod.conf -> AllowUsers"],
            reasoning_required="Comprehensive least-privilege audit across service accounts, users, and credentials",
        ),
        EvalQuestion(
            id="DC-112",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with a standard for PostgreSQL replication and high availability?",
            reference_answer="Partial compliance. Primary has wal_level=replica, max_wal_senders=10, archive_mode=on. Replica has hot_standby=on, allowing read queries. However: replica lacks SSL, archive_mode, and matching logging config. Analytics has no replication configuration for itself. Migration is standalone with no replication. There is no evidence of automatic failover (no Patroni, repmgr, or similar). HA is manual and incomplete.",
            evidence_locations=["pg-prod-primary.conf -> wal_level", "pg-prod-primary.conf -> max_wal_senders", "pg-prod-replica.conf -> hot_standby", "pg-prod-replica.conf -> archive_mode"],
            reasoning_required="Audit PostgreSQL HA compliance across all instances",
        ),
        EvalQuestion(
            id="DC-113",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the web-app package.json follow standard practices for Node.js project configuration?",
            reference_answer="Mostly compliant. Good: private=true (prevents accidental npm publish), engines specifying node>=20.0.0, standard scripts (dev, build, start, lint, test), uses modern deps (Next.js 14, React 18). Minor issues: all dependencies use caret (^) ranges which could introduce breaking changes in CI. License is UNLICENSED (correct for private repo). TypeScript and testing dependencies are properly in devDependencies.",
            evidence_locations=["package-json-web-app.json"],
            reasoning_required="Audit Node.js project configuration compliance with standard practices",
        ),
        EvalQuestion(
            id="DC-114",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet follow a consistent standard for sysctl tuning based on server role?",
            reference_answer="Partially. DB-tuned has role-appropriate settings (low swappiness, high somaxconn, huge pages, high shmmax). App-server uses mostly default values. Dev matches app-server defaults. However: there are no sysctl configs for monitoring, edge, or worker servers. The edge and hardened sysctl configs exist but their relationship to the overall standard is not clearly documented. The fleet lacks a baseline sysctl profile that all servers should share.",
            evidence_locations=["sysctl-db-tuned.conf", "sysctl-app-server.conf", "sysctl-dev.conf"],
            reasoning_required="Audit sysctl tuning standard compliance across server roles",
        ),
        EvalQuestion(
            id="DC-115",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with a standard for service dependency declarations?",
            reference_answer="Inconsistent. Docker Compose: some services declare depends_on (prod core-api depends on redis, dev core-api depends on postgres) but not all (staging core-api has no depends_on). Systemd: core-api has After=postgresql.service, legacy-dashboard has After=mariadb.service, backup has After=postgresql.service. However, the relationship between compose depends_on and systemd After is not aligned (some services exist in both compose and systemd). Service discovery relies on Traefik labels but not all services have them.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.depends_on", "compose-dev.yaml -> services.core-api.depends_on", "systemd-core-api.conf -> [Unit].After", "systemd-legacy-dashboard.conf -> [Unit].After"],
            reasoning_required="Audit service dependency declaration consistency across compose and systemd",
        ),
        EvalQuestion(
            id="DC-116",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with a consistent Prometheus monitoring standard across all environments?",
            reference_answer="No. Prod: 15 jobs, scrape_interval=15s, alerting, remote_write, rule_files. Staging: 8 jobs, scrape_interval=30s, rule_files but no alerting/remote_write. Dev: 4 jobs, scrape_interval=1m, no rule_files, no alerting, no remote_write. The monitoring fidelity decreases dramatically from prod to dev. Services like billing-api, notification-svc, search-svc are only monitored in prod. There is no standardized minimum set of scrape targets per environment.",
            evidence_locations=["prometheus-prod.yaml", "prometheus-staging.yaml", "prometheus-dev.yaml"],
            reasoning_required="Comprehensive Prometheus standard compliance audit across all environments",
        ),
        EvalQuestion(
            id="DC-117",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with a consistent standard for Docker image version pinning?",
            reference_answer="Good for application images: prod uses exact versions (2.1.0), staging uses -rc1 variants, dev uses :dev tag, CI uses ${CI_COMMIT_SHA}. For infrastructure images: postgres:16-alpine, mariadb:10.11, redis:7-alpine are consistent across environments. prom/prometheus:v2.51.0 and grafana/grafana:10.4.0 are pinned. Exception: compose-staging.yaml uses legacy-dashboard:latest which is not version-pinned, breaking the standard.",
            evidence_locations=["compose-app-prod-01.yaml -> services.core-api.image", "compose-staging.yaml -> services.legacy-dashboard.image", "compose-monitoring-prod.yaml -> services.prometheus.image"],
            reasoning_required="Audit Docker image version pinning standard across all compose files",
        ),
        EvalQuestion(
            id="DC-118",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with a standard for Ansible playbook error handling and idempotency?",
            reference_answer="Inconsistent. Base-server: check_mode=false, ignore_errors=false, no_log=false per task (good). Deploy-core-api: same pattern (good). Emergency-rollback: ignore_errors=true at play level (bad - silences all failures), become=false for system tasks (bug). Setup playbooks are not visible but likely follow the base-server pattern. The emergency playbook is the weakest link, likely failing silently during incidents.",
            evidence_locations=["ansible-base-server.yaml -> tasks", "ansible-deploy-core-api.yaml -> tasks", "ansible-emergency-rollback.yaml"],
            reasoning_required="Audit Ansible playbook quality standards across all playbooks",
        ),
        EvalQuestion(
            id="DC-119",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with a consistent standard for database connection pooling and management?",
            reference_answer="Partially. App configs use pool_size=10 and pool_timeout=30 consistently. However: there is no external connection pooler (PgBouncer/ProxySQL). PostgreSQL primary allows 200 connections shared between all services without coordination. MariaDB legacy-main allows 151, billing allows 200. The Galera node allows 300. No connection pooling is configured at the infrastructure level. Multiple services could exhaust PostgreSQL connections simultaneously.",
            evidence_locations=["app-config-auth-jwt.json -> database.pool_size", "app-config-billing.json -> database.pool_size", "pg-prod-primary.conf -> max_connections"],
            reasoning_required="Audit database connection management compliance across app and database configs",
        ),
        EvalQuestion(
            id="DC-120",
            question_class=EvalQuestionClass.DESIGN_COMPLIANCE,
            difficulty=Difficulty.HARD,
            question="Does the fleet comply with a standard for CORS configuration across all web-facing services?",
            reference_answer="Partially. All three app configs have CORS enabled with consistent methods (GET, POST, PUT, DELETE) and allow_credentials=true. However, origins differ: auth-jwt allows app.ridgeline.io and admin.ridgeline.io, billing only allows app.ridgeline.io, search allows app.ridgeline.io and admin.ridgeline.io. There is no CORS configuration at the Traefik level for consistent edge enforcement. The lack of centralized CORS policy at the reverse proxy means each service must correctly implement its own.",
            evidence_locations=["app-config-auth-jwt.json -> cors", "app-config-billing.json -> cors", "app-config-search.json -> cors"],
            reasoning_required="Audit CORS configuration standard across all web-facing services",
        ),
    ]


def negative_absence() -> list[EvalQuestion]:
    """120 NEGATIVE_ABSENCE questions — things missing from the fleet."""
    return [
        # ── Easy (NA-001 → NA-048) ──────────────────────────────────────────────
        EvalQuestion(
            id="NA-001",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a WAF (Web Application Firewall) configuration present in the fleet?",
            reference_answer="No. There is no WAF configuration anywhere in the fleet. Traefik handles reverse proxy duties but has no WAF middleware configured. No ModSecurity, OWASP CRS, or cloud WAF configurations exist.",
            evidence_locations=["traefik-prod-static.yaml", "traefik-staging.yaml", "traefik-dev.yaml"],
            reasoning_required="Check for WAF configuration across all edge/proxy configs",
        ),
        EvalQuestion(
            id="NA-002",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a centralized log aggregation configuration (e.g., ELK, Loki, Fluentd) in the fleet?",
            reference_answer="No. Individual services log to stdout/json-file, and the logging YAML config writes to /var/log/ridgeline/app.log, but there is no centralized log aggregation stack (no ELK, Loki, Fluentd, or Fluentbit configs) in the fleet.",
            evidence_locations=["compose-monitoring-prod.yaml", "yaml-app-logging-config.yaml"],
            reasoning_required="Search for log aggregation service configs across the fleet",
        ),
        EvalQuestion(
            id="NA-003",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the pg-prod-replica configuration have SSL enabled?",
            reference_answer="No. The pg-prod-replica config has ssl = off, unlike the primary which has ssl = on. The replica does not have ssl_cert_file or ssl_key_file configured.",
            evidence_locations=["pg-prod-replica.conf -> ssl"],
            reasoning_required="Check SSL setting on the PostgreSQL replica",
        ),
        EvalQuestion(
            id="NA-004",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a PgBouncer or connection pooler configuration in the fleet?",
            reference_answer="No. There is no PgBouncer, pgpool-II, or any external connection pooler configuration. App configs define their own pool_size (10) but there is no infrastructure-level connection pooling.",
            evidence_locations=["app-config-auth-jwt.json -> database.pool_size", "pg-prod-primary.conf -> max_connections"],
            reasoning_required="Search for connection pooler configs in the fleet",
        ),
        EvalQuestion(
            id="NA-005",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the Docker daemon dev configuration include a metrics-addr setting?",
            reference_answer="No. The docker-daemon-dev.json does not include a metrics-addr field, unlike docker-daemon-prod.json which exposes metrics on 0.0.0.0:9323.",
            evidence_locations=["docker-daemon-dev.json", "docker-daemon-prod.json -> metrics-addr"],
            reasoning_required="Compare Docker daemon configs for metrics endpoint",
        ),
        EvalQuestion(
            id="NA-006",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there an AllowUsers directive in the sshd-staging configuration?",
            reference_answer="No. The sshd-staging.conf does not have an AllowUsers directive. Only sshd-prod (AllowUsers deploy ops admins) and sshd-bastion (AllowUsers deploy ops) have AllowUsers configured.",
            evidence_locations=["sshd-staging.conf", "sshd-prod.conf -> AllowUsers"],
            reasoning_required="Check for AllowUsers in the staging sshd config",
        ),
        EvalQuestion(
            id="NA-007",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a Banner directive in the sshd-dev configuration?",
            reference_answer="No. The sshd-dev.conf does not have a Banner directive. Only sshd-prod and sshd-bastion have Banner /etc/ssh/banner.txt configured.",
            evidence_locations=["sshd-dev.conf", "sshd-prod.conf -> Banner"],
            reasoning_required="Check for Banner setting in the dev sshd config",
        ),
        EvalQuestion(
            id="NA-008",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the Traefik dev configuration have a websecure (port 443) entrypoint?",
            reference_answer="No. The traefik-dev.yaml only defines web (:80) and traefik (:8080) entrypoints. There is no websecure (:443) entrypoint, unlike prod and staging which both have it.",
            evidence_locations=["traefik-dev.yaml -> entryPoints", "traefik-prod-static.yaml -> entryPoints.websecure"],
            reasoning_required="Check for HTTPS entrypoint in Traefik dev config",
        ),
        EvalQuestion(
            id="NA-009",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a Kubernetes manifest or Helm chart in the fleet?",
            reference_answer="No. The fleet uses Docker Compose for container orchestration. There are no Kubernetes manifests, Helm charts, or Kustomize configs anywhere in the fleet.",
            evidence_locations=["compose-app-prod-01.yaml", "compose-staging.yaml"],
            reasoning_required="Search for Kubernetes-related configs in the fleet",
        ),
        EvalQuestion(
            id="NA-010",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the search service app config have a database section?",
            reference_answer="No. The app-config-search.json does not have a database section, unlike app-config-auth-jwt.json and app-config-billing.json which both define database host, port, user, password, and pool settings. The search service only connects to Redis.",
            evidence_locations=["app-config-search.json", "app-config-auth-jwt.json -> database"],
            reasoning_required="Check for database configuration in the search service config",
        ),
        EvalQuestion(
            id="NA-011",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a CI/CD pipeline configuration (e.g., GitHub Actions, GitLab CI) in the fleet?",
            reference_answer="No. There are no CI/CD pipeline configs (.github/workflows/, .gitlab-ci.yml, Jenkinsfile, etc.) in the fleet. The compose-ci.yaml is a Docker Compose file for running tests, not a CI pipeline definition.",
            evidence_locations=["compose-ci.yaml"],
            reasoning_required="Search for CI/CD pipeline definitions in the fleet",
        ),
        EvalQuestion(
            id="NA-012",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the billing app config have an encryption_key in its auth section?",
            reference_answer="No. The app-config-billing.json auth section has jwt_secret, jwt_expiry_seconds, jwt_refresh_expiry_seconds, jwt_issuer, and api_key, but no encryption_key. The auth-jwt config does have encryption_key='aes256_Enc4ypt10nK3y_Pr0d!'.",
            evidence_locations=["app-config-billing.json -> auth", "app-config-auth-jwt.json -> auth.encryption_key"],
            reasoning_required="Check for encryption_key field in billing config auth section",
        ),
        EvalQuestion(
            id="NA-013",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a Grafana configuration file (grafana.ini or provisioning YAML) in the fleet?",
            reference_answer="No. Grafana is deployed via compose-monitoring-prod.yaml with environment variables (GF_SECURITY_ADMIN_PASSWORD, GF_SERVER_ROOT_URL) but there is no separate grafana.ini or provisioning config file in the fleet.",
            evidence_locations=["compose-monitoring-prod.yaml -> grafana"],
            reasoning_required="Search for Grafana configuration files in the fleet",
        ),
        EvalQuestion(
            id="NA-014",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the Docker daemon CI configuration include a dns setting?",
            reference_answer="No. The docker-daemon-ci.json does not include a dns setting. The docker-daemon-prod.json is the only one with dns configured ([\"8.8.8.8\", \"8.8.4.4\"]).",
            evidence_locations=["docker-daemon-ci.json", "docker-daemon-prod.json -> dns"],
            reasoning_required="Check for DNS configuration in CI Docker daemon config",
        ),
        EvalQuestion(
            id="NA-015",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there an Alertmanager configuration file (alertmanager.yml) in the fleet?",
            reference_answer="No. Alertmanager is deployed via compose-monitoring-prod.yaml with a volume mount from /opt/ridgeline/alertmanager, but there is no alertmanager.yml config file in the fleet. Only Prometheus references it as a target at mon-prod-01:9093.",
            evidence_locations=["compose-monitoring-prod.yaml -> alertmanager", "prometheus-prod.yaml -> alerting"],
            reasoning_required="Search for Alertmanager configuration files in the fleet",
        ),
        EvalQuestion(
            id="NA-016",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the systemd-core-api unit have a Restart=always policy?",
            reference_answer="No. The systemd-core-api.conf has Restart=on-failure, not Restart=always. The systemd-legacy-dashboard.conf uses Restart=always. This means the core API service will not restart on clean exit (exit code 0).",
            evidence_locations=["systemd-core-api.conf -> Restart", "systemd-legacy-dashboard.conf -> Restart"],
            reasoning_required="Check restart policy on core API systemd unit",
        ),
        EvalQuestion(
            id="NA-017",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there any Redis configuration file (redis.conf) in the fleet?",
            reference_answer="No. Redis is deployed as a service in Docker Compose files (redis:7-alpine) and referenced by URL in app configs (redis://:R3disP@ss!@redis:6379/N), but there is no redis.conf file in the fleet.",
            evidence_locations=["compose-app-prod-01.yaml", "app-config-auth-jwt.json -> redis"],
            reasoning_required="Search for Redis server configuration files in the fleet",
        ),
        EvalQuestion(
            id="NA-018",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the pg-prod-primary configuration have log_connections enabled?",
            reference_answer="Yes. The pg-prod-primary.conf has log_connections = on. Connection logging is enabled, so new connections are recorded in the PostgreSQL log.",
            evidence_locations=["pg-prod-primary.conf -> log_connections"],
            reasoning_required="Check connection logging setting on PostgreSQL primary",
        ),
        EvalQuestion(
            id="NA-019",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there any LDAP or Active Directory integration configuration in the fleet?",
            reference_answer="No. There is no LDAP, Active Directory, or centralized identity provider configuration. SSH uses key-based auth, PostgreSQL uses scram-sha-256 password auth, and app configs use JWT tokens. No PAM/LDAP/SSSD configs exist.",
            evidence_locations=["sshd-prod.conf", "pg-prod-primary.conf -> password_encryption"],
            reasoning_required="Search for LDAP/AD integration across the fleet",
        ),
        EvalQuestion(
            id="NA-020",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the Traefik dev configuration have a metrics section?",
            reference_answer="No. The traefik-dev.yaml does not have a metrics section. Both traefik-prod-static.yaml and traefik-staging.yaml have metrics.prometheus configured, but the dev config omits it entirely.",
            evidence_locations=["traefik-dev.yaml", "traefik-prod-static.yaml -> metrics"],
            reasoning_required="Check for metrics configuration in Traefik dev config",
        ),
        EvalQuestion(
            id="NA-021",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a VPN or WireGuard configuration in the fleet?",
            reference_answer="No. There are no VPN, WireGuard, IPsec, or OpenVPN configurations in the fleet. SSH access is the only remote access mechanism configured.",
            evidence_locations=["sshd-prod.conf", "sshd-bastion.conf"],
            reasoning_required="Search for VPN-related configs in the fleet",
        ),
        EvalQuestion(
            id="NA-022",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the tfvars-dev configuration have an ssl_cert_arn field?",
            reference_answer="No. The tfvars-dev.json networking section only has vpc_cidr, subnet_cidrs, and domain. It does not have ssl_cert_arn, unlike tfvars-prod.json which includes networking.ssl_cert_arn.",
            evidence_locations=["tfvars-dev.json -> networking", "tfvars-prod.json -> networking.ssl_cert_arn"],
            reasoning_required="Check for SSL certificate ARN in dev terraform vars",
        ),
        EvalQuestion(
            id="NA-023",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a backup schedule or retention policy defined for MariaDB in the fleet?",
            reference_answer="No. There is no MariaDB-specific backup configuration. The systemd-backup-svc runs /opt/ridgeline/scripts/backup-all-dbs.sh but only has After=postgresql.service dependency. MariaDB binlog_expire_logs_seconds is set (2592000 on legacy, 2592000 on galera) but that is log rotation, not backup.",
            evidence_locations=["systemd-backup-svc.conf -> After", "maria-legacy-main.cnf -> binlog_expire_logs_seconds"],
            reasoning_required="Search for MariaDB backup configuration in the fleet",
        ),
        EvalQuestion(
            id="NA-024",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the cloud-init-app-server configuration install any firewall packages?",
            reference_answer="No. The cloud-init-app-server.yaml packages list includes curl, wget, htop, vim, net-tools, python3-pip, docker-ce, and docker-compose-plugin. There are no firewall packages (ufw, firewalld, iptables-persistent) listed.",
            evidence_locations=["cloud-init-app-server.yaml -> packages"],
            reasoning_required="Check for firewall package installation in cloud-init config",
        ),
        EvalQuestion(
            id="NA-025",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a CORS or rate_limiting section in the service mesh YAML config?",
            reference_answer="No. The yaml-app-service-mesh.yaml does not have cors or rate_limiting sections. It only has app, logging, cache, database, and services sections. The yaml-app-logging-config.yaml is the one with cors and rate_limiting.",
            evidence_locations=["yaml-app-service-mesh.yaml", "yaml-app-logging-config.yaml -> cors"],
            reasoning_required="Check for CORS and rate limiting in service mesh config",
        ),
        EvalQuestion(
            id="NA-026",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the tfvars-dev configuration have an edge_instance_type field?",
            reference_answer="No. The tfvars-dev.json compute section only has app_instance_type, app_instance_count, db_instance_type, and db_instance_count. There is no edge_instance_type, unlike prod (t3.medium) and staging (t3.small) which both include it.",
            evidence_locations=["tfvars-dev.json -> compute", "tfvars-prod.json -> compute.edge_instance_type"],
            reasoning_required="Check for edge compute definition in dev terraform vars",
        ),
        EvalQuestion(
            id="NA-027",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a Nginx configuration in the fleet?",
            reference_answer="No. The fleet uses Traefik as the reverse proxy/load balancer. There are no Nginx configuration files (nginx.conf, sites-available, etc.) in the fleet.",
            evidence_locations=["traefik-prod-static.yaml", "traefik-staging.yaml", "traefik-dev.yaml"],
            reasoning_required="Search for Nginx configs in the fleet",
        ),
        EvalQuestion(
            id="NA-028",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the sshd-staging configuration have Compression disabled?",
            reference_answer="No. The sshd-staging.conf has Compression yes (enabled). Only sshd-prod and sshd-bastion have Compression no (disabled). This is a security weakness in the staging environment.",
            evidence_locations=["sshd-staging.conf -> Compression", "sshd-prod.conf -> Compression"],
            reasoning_required="Check Compression setting on staging sshd config",
        ),
        EvalQuestion(
            id="NA-029",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there any queue/message broker configuration (RabbitMQ, Kafka) in the fleet?",
            reference_answer="No. There is no RabbitMQ, Kafka, NATS, or any message broker configuration in the fleet. Redis is used for caching but not configured as a message broker (no Pub/Sub, Streams, or Celery broker settings).",
            evidence_locations=["compose-app-prod-01.yaml", "app-config-auth-jwt.json -> redis"],
            reasoning_required="Search for message broker configs in the fleet",
        ),
        EvalQuestion(
            id="NA-030",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the feature flags YAML config have an admin_audit_log feature enabled?",
            reference_answer="No. The yaml-app-feature-flags.yaml has admin_audit_log: false. The audit logging feature is defined but explicitly disabled.",
            evidence_locations=["yaml-app-feature-flags.yaml -> features.admin_audit_log"],
            reasoning_required="Check admin audit log feature flag status",
        ),
        EvalQuestion(
            id="NA-031",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a pg_hba.conf (PostgreSQL host-based authentication) file in the fleet?",
            reference_answer="No. There is no pg_hba.conf file in the fleet. PostgreSQL configs define password_encryption=scram-sha-256 but there is no host-based access control file defining which hosts can connect.",
            evidence_locations=["pg-prod-primary.conf -> password_encryption"],
            reasoning_required="Search for PostgreSQL HBA configuration in the fleet",
        ),
        EvalQuestion(
            id="NA-032",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the compose-ci.yaml define any volumes?",
            reference_answer="No. The compose-ci.yaml does not define any volumes or volume mounts for its services. The prod Compose files define host-mounted volumes like /opt/ridgeline/core-api/data:/app/data, but CI runs ephemeral containers.",
            evidence_locations=["compose-ci.yaml", "compose-app-prod-01.yaml -> volumes"],
            reasoning_required="Check for volume definitions in CI Compose config",
        ),
        EvalQuestion(
            id="NA-033",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there an SSL/TLS certificate rotation or renewal configuration in the fleet?",
            reference_answer="No. The fleet references SSL certificates (pg-prod-primary.conf ssl_cert_file, tfvars-prod.json ssl_cert_arn) but there is no certificate rotation or renewal configuration (no certbot, ACME, or Let's Encrypt config).",
            evidence_locations=["pg-prod-primary.conf -> ssl_cert_file", "tfvars-prod.json -> networking.ssl_cert_arn"],
            reasoning_required="Search for certificate management configs in the fleet",
        ),
        EvalQuestion(
            id="NA-034",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the systemd-backup-svc unit have a Restart directive?",
            reference_answer="No. The systemd-backup-svc.conf does not have a Restart directive. It is Type=oneshot with RemainAfterExit=yes. The other systemd units (core-api, legacy-dashboard) have Restart policies, but the backup service does not.",
            evidence_locations=["systemd-backup-svc.conf", "systemd-core-api.conf -> Restart"],
            reasoning_required="Check for restart policy on backup service systemd unit",
        ),
        EvalQuestion(
            id="NA-035",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a .env or environment variable file referenced by any Compose file?",
            reference_answer="No. The Compose files inline their environment variables directly (e.g., DATABASE_URL, API_KEY, JWT_SECRET) rather than referencing .env files or env_file directives. compose-ci.yaml uses ${CI_COMMIT_SHA} variable substitution but no .env file.",
            evidence_locations=["compose-app-prod-01.yaml -> environment", "compose-ci.yaml -> image"],
            reasoning_required="Search for .env file references in Compose configs",
        ),
        EvalQuestion(
            id="NA-036",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the Traefik prod configuration define any middleware?",
            reference_answer="No. The traefik-prod-static.yaml does not define any middleware (no rate limiting, authentication, IP whitelisting, or circuit breaker middleware). It has entrypoints, providers, TLS options, and metrics, but no middleware configuration.",
            evidence_locations=["traefik-prod-static.yaml"],
            reasoning_required="Check for middleware definitions in Traefik prod config",
        ),
        EvalQuestion(
            id="NA-037",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a cron or scheduling configuration for database vacuum/analyze in the fleet?",
            reference_answer="No. PostgreSQL autovacuum is enabled (autovacuum = on) with default-like thresholds, but there is no explicit cron job or scheduling for manual VACUUM or ANALYZE operations in the fleet.",
            evidence_locations=["pg-prod-primary.conf -> autovacuum"],
            reasoning_required="Search for database maintenance scheduling in the fleet",
        ),
        EvalQuestion(
            id="NA-038",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the ansible-emergency-rollback playbook set become: true?",
            reference_answer="No. The ansible-emergency-rollback.yaml has become: false at the play level. Individual tasks also have become: false. This means the rollback runs as the connecting user without privilege escalation, which may fail for system operations.",
            evidence_locations=["ansible-emergency-rollback.yaml -> become"],
            reasoning_required="Check privilege escalation in emergency rollback playbook",
        ),
        EvalQuestion(
            id="NA-039",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there an IP allowlist or deny list in any Traefik configuration?",
            reference_answer="No. None of the Traefik configs (prod, staging, dev) define IP allowlists or deny lists. There is no ipWhiteList middleware or ipAllowList configuration. The dashboard is exposed without IP restrictions (insecure: true on staging and dev).",
            evidence_locations=["traefik-prod-static.yaml", "traefik-staging.yaml -> api.insecure"],
            reasoning_required="Search for IP-based access control in Traefik configs",
        ),
        EvalQuestion(
            id="NA-040",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the monitoring Compose file define log rotation options for its containers?",
            reference_answer="No. The compose-monitoring-prod.yaml has logging driver: json-file for all services (prometheus, grafana, alertmanager) but does not specify log-opts (max-size, max-file). The Docker daemon configs define log-opts but the Compose file does not override them.",
            evidence_locations=["compose-monitoring-prod.yaml -> logging", "docker-daemon-prod.json -> log-opts"],
            reasoning_required="Check for log rotation in monitoring Compose config",
        ),
        EvalQuestion(
            id="NA-041",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a feature-flags configuration for the search service app config?",
            reference_answer="No. The app-config-search.json has a features section with fuzzy_search, autocomplete, and index_cache, but no feature-flags YAML equivalent exists for the search service. The yaml-app-feature-flags.yaml is for the ridgeline-platform, not the search service specifically.",
            evidence_locations=["app-config-search.json -> features", "yaml-app-feature-flags.yaml -> app.name"],
            reasoning_required="Check for search-specific feature flags config",
        ),
        EvalQuestion(
            id="NA-042",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the sysctl-dev configuration set vm.nr_hugepages?",
            reference_answer="No. The sysctl-dev.conf does not set vm.nr_hugepages. Only sysctl-db-tuned.conf sets vm.nr_hugepages = 1024. The dev sysctl config uses default kernel hugepages settings.",
            evidence_locations=["sysctl-dev.conf", "sysctl-db-tuned.conf -> vm.nr_hugepages"],
            reasoning_required="Check for huge pages configuration in dev sysctl",
        ),
        EvalQuestion(
            id="NA-043",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a secrets management tool configuration (Vault, AWS Secrets Manager, SOPS) in the fleet?",
            reference_answer="No. Secrets are stored as plaintext in config files (e.g., JWT_SECRET in Compose, database passwords in app configs, ssh_private_key in tfvars). There is no HashiCorp Vault, AWS Secrets Manager, SOPS, or any secrets management configuration.",
            evidence_locations=["compose-app-prod-01.yaml -> environment", "tfvars-prod.json -> secrets"],
            reasoning_required="Search for secrets management tool configs in the fleet",
        ),
        EvalQuestion(
            id="NA-044",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the docker-daemon-prod.json have an insecure-registries setting?",
            reference_answer="No. The docker-daemon-prod.json does not include an insecure-registries field. Only docker-daemon-dev.json has insecure-registries: [\"registry.ridgeline.internal:5000\"] configured, which allows unencrypted image pulls from the internal registry in dev only.",
            evidence_locations=["docker-daemon-prod.json", "docker-daemon-dev.json -> insecure-registries"],
            reasoning_required="Check for insecure registry in Docker daemon prod config",
        ),
        EvalQuestion(
            id="NA-045",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there a network segmentation or VLAN configuration in the fleet?",
            reference_answer="No. The fleet uses simple bridge networks in Docker Compose (app-network, monitoring, ci-network) and VPC CIDRs in tfvars, but there is no VLAN, network segmentation, or micro-segmentation configuration. Docker networks are flat bridges.",
            evidence_locations=["compose-app-prod-01.yaml -> networks", "tfvars-prod.json -> networking.vpc_cidr"],
            reasoning_required="Search for network segmentation configs in the fleet",
        ),
        EvalQuestion(
            id="NA-046",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the Traefik prod configuration have file provider watching a specific config file?",
            reference_answer="Yes. The traefik-prod-static.yaml providers.file section sets directory: /etc/traefik/dynamic with watch: true. However, only the prod config has the file provider; staging and dev do not include a file provider.",
            evidence_locations=["traefik-prod-static.yaml -> providers.file"],
            reasoning_required="Check for file provider in Traefik prod config",
        ),
        EvalQuestion(
            id="NA-047",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Is there any OpenTelemetry or distributed tracing configuration in the fleet?",
            reference_answer="No. There is no OpenTelemetry, Jaeger, Zipkin, or any distributed tracing configuration in the fleet. Prometheus is used for metrics, but there is no tracing instrumentation or collector.",
            evidence_locations=["prometheus-prod.yaml", "compose-monitoring-prod.yaml"],
            reasoning_required="Search for distributed tracing configs in the fleet",
        ),
        EvalQuestion(
            id="NA-048",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.EASY,
            question="Does the MariaDB legacy-main configuration use utf8mb4 character set?",
            reference_answer="No. The maria-legacy-main.cnf uses character_set_server = latin1 and collation_server = latin1_swedish_ci. The galera node (maria-galera-node.cnf), dev (maria-dev.cnf), billing (maria-legacy-billing.cnf), and staging (maria-staging.cnf) all use utf8mb4. Only legacy-main retains the latin1 charset.",
            evidence_locations=["maria-legacy-main.cnf -> character_set_server", "maria-galera-node.cnf -> character_set_server", "maria-dev.cnf -> character_set_server", "maria-legacy-billing.cnf -> character_set_server", "maria-staging.cnf -> character_set_server"],
            reasoning_required="Check character set on legacy MariaDB config",
        ),
        # ── Medium (NA-049 → NA-096) ────────────────────────────────────────────
        EvalQuestion(
            id="NA-049",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there any rate limiting configured at the Traefik reverse proxy level for the fleet?",
            reference_answer="No. None of the Traefik configs (prod, staging, dev) define rate limiting middleware. Rate limiting is only configured at the application level in app-config JSON files (e.g., auth-jwt: 1000 rpm, billing: 500 rpm, search: 2000 rpm) and in the YAML logging config (1000 rpm). There is no edge-level rate limiting.",
            evidence_locations=["traefik-prod-static.yaml", "app-config-auth-jwt.json -> rate_limiting", "yaml-app-logging-config.yaml -> rate_limiting"],
            reasoning_required="Compare rate limiting across proxy and application layers",
        ),
        EvalQuestion(
            id="NA-050",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a health check or readiness probe defined for the Redis service in the prod Compose files?",
            reference_answer="Yes, in compose-ci.yaml the Redis service has a healthcheck using redis-cli ping. However, in compose-app-prod-01.yaml the Redis service is not visible in the provided portion. The CI Redis has test: [\"CMD-SHELL\", \"redis-cli ping\"] with interval 30s, timeout 30s, retries 3.",
            evidence_locations=["compose-ci.yaml -> redis.healthcheck"],
            reasoning_required="Check for Redis health checks across Compose files",
        ),
        EvalQuestion(
            id="NA-051",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is database SSL enabled for the app-config-billing database connection?",
            reference_answer="No. The app-config-billing.json database section has ssl: false. Similarly, app-config-auth-jwt.json also has ssl: false. Neither application service connects to its database over SSL despite the PostgreSQL primary having SSL enabled.",
            evidence_locations=["app-config-billing.json -> database.ssl", "app-config-auth-jwt.json -> database.ssl"],
            reasoning_required="Check for SSL in application database connections",
        ),
        EvalQuestion(
            id="NA-052",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there any database read replica routing or load balancing configuration in the app configs?",
            reference_answer="No. App configs point to single database hosts (auth-jwt: db-prod-01:5432, billing: db-legacy-01:3306) with no read replica routing. The pg-prod-replica exists but is not referenced by any app config. There is no connection routing, failover, or read/write split configured.",
            evidence_locations=["app-config-auth-jwt.json -> database.host", "app-config-billing.json -> database.host", "pg-prod-replica.conf"],
            reasoning_required="Check for database replica usage in application configs",
        ),
        EvalQuestion(
            id="NA-053",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there any disk encryption or encryption-at-rest configuration in the fleet?",
            reference_answer="No. There is no dm-crypt/LUKS, EBS encryption, or filesystem-level encryption configured. The systemd-backup-svc has BACKUP_ENCRYPTION_KEY but that is for backup files, not disk encryption. Database configs do not reference tablespace encryption or TDE.",
            evidence_locations=["systemd-backup-svc.conf -> BACKUP_ENCRYPTION_KEY", "pg-prod-primary.conf"],
            reasoning_required="Search for disk/data encryption configs across the fleet",
        ),
        EvalQuestion(
            id="NA-054",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the Traefik staging or dev configuration have a file provider for dynamic configuration?",
            reference_answer="No. Only traefik-prod-static.yaml has a file provider (directory: /etc/traefik/dynamic, watch: true). Both traefik-staging.yaml and traefik-dev.yaml only have the Docker provider configured. Dynamic routing on staging/dev relies entirely on Docker labels.",
            evidence_locations=["traefik-staging.yaml -> providers", "traefik-dev.yaml -> providers", "traefik-prod-static.yaml -> providers.file"],
            reasoning_required="Compare provider configuration across Traefik environments",
        ),
        EvalQuestion(
            id="NA-055",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there any container resource limit (mem_limit/cpus) defined in the staging Compose file?",
            reference_answer="The compose-staging.yaml likely follows a similar pattern to other Compose files. The compose-ci.yaml has mem_limit and cpus for each service. The prod compose-app-prod-01.yaml sets mem_limit: 2g and cpus: 1.0 for core-api. Staging likely has similar limits but may differ in values.",
            evidence_locations=["compose-app-prod-01.yaml -> mem_limit", "compose-ci.yaml -> mem_limit"],
            reasoning_required="Check for resource limits in staging Compose config",
        ),
        EvalQuestion(
            id="NA-056",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a PostgreSQL configuration with log_min_duration_statement enabled for slow query tracking?",
            reference_answer="No. All PostgreSQL configs have log_min_duration_statement = -1, which means slow query logging is disabled. The MariaDB configs have slow_query_log = ON with long_query_time = 1 (or 2 for dev), but PostgreSQL does not log slow queries.",
            evidence_locations=["pg-prod-primary.conf -> log_min_duration_statement", "maria-legacy-main.cnf -> slow_query_log"],
            reasoning_required="Check for slow query logging in PostgreSQL configs",
        ),
        EvalQuestion(
            id="NA-057",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a health check endpoint verification in the ansible-base-server playbook?",
            reference_answer="No. The ansible-base-server.yaml configures base packages, users, and system settings but does not verify any health check endpoints. Only ansible-emergency-rollback.yaml verifies health endpoints (http://localhost:APP_PORT/health via ansible.builtin.uri).",
            evidence_locations=["ansible-base-server.yaml", "ansible-emergency-rollback.yaml -> Verify service health endpoint"],
            reasoning_required="Compare health check verification across Ansible playbooks",
        ),
        EvalQuestion(
            id="NA-058",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there any backup verification or restore testing configuration in the fleet?",
            reference_answer="No. The systemd-backup-svc.conf runs backup-all-dbs.sh with systemd-backup-timer.conf scheduling, but there is no backup verification, restore testing, or integrity check configuration. Backups are fire-and-forget with no validation step.",
            evidence_locations=["systemd-backup-svc.conf", "systemd-backup-timer.conf"],
            reasoning_required="Search for backup verification mechanisms in the fleet",
        ),
        EvalQuestion(
            id="NA-059",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the Traefik configuration have TLS 1.3 as the minimum version anywhere?",
            reference_answer="No. All Traefik configs (prod, staging, dev) set tls.options.default.minVersion to VersionTLS12. None use TLS 1.3 as the minimum. The PostgreSQL primary also uses ssl_min_protocol_version = TLSv1.2.",
            evidence_locations=["traefik-prod-static.yaml -> tls.options.default.minVersion", "traefik-staging.yaml -> tls.options.default.minVersion"],
            reasoning_required="Check minimum TLS version across all TLS configurations",
        ),
        EvalQuestion(
            id="NA-060",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is IPv6 enabled on any Docker Compose network in the fleet?",
            reference_answer="No. All Docker Compose network definitions have enable_ipv6: false. For example, compose-monitoring-prod.yaml defines monitoring and edge-network both with enable_ipv6: false. compose-ci.yaml defines ci-network with enable_ipv6: false.",
            evidence_locations=["compose-monitoring-prod.yaml -> networks", "compose-ci.yaml -> networks"],
            reasoning_required="Check for IPv6 enablement across Docker Compose networks",
        ),
        EvalQuestion(
            id="NA-061",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a PostgreSQL archive_command configured on the replica?",
            reference_answer="No. The pg-prod-replica.conf has archive_mode = off and no archive_command. Only pg-prod-primary.conf has archive_mode = on with archive_command = 'cp %p /archive/%f'. The replica does not perform WAL archiving.",
            evidence_locations=["pg-prod-replica.conf -> archive_mode", "pg-prod-primary.conf -> archive_command"],
            reasoning_required="Compare WAL archiving between primary and replica PostgreSQL configs",
        ),
        EvalQuestion(
            id="NA-062",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a container image scanning or vulnerability assessment configuration in the fleet?",
            reference_answer="No. There is no Trivy, Clair, Snyk, or any container image scanning configuration. Docker images are referenced by tag (e.g., ridgeline/core-api:2.1.0, postgres:16-alpine) but no image scanning or signing is configured.",
            evidence_locations=["compose-app-prod-01.yaml -> image", "compose-ci.yaml -> image"],
            reasoning_required="Search for container security scanning configs in the fleet",
        ),
        EvalQuestion(
            id="NA-063",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a monitoring or alerting configuration for the MariaDB databases?",
            reference_answer="No. Prometheus prod config scrapes core-api, auth, billing, and other services, but there is no MariaDB exporter or MySQL metrics scrape job configured. MariaDB performance_schema is ON on galera, but no metrics exporter collects from it.",
            evidence_locations=["prometheus-prod.yaml -> scrape_configs", "maria-galera-node.cnf -> performance_schema"],
            reasoning_required="Check for MariaDB monitoring integration in the fleet",
        ),
        EvalQuestion(
            id="NA-064",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the sshd-dev configuration disable PasswordAuthentication?",
            reference_answer="No. The sshd-dev.conf has PasswordAuthentication yes. Only sshd-prod and sshd-bastion disable password authentication (PasswordAuthentication no). Both staging and dev allow password-based SSH login.",
            evidence_locations=["sshd-dev.conf -> PasswordAuthentication", "sshd-prod.conf -> PasswordAuthentication"],
            reasoning_required="Compare password authentication across sshd environments",
        ),
        EvalQuestion(
            id="NA-065",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a disaster recovery runbook or failover automation configuration in the fleet?",
            reference_answer="No. There is a compose-dr.yaml file and tfvars-dr.json, and ansible-emergency-rollback.yaml, but there is no automated failover configuration (no Patroni, repmgr, or automated PostgreSQL failover). The DR setup is manual with rollback playbook.",
            evidence_locations=["ansible-emergency-rollback.yaml", "compose-dr.yaml"],
            reasoning_required="Search for automated failover and DR configs in the fleet",
        ),
        EvalQuestion(
            id="NA-066",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the cloud-init-app-server.yaml configure SSH authorized_keys for users?",
            reference_answer="No. The cloud-init-app-server.yaml defines users (deploy, ops) with groups, sudo, and shell settings, but does not include ssh_authorized_keys for any user. SSH keys would need to be provisioned separately.",
            evidence_locations=["cloud-init-app-server.yaml -> users"],
            reasoning_required="Check for SSH key provisioning in cloud-init config",
        ),
        EvalQuestion(
            id="NA-067",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is the new_billing feature flag enabled in the feature flags YAML config?",
            reference_answer="No. The yaml-app-feature-flags.yaml has new_billing: false. The billing API uses its own app-config-billing.json but the platform-level feature flag for new billing is disabled.",
            evidence_locations=["yaml-app-feature-flags.yaml -> features.new_billing"],
            reasoning_required="Check new_billing feature flag status",
        ),
        EvalQuestion(
            id="NA-068",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a network policy or firewall rule configuration for Docker Compose networks?",
            reference_answer="No. Docker Compose networks are defined with driver: bridge, external: false, internal: false, attachable: false. There are no network policies, iptables rules, or security groups attached to these networks. The networks are flat and unprotected.",
            evidence_locations=["compose-monitoring-prod.yaml -> networks", "compose-ci.yaml -> networks"],
            reasoning_required="Check for network-level security in Docker Compose configs",
        ),
        EvalQuestion(
            id="NA-069",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the Traefik prod configuration enable sniStrict for TLS?",
            reference_answer="No. The traefik-prod-static.yaml has tls.options.default.sniStrict: false. This means Traefik will serve a default certificate for requests without matching SNI, which could be a security concern.",
            evidence_locations=["traefik-prod-static.yaml -> tls.options.default.sniStrict"],
            reasoning_required="Check SNI strict mode in Traefik TLS configuration",
        ),
        EvalQuestion(
            id="NA-070",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a service discovery mechanism (Consul, etcd) configured in the fleet?",
            reference_answer="No. There is no Consul, etcd, or service discovery tool configured. Services reference each other by hostname (e.g., core-api:8000, auth-service:8001 in service mesh config) via Docker Compose DNS or static configuration.",
            evidence_locations=["yaml-app-service-mesh.yaml -> services", "compose-app-prod-01.yaml"],
            reasoning_required="Search for service discovery configs in the fleet",
        ),
        EvalQuestion(
            id="NA-071",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the Prometheus prod configuration have any recording rules defined?",
            reference_answer="No, not inline. The prometheus-prod.yaml references rule_files: [\"/etc/prometheus/rules/*.yaml\"] but no actual rule files are present in the fleet configuration. The rules directory is referenced but the rule content is absent.",
            evidence_locations=["prometheus-prod.yaml -> rule_files"],
            reasoning_required="Check for recording rules in Prometheus configuration",
        ),
        EvalQuestion(
            id="NA-072",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a container security context (read_only, no_new_privileges) enforced in the Compose files?",
            reference_answer="The Compose files set read_only: false for all services. No service runs with read_only: true filesystem. privileged: false is set consistently, but no_new_privileges is not configured. Containers can escalate privileges within their namespace.",
            evidence_locations=["compose-app-prod-01.yaml -> read_only", "compose-ci.yaml -> privileged"],
            reasoning_required="Check for container security hardening in Compose configs",
        ),
        EvalQuestion(
            id="NA-073",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a configuration for automatic database failover (e.g., Patroni, repmgr) in the PostgreSQL setup?",
            reference_answer="No. PostgreSQL is configured with primary/replica replication (wal_level=replica, hot_standby=on) but there is no Patroni, repmgr, pg_auto_failover, or any automatic failover tool configured. Failover is manual.",
            evidence_locations=["pg-prod-primary.conf -> wal_level", "pg-prod-replica.conf -> hot_standby"],
            reasoning_required="Search for automatic PostgreSQL failover configuration",
        ),
        EvalQuestion(
            id="NA-074",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a Prometheus scrape job for Traefik metrics?",
            reference_answer="Traefik exports metrics to its :8080 entrypoint (metrics.prometheus in traefik-prod-static.yaml), but the prometheus-prod.yaml scrape_configs need to be fully examined to determine if Traefik is scraped. The Traefik metrics are configured for export but may or may not have a matching Prometheus scrape job.",
            evidence_locations=["traefik-prod-static.yaml -> metrics.prometheus", "prometheus-prod.yaml -> scrape_configs"],
            reasoning_required="Cross-reference Traefik metrics export with Prometheus scrape targets",
        ),
        EvalQuestion(
            id="NA-075",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the fleet have any load testing or performance testing configuration beyond compose-load-test.yaml?",
            reference_answer="No. There is a compose-load-test.yaml file but no load testing tool configuration (no k6, Locust, JMeter, or Gatling config files). The load test compose file exists but the actual test definitions are absent from the fleet.",
            evidence_locations=["compose-load-test.yaml"],
            reasoning_required="Search for performance testing configs in the fleet",
        ),
        EvalQuestion(
            id="NA-076",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a database migration tool configuration (Alembic, Flyway, Liquibase) in the fleet?",
            reference_answer="No. There is a pg-migration.conf PostgreSQL config for the migration server, but no actual migration tool configuration (no alembic.ini, flyway.conf, or Liquibase changelog). Database schema migrations are not version-controlled in the fleet.",
            evidence_locations=["pg-migration.conf"],
            reasoning_required="Search for database migration tool configs in the fleet",
        ),
        EvalQuestion(
            id="NA-077",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the MariaDB legacy-billing configuration have slow_query_log enabled?",
            reference_answer="Yes, MariaDB configs have slow_query_log = ON. The maria-legacy-main.cnf has slow_query_log = ON with long_query_time = 1. The legacy-billing config follows the same pattern. This is a capability that IS present, unlike PostgreSQL which lacks slow query logging.",
            evidence_locations=["maria-legacy-main.cnf -> slow_query_log"],
            reasoning_required="Check slow query logging on MariaDB billing config",
        ),
        EvalQuestion(
            id="NA-078",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a HTTPS redirect (HTTP to HTTPS) configured in Traefik?",
            reference_answer="No. None of the Traefik configs define an HTTP-to-HTTPS redirect middleware or entrypoint redirect. The web (:80) and websecure (:443) entrypoints exist independently in prod and staging, but traffic on port 80 is not automatically redirected to 443.",
            evidence_locations=["traefik-prod-static.yaml -> entryPoints.web", "traefik-prod-static.yaml -> entryPoints.websecure"],
            reasoning_required="Check for HTTP-to-HTTPS redirect in Traefik configs",
        ),
        EvalQuestion(
            id="NA-079",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there an AppArmor or SELinux configuration in the fleet?",
            reference_answer="No. There are no AppArmor profiles, SELinux policies, or mandatory access control configurations in the fleet. Cloud-init does not install or configure AppArmor/SELinux. Systemd units do not reference security profiles.",
            evidence_locations=["cloud-init-app-server.yaml", "systemd-core-api.conf"],
            reasoning_required="Search for mandatory access control configs in the fleet",
        ),
        EvalQuestion(
            id="NA-080",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the tfvars-staging configuration define multiple subnets?",
            reference_answer="No. The tfvars-staging.json networking.subnet_cidrs contains only one subnet: [\"10.1.1.0/24\"]. The prod config has three subnets (10.0.1.0/24, 10.0.2.0/24, 10.0.3.0/24), making staging a single-AZ deployment.",
            evidence_locations=["tfvars-staging.json -> networking.subnet_cidrs", "tfvars-prod.json -> networking.subnet_cidrs"],
            reasoning_required="Compare subnet configuration between staging and prod terraform vars",
        ),
        EvalQuestion(
            id="NA-081",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there an audit logging configuration for database operations in PostgreSQL?",
            reference_answer="No. PostgreSQL configs have log_statement = 'none' and log_connections = off, log_disconnections = off. There is no pgAudit extension or statement-level audit logging configured. MariaDB has general_log = OFF as well.",
            evidence_locations=["pg-prod-primary.conf -> log_statement", "pg-prod-primary.conf -> log_connections"],
            reasoning_required="Check for audit logging in database configurations",
        ),
        EvalQuestion(
            id="NA-082",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the compose-monitoring-prod.yaml include a node_exporter service?",
            reference_answer="No. The compose-monitoring-prod.yaml only defines prometheus, grafana, and alertmanager services. There is no node_exporter for host-level metrics (CPU, memory, disk). Infrastructure metrics collection is absent.",
            evidence_locations=["compose-monitoring-prod.yaml -> services"],
            reasoning_required="Check for node exporter in monitoring Compose config",
        ),
        EvalQuestion(
            id="NA-083",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a circuit breaker or retry configuration at the Traefik level?",
            reference_answer="No. Traefik configs do not define any circuit breaker, retry, or resilience middleware. The service mesh YAML config defines per-service retries (e.g., core-api: 3, auth-service: 2) but this is at the application level, not the proxy level.",
            evidence_locations=["traefik-prod-static.yaml", "yaml-app-service-mesh.yaml -> services"],
            reasoning_required="Check for circuit breaker/retry configs across proxy and app layers",
        ),
        EvalQuestion(
            id="NA-084",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a container image tag pinning strategy enforced in the fleet?",
            reference_answer="Partially. Application images use version tags (ridgeline/core-api:2.1.0, ridgeline/auth-service:1.8.0). Infrastructure images use version tags (prom/prometheus:v2.51.0, grafana/grafana:10.4.0). But some use branch/variant tags (postgres:16-alpine, redis:7-alpine, mariadb:10.11) which are mutable and not digest-pinned.",
            evidence_locations=["compose-app-prod-01.yaml -> image", "compose-ci.yaml -> image", "compose-monitoring-prod.yaml -> image"],
            reasoning_required="Analyze image tag pinning across Compose files",
        ),
        EvalQuestion(
            id="NA-085",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a shared configuration management system (Ansible Tower/AWX, Puppet, Chef) configured in the fleet?",
            reference_answer="No. There are standalone Ansible playbooks but no Ansible Tower, AWX, Puppet, Chef, or SaltStack server configuration. Ansible inventories are defined in YAML files (ansible-inventory-prod.yaml, etc.) but there is no centralized configuration management platform.",
            evidence_locations=["ansible-inventory-prod.yaml", "ansible-base-server.yaml"],
            reasoning_required="Search for centralized config management platform configs",
        ),
        EvalQuestion(
            id="NA-086",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the fleet define any Docker Compose health check for the MariaDB service in production?",
            reference_answer="The compose-ci.yaml defines a MariaDB health check (healthcheck.sh --connect --innodb_initialized). Production Compose files for the billing service likely also reference MariaDB, but the mariadb service in CI is the only visible healthcheck definition.",
            evidence_locations=["compose-ci.yaml -> mariadb.healthcheck"],
            reasoning_required="Check for MariaDB health checks across Compose environments",
        ),
        EvalQuestion(
            id="NA-087",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a Terraform backend configuration for remote state storage?",
            reference_answer="No. The tfvars files define workspace, compute, networking, and features, but there is no Terraform backend configuration for remote state storage (no S3/GCS/Azure blob backend). The tags.managed_by is 'opentofu' but state management is not configured in the fleet.",
            evidence_locations=["tfvars-prod.json -> tags.managed_by", "tfvars-staging.json"],
            reasoning_required="Search for Terraform state backend configuration in the fleet",
        ),
        EvalQuestion(
            id="NA-088",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the fleet have any S3 or object storage configuration for backups?",
            reference_answer="No. The PostgreSQL archive_command copies WAL files to /archive/%f (local disk), not to S3 or object storage. The backup service runs backup-all-dbs.sh but no S3/GCS/MinIO destination is configured. All backups are stored locally.",
            evidence_locations=["pg-prod-primary.conf -> archive_command", "systemd-backup-svc.conf"],
            reasoning_required="Check for object storage in backup configurations",
        ),
        EvalQuestion(
            id="NA-089",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a Prometheus alerting rule for any specific condition in the fleet?",
            reference_answer="No. The prometheus-prod.yaml references rule_files: [\"/etc/prometheus/rules/*.yaml\"] but no actual rule files or alert definitions are present in the fleet. Alertmanager is deployed and configured as a target, but no alerts are defined.",
            evidence_locations=["prometheus-prod.yaml -> rule_files", "prometheus-prod.yaml -> alerting"],
            reasoning_required="Check for concrete alerting rules in Prometheus configuration",
        ),
        EvalQuestion(
            id="NA-090",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does any PostgreSQL config enable JIT compilation by default at a lower cost threshold?",
            reference_answer="No. All PostgreSQL configs have jit = on but use default high cost thresholds: jit_above_cost = 100000, jit_inline_above_cost = 500000, jit_optimize_above_cost = 500000. No config lowers these thresholds for more aggressive JIT usage.",
            evidence_locations=["pg-prod-primary.conf -> jit_above_cost", "pg-prod-replica.conf -> jit_above_cost"],
            reasoning_required="Check JIT cost thresholds across PostgreSQL configs",
        ),
        EvalQuestion(
            id="NA-091",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a canary deployment or blue-green deployment configuration in the fleet?",
            reference_answer="No. Docker Compose deploy sections use update_config with parallelism: 1 and order: stop-first. There is no canary routing, traffic splitting, or blue-green deployment configuration. The ansible-deploy playbooks do direct deployments without canary stages.",
            evidence_locations=["compose-app-prod-01.yaml -> deploy.update_config", "ansible-deploy-core-api.yaml"],
            reasoning_required="Search for progressive delivery configs in the fleet",
        ),
        EvalQuestion(
            id="NA-092",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the cloud-init configuration disable SSH password authentication?",
            reference_answer="No. The cloud-init-app-server.yaml configures SSH (emit_keys_to_console, ssh_deletekeys, disable_root) but does not set ssh_pwauth to false. SSH password authentication status is left to the sshd config, creating a dependency on the sshd hardening playbook running after cloud-init.",
            evidence_locations=["cloud-init-app-server.yaml -> ssh"],
            reasoning_required="Check for SSH password auth control in cloud-init config",
        ),
        EvalQuestion(
            id="NA-093",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a Galera Cluster SST (State Snapshot Transfer) configuration in the galera MariaDB config?",
            reference_answer="No. The maria-galera-node.cnf is configured as a single MariaDB server with server_id=100 and galera-tuned settings (innodb_flush_log_at_trx_commit=2, innodb_change_buffering=none), but there is no wsrep_ configuration (wsrep_cluster_address, wsrep_sst_method, etc.) for actual Galera clustering.",
            evidence_locations=["maria-galera-node.cnf"],
            reasoning_required="Check for Galera clustering configuration in the galera MariaDB config",
        ),
        EvalQuestion(
            id="NA-094",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there any log rotation configuration for systemd service logs?",
            reference_answer="No. The systemd unit files do not configure journal log rotation (SystemMaxUse, SystemMaxFileSize, RuntimeMaxUse). Log rotation depends on system-wide journald defaults. The Docker daemon configs set log-opts (max-size, max-file) but systemd services rely on unspecified defaults.",
            evidence_locations=["systemd-core-api.conf", "systemd-legacy-dashboard.conf"],
            reasoning_required="Check for log rotation in systemd unit configurations",
        ),
        EvalQuestion(
            id="NA-095",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Is there a configuration for PostgreSQL connection timeout or statement timeout?",
            reference_answer="No. PostgreSQL configs set authentication_timeout = 1min but do not set statement_timeout or idle_in_transaction_session_timeout. Long-running queries and idle transactions are not automatically killed. MariaDB has wait_timeout = 28800 (8 hours) and connect_timeout = 10.",
            evidence_locations=["pg-prod-primary.conf -> authentication_timeout", "maria-legacy-main.cnf -> wait_timeout"],
            reasoning_required="Check for query timeout settings in database configs",
        ),
        EvalQuestion(
            id="NA-096",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.MEDIUM,
            question="Does the fleet have any DNS configuration beyond the Docker daemon?",
            reference_answer="No. Only docker-daemon-prod.json defines DNS servers (8.8.8.8, 8.8.4.4). There is no resolv.conf management, no CoreDNS/Unbound config, and cloud-init has manage_resolv_conf: false. DNS resolution relies on host defaults except for Docker containers in prod.",
            evidence_locations=["docker-daemon-prod.json -> dns", "cloud-init-app-server.yaml -> manage_resolv_conf"],
            reasoning_required="Search for DNS configuration across the fleet",
        ),
        # ── Hard (NA-097 → NA-120) ──────────────────────────────────────────────
        EvalQuestion(
            id="NA-097",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What critical security gaps exist from the absence of a centralized secrets management solution across the fleet?",
            reference_answer="The fleet has no HashiCorp Vault, AWS Secrets Manager, SOPS, or any centralized secrets management. Secrets are hardcoded as plaintext across: Compose env vars (JWT_SECRET, DATABASE_URL with passwords, API_KEY), app config JSON files (jwt_secret, encryption_key, api_key, database passwords), tfvars (database.password, secrets.api_secret, ssh_private_key), systemd units (DATABASE_URL with passwords, BACKUP_ENCRYPTION_KEY, SECRET_KEY), Ansible vars (deploy_key RSA private key), and Docker Compose alertmanager (SLACK_WEBHOOK_URL). This means: (1) secrets are in version control, (2) no secret rotation without redeploying, (3) no access audit trail, (4) lateral movement is trivial with any config file access.",
            evidence_locations=["compose-app-prod-01.yaml -> environment", "tfvars-prod.json -> secrets", "systemd-core-api.conf -> DATABASE_URL", "ansible-emergency-rollback.yaml -> vars.deploy_key"],
            reasoning_required="Comprehensive analysis of secrets management gaps and their security implications",
        ),
        EvalQuestion(
            id="NA-098",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What are the implications of the fleet lacking any form of centralized authentication or identity provider?",
            reference_answer="The fleet has no LDAP, Active Directory, SAML, OAuth2 provider, or centralized identity system. Each layer manages authentication independently: SSH uses key-based auth (sshd configs), PostgreSQL uses scram-sha-256 local passwords, MariaDB uses local mysql users, app services use JWT tokens with per-service secrets, Grafana uses environment variable admin password. This creates: (1) no single sign-on, (2) no centralized user lifecycle (offboarding requires touching every system), (3) no MFA at infrastructure level (only app-config-auth-jwt has mfa_required), (4) password policies are inconsistent across systems.",
            evidence_locations=["sshd-prod.conf -> PubkeyAuthentication", "pg-prod-primary.conf -> password_encryption", "app-config-auth-jwt.json -> auth", "compose-monitoring-prod.yaml -> GF_SECURITY_ADMIN_PASSWORD"],
            reasoning_required="Analyze identity management gaps across all fleet components",
        ),
        EvalQuestion(
            id="NA-099",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What monitoring blind spots exist from the absence of host-level metrics collection and log aggregation?",
            reference_answer="The fleet deploys Prometheus, Grafana, and Alertmanager but has critical monitoring gaps: (1) No node_exporter — no host CPU, memory, disk, or network metrics, (2) No log aggregation (no ELK/Loki/Fluentd) — application logs go to stdout/json-file with no centralized search, (3) No MariaDB exporter — legacy database metrics are invisible, (4) No PostgreSQL exporter visible in scrape configs, (5) No container-level metrics (cAdvisor), (6) No distributed tracing (no OpenTelemetry/Jaeger). The monitoring stack sees application metrics but is blind to infrastructure health.",
            evidence_locations=["compose-monitoring-prod.yaml -> services", "prometheus-prod.yaml -> scrape_configs", "yaml-app-logging-config.yaml -> logging"],
            reasoning_required="Comprehensive analysis of monitoring coverage gaps across the fleet",
        ),
        EvalQuestion(
            id="NA-100",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What disaster recovery weaknesses arise from the fleet's backup architecture lacking verification, offsite storage, and MariaDB coverage?",
            reference_answer="The backup architecture has multiple gaps: (1) systemd-backup-svc only depends on postgresql.service — MariaDB is not included in the backup dependency chain, (2) PostgreSQL archive_command copies WAL to /archive/%f on local disk, not offsite/S3, (3) No backup verification or restore testing — backups are fire-and-forget, (4) tfvars-dev has enable_backups: false and backup_retention_days: 0, (5) No point-in-time recovery testing, (6) pg-prod-replica has archive_mode = off so the replica cannot independently archive, (7) MariaDB has no equivalent backup mechanism.",
            evidence_locations=["systemd-backup-svc.conf -> After", "pg-prod-primary.conf -> archive_command", "pg-prod-replica.conf -> archive_mode", "tfvars-dev.json -> features"],
            reasoning_required="Analyze backup and DR gaps across database and infrastructure layers",
        ),
        EvalQuestion(
            id="NA-101",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What security risks arise from the absence of network segmentation between application, database, and monitoring tiers?",
            reference_answer="Docker Compose networks are flat bridges with no segmentation: (1) compose-app-prod-01 puts core-api on both app-network and monitoring — it can reach monitoring services directly, (2) No internal: true networks to prevent external access, (3) All networks have enable_ipv6: false but no iptables rules, (4) Database services share networks with application services, (5) No network policies or security groups within Docker, (6) tfvars define VPC CIDRs but no subnet-level security groups or NACLs are configured, (7) Traefik exposedByDefault: true means all Docker services are automatically exposed.",
            evidence_locations=["compose-app-prod-01.yaml -> networks", "compose-monitoring-prod.yaml -> networks", "traefik-prod-static.yaml -> providers.docker.exposedByDefault"],
            reasoning_required="Analyze network segmentation gaps and their security implications",
        ),
        EvalQuestion(
            id="NA-102",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What operational risks stem from the fleet lacking any automated failover for its database tier?",
            reference_answer="The fleet has no automated database failover: (1) PostgreSQL has primary/replica with hot_standby=on but no Patroni, repmgr, or pg_auto_failover, (2) MariaDB galera-node has server_id=100 but no wsrep_ clustering configuration — it's a standalone node named 'galera' without actual Galera, (3) ansible-emergency-rollback.yaml is manual and has become: false (may fail for system tasks), (4) App configs point to single database hosts (db-prod-01, db-legacy-01) with no failover endpoints, (5) No HAProxy or ProxySQL for database connection routing. Database failure requires manual intervention.",
            evidence_locations=["pg-prod-primary.conf -> wal_level", "maria-galera-node.cnf", "ansible-emergency-rollback.yaml -> become", "app-config-auth-jwt.json -> database.host"],
            reasoning_required="Analyze database failover gaps across PostgreSQL and MariaDB",
        ),
        EvalQuestion(
            id="NA-103",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What are the implications of the fleet having no HTTP-to-HTTPS redirect, no WAF, and insecure Traefik dashboards in non-prod environments?",
            reference_answer="The fleet's edge security has multiple gaps: (1) No HTTP-to-HTTPS redirect — port 80 serves traffic without redirecting to 443, (2) No WAF middleware — Traefik has no ModSecurity, rate limiting, or request filtering at the proxy level, (3) Traefik dashboard insecure: true on staging and dev — the management dashboard is accessible without authentication, (4) insecureSkipVerify: true on staging and dev serversTransport — backend TLS verification is skipped, (5) sniStrict: false on all environments — default cert served for unmatched SNI, (6) No IP allowlists on any Traefik config. Combined, these make the fleet vulnerable to DDoS, injection attacks, and unauthorized management access.",
            evidence_locations=["traefik-prod-static.yaml -> entryPoints", "traefik-staging.yaml -> api.insecure", "traefik-dev.yaml -> api.insecure", "traefik-staging.yaml -> serversTransport.insecureSkipVerify"],
            reasoning_required="Comprehensive analysis of edge security gaps across Traefik environments",
        ),
        EvalQuestion(
            id="NA-104",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What gaps exist in the fleet's container security posture from the absence of image scanning, read-only filesystems, and security contexts?",
            reference_answer="Container security is weak: (1) No image scanning (Trivy, Clair, Snyk) configured, (2) All containers have read_only: false — writable filesystem inside containers, (3) No no_new_privileges or seccomp profiles configured, (4) privileged: false is set but that's the minimum, (5) Some images use mutable tags (postgres:16-alpine, redis:7-alpine) that could be compromised, (6) compose-ci uses ${CI_COMMIT_SHA} for app images but infrastructure images are pinned to version tags (not digests), (7) docker-daemon-dev has insecure-registries allowing unencrypted image pulls, (8) No AppArmor/SELinux profiles referenced.",
            evidence_locations=["compose-app-prod-01.yaml -> read_only", "compose-ci.yaml -> image", "docker-daemon-dev.json -> insecure-registries"],
            reasoning_required="Comprehensive analysis of container security hardening gaps",
        ),
        EvalQuestion(
            id="NA-105",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What are the implications of the fleet having no connection pooling, no statement timeout, and no slow query logging in PostgreSQL?",
            reference_answer="PostgreSQL operational gaps: (1) No PgBouncer/pgpool — each app manages its own pool (pool_size=10) against max_connections=200, multiple services could exhaust connections, (2) log_min_duration_statement = -1 means no slow query logging, unlike MariaDB which has slow_query_log = ON, (3) No statement_timeout — runaway queries run indefinitely, (4) No idle_in_transaction_session_timeout — idle transactions hold locks forever, (5) log_connections = off and log_disconnections = off — connection lifecycle is invisible, (6) No query plan logging (auto_explain not configured). These gaps make PostgreSQL performance issues very hard to diagnose.",
            evidence_locations=["pg-prod-primary.conf -> log_min_duration_statement", "pg-prod-primary.conf -> log_connections", "app-config-auth-jwt.json -> database.pool_size"],
            reasoning_required="Analyze PostgreSQL operational visibility and resource management gaps",
        ),
        EvalQuestion(
            id="NA-106",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What CI/CD gaps exist from the fleet having only a Docker Compose CI file but no pipeline definition, deployment automation, or artifact management?",
            reference_answer="The fleet's CI/CD is skeletal: (1) compose-ci.yaml runs pytest but there is no actual CI pipeline (no GitHub Actions, GitLab CI, Jenkins), (2) No artifact registry configuration — images are referenced but no build/push pipeline exists, (3) Ansible deploy playbooks exist but are not triggered by CI, (4) No staged deployment (no canary, blue-green, or progressive delivery), (5) Compose update_config uses stop-first order which causes downtime, (6) No rollback automation — ansible-emergency-rollback exists but is manual and has become: false, (7) No integration testing beyond the CI compose.",
            evidence_locations=["compose-ci.yaml", "ansible-deploy-core-api.yaml", "compose-app-prod-01.yaml -> deploy.update_config"],
            reasoning_required="Analyze CI/CD maturity gaps across the fleet",
        ),
        EvalQuestion(
            id="NA-107",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What are the security implications of the fleet having no SSL for application-to-database connections despite PostgreSQL having SSL enabled?",
            reference_answer="Critical SSL gap: (1) pg-prod-primary has ssl = on with ssl_cert_file and ssl_key_file configured, (2) But app-config-auth-jwt.json database.ssl = false and app-config-billing.json database.ssl = false, (3) pg-prod-replica has ssl = off entirely, (4) Compose env vars use postgresql:// URLs without sslmode parameter, (5) systemd-core-api DATABASE_URL lacks sslmode, (6) MariaDB configs do not configure SSL at all. This means database traffic flows unencrypted on the network even though the PostgreSQL primary supports SSL. An attacker on the network can intercept database credentials and query data.",
            evidence_locations=["pg-prod-primary.conf -> ssl", "app-config-auth-jwt.json -> database.ssl", "app-config-billing.json -> database.ssl", "pg-prod-replica.conf -> ssl"],
            reasoning_required="Trace SSL configuration from database server to application client",
        ),
        EvalQuestion(
            id="NA-108",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What gaps exist in the fleet's observability from missing alerting rules, missing exporters, and no SLO/SLI definitions?",
            reference_answer="Observability is incomplete: (1) Prometheus rule_files references /etc/prometheus/rules/*.yaml but no rules exist in the fleet, (2) Alertmanager is deployed but has no alert routing rules or notification channels beyond SLACK_WEBHOOK_URL env var, (3) No database exporters (postgres_exporter, mysqld_exporter) in scrape configs, (4) No node_exporter for host metrics, (5) No SLO/SLI definitions or error budget tracking, (6) No custom dashboards in Grafana (only volume mount, no provisioning), (7) Traefik addRoutersLabels: false means router-level metrics are not collected. The monitoring stack is deployed but minimally configured.",
            evidence_locations=["prometheus-prod.yaml -> rule_files", "compose-monitoring-prod.yaml -> alertmanager", "traefik-prod-static.yaml -> metrics.prometheus.addRoutersLabels"],
            reasoning_required="Comprehensive analysis of observability and alerting gaps",
        ),
        EvalQuestion(
            id="NA-109",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What operational risks arise from the fleet having no certificate management, no ACME/Let's Encrypt, and no certificate rotation?",
            reference_answer="Certificate management is manual and fragile: (1) PostgreSQL SSL cert is at /etc/ssl/certs/server.crt with passphrase in config (ssl_passphrase_command = 'echo Sup3rS3cretPgPassw0rd'), (2) tfvars-prod references ssl_cert_arn for AWS but no renewal automation, (3) Traefik has no ACME/Let's Encrypt certificatesResolvers configured, (4) No cert-manager or certbot config exists, (5) tls.options only sets minVersion but no certificate stores, (6) TLS certificates are static and will expire without alerting. Certificate expiry will cause outages with no automated renewal.",
            evidence_locations=["pg-prod-primary.conf -> ssl_passphrase_command", "tfvars-prod.json -> networking.ssl_cert_arn", "traefik-prod-static.yaml -> tls"],
            reasoning_required="Analyze certificate lifecycle management gaps across the fleet",
        ),
        EvalQuestion(
            id="NA-110",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What compliance and audit gaps exist from the fleet lacking admin audit logging, database audit logging, and centralized access logging?",
            reference_answer="Audit capability is nearly absent: (1) yaml-app-feature-flags has admin_audit_log: false — admin actions are not logged, (2) PostgreSQL log_statement = 'none', log_connections = off — no SQL audit trail, (3) MariaDB general_log = OFF — no query audit, (4) No pgAudit extension configured, (5) sshd-prod has LogLevel VERBOSE but logs go to syslog with no centralized collection, (6) Traefik access logs exist (accessLog format: common) but are not shipped to a SIEM, (7) No audit trail for Grafana, Prometheus, or Compose operations. The fleet cannot demonstrate compliance with standards like SOC2, PCI-DSS, or GDPR that require audit logging.",
            evidence_locations=["yaml-app-feature-flags.yaml -> features.admin_audit_log", "pg-prod-primary.conf -> log_statement", "sshd-prod.conf -> LogLevel"],
            reasoning_required="Comprehensive analysis of audit and compliance gaps across all systems",
        ),
        EvalQuestion(
            id="NA-111",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What risks exist from the MariaDB galera-node being named 'galera' but lacking actual Galera Cluster configuration?",
            reference_answer="The maria-galera-node.cnf is misleadingly named — it has no Galera configuration: (1) No wsrep_provider, wsrep_cluster_address, or wsrep_cluster_name, (2) No wsrep_sst_method for state transfer, (3) No wsrep_node_address or wsrep_node_name, (4) innodb_flush_log_at_trx_commit=2 and innodb_change_buffering=none are Galera-recommended settings but without wsrep, it's a standalone node, (5) sync_binlog=0 (unsafe without Galera's certification-based replication), (6) server_id=100 suggests intended multi-node but not implemented. This node provides no HA benefit and the naming creates false confidence in database redundancy.",
            evidence_locations=["maria-galera-node.cnf"],
            reasoning_required="Analyze the gap between the galera node's naming and actual clustering configuration",
        ),
        EvalQuestion(
            id="NA-112",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What security gaps exist from the fleet's sshd configurations lacking consistent hardening across environments?",
            reference_answer="SSH hardening is inconsistent: (1) sshd-prod and sshd-bastion disable password auth (PasswordAuthentication no), but staging and dev allow it (PasswordAuthentication yes), (2) Only prod and bastion have AllowUsers — staging and dev have unrestricted user access, (3) Only prod and bastion have Banner configured, (4) Staging and dev have Compression yes (compression oracle attacks), (5) Dev has PermitRootLogin prohibit-password (allows key-based root login), (6) Staging/dev have MaxAuthTries 6 vs prod's 3, (7) No AllowUsers on staging means any valid user can SSH in, (8) Dev has X11Forwarding yes creating a GUI attack surface. Non-prod environments are significantly weaker.",
            evidence_locations=["sshd-prod.conf", "sshd-bastion.conf", "sshd-staging.conf", "sshd-dev.conf"],
            reasoning_required="Comprehensive comparison of SSH hardening across all environments",
        ),
        EvalQuestion(
            id="NA-113",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What operational risks arise from the fleet lacking any form of infrastructure-as-code state management or drift detection?",
            reference_answer="IaC management is incomplete: (1) tfvars files define parameters but no Terraform/OpenTofu state backend (no S3/GCS remote state), (2) No state locking mechanism configured, (3) Ansible playbooks exist but no AWX/Tower for run tracking, (4) No drift detection (no terraform plan scheduled runs, no Ansible check mode cron), (5) Docker Compose files are applied manually with no state tracking, (6) Cloud-init runs once at provisioning but subsequent drift is undetected, (7) tags.managed_by='opentofu' but no OpenTofu configuration beyond tfvars. Configuration drift between declared and actual state is invisible.",
            evidence_locations=["tfvars-prod.json -> tags.managed_by", "ansible-inventory-prod.yaml", "cloud-init-app-server.yaml"],
            reasoning_required="Analyze infrastructure state management and drift detection gaps",
        ),
        EvalQuestion(
            id="NA-114",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What are the implications of the fleet having no environment parity between dev, staging, and prod tfvars?",
            reference_answer="Environment parity is poor: (1) Dev has no edge_instance_type (prod has t3.medium, staging has t3.small), (2) Dev has 1 subnet (10.2.1.0/24) vs prod's 3 subnets, (3) Dev uses domain dev.ridgeline.internal (internal) vs prod's ridgeline.io (public), (4) Dev has enable_monitoring: false and enable_backups: false (prod: both true), (5) Dev has no ssl_cert_arn (prod has ACM cert), (6) Dev app_instance_type is t3.small vs prod's t3.xlarge, (7) Dev db_instance_type is t3.medium vs prod's r6g.2xlarge. Issues discovered in dev may not reproduce in prod due to entirely different architecture, and dev cannot test edge, SSL, monitoring, or backup functionality.",
            evidence_locations=["tfvars-dev.json", "tfvars-staging.json", "tfvars-prod.json"],
            reasoning_required="Compare infrastructure definitions across all three environments",
        ),
        EvalQuestion(
            id="NA-115",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What security risks arise from the fleet's Docker daemon configurations lacking consistent security settings across environments?",
            reference_answer="Docker daemon security is inconsistent: (1) Prod has userland-proxy: false (good) but dev has userland-proxy: true (bypasses iptables), (2) Dev has experimental: true and debug: true — exposes debug APIs, (3) Dev has insecure-registries allowing unencrypted pulls from registry.ridgeline.internal:5000, (4) CI has live-restore: false (containers die on daemon restart), (5) Dev has no dns setting (uses host DNS), no metrics-addr (no monitoring), (6) Prod log rotation (50m/5 files) vs dev (100m/3 files) vs CI (10m/2 files) — no standard, (7) Dev ulimits are lower (1024/1024) vs prod (65535/65535). The dev Docker daemon is essentially unhardenened.",
            evidence_locations=["docker-daemon-prod.json", "docker-daemon-dev.json", "docker-daemon-ci.json"],
            reasoning_required="Compare Docker daemon security posture across all environments",
        ),
        EvalQuestion(
            id="NA-116",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What are the implications of the fleet having no service mesh authentication, no mTLS, and no service-to-service authorization?",
            reference_answer="Service-to-service security is absent: (1) yaml-app-service-mesh.yaml defines service URLs (http://, not https://) with no mTLS, (2) Services communicate over plain HTTP within Docker networks, (3) No service-to-service JWT or API key validation beyond the initial client JWT, (4) Docker bridge networks provide no encryption in transit, (5) Traefik serversTransport.insecureSkipVerify: false in prod but true in staging/dev — backend TLS is not verified, (6) No Istio, Linkerd, or Consul Connect for service mesh security, (7) Any container on the network can call any service without authentication. East-west traffic is completely unprotected.",
            evidence_locations=["yaml-app-service-mesh.yaml -> services", "traefik-prod-static.yaml -> serversTransport", "compose-app-prod-01.yaml -> networks"],
            reasoning_required="Analyze service-to-service security gaps across the fleet",
        ),
        EvalQuestion(
            id="NA-117",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What capacity planning gaps exist from the fleet lacking resource monitoring, auto-scaling, and capacity alerts?",
            reference_answer="Capacity planning is blind: (1) No node_exporter means no host disk/CPU/memory trending, (2) No auto-scaling configured — compose deploy.replicas are static (core-api: 2, grafana: 1, etc.), (3) PostgreSQL max_connections = 200 is static with no connection monitoring, (4) MariaDB max_connections varies (151, 200, 300) with no load-based adjustment, (5) No disk usage monitoring for /archive (WAL archives grow unbounded), (6) Docker log rotation varies across daemons but no alerting on log disk usage, (7) No Prometheus alerts for capacity thresholds, (8) tfvars instance counts are fixed with no ASG configuration. The fleet cannot detect or respond to capacity pressure.",
            evidence_locations=["compose-app-prod-01.yaml -> deploy.replicas", "pg-prod-primary.conf -> max_connections", "compose-monitoring-prod.yaml -> services"],
            reasoning_required="Comprehensive analysis of capacity planning and auto-scaling gaps",
        ),
        EvalQuestion(
            id="NA-118",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What are the implications of the emergency rollback playbook having become: false, ignore_errors: true, and no actual rollback logic?",
            reference_answer="The emergency rollback is dangerously weak: (1) become: false at play level and per-task — cannot restart systemd services, modify system files, or perform privileged operations, (2) ignore_errors: true at play level — failures are silently swallowed, masking rollback failures during incidents, (3) The playbook only starts a service, waits for port, and checks health — it does not actually perform a rollback (no image tag revert, no deployment version change, no database migration rollback), (4) any_errors_fatal: false means partial failures don't stop the play, (5) vars.rollback_version = 'previous' is defined but never used in tasks, (6) gather_facts: false means the playbook has no system context. This playbook gives false confidence in rollback capability.",
            evidence_locations=["ansible-emergency-rollback.yaml"],
            reasoning_required="Deep analysis of the emergency rollback playbook's actual capabilities vs expectations",
        ),
        EvalQuestion(
            id="NA-119",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What risks stem from the fleet having no consistent restart policy across Docker Compose, systemd, and application layers?",
            reference_answer="Restart policies are inconsistent: (1) Compose: core-api uses restart: 'always', but monitoring services use 'unless-stopped', CI uses 'no', (2) Compose deploy.restart_policy uses condition: any with max_attempts: 0 (unlimited) — contradicts the service-level restart setting, (3) Systemd: core-api uses Restart=on-failure (won't restart on clean exit), legacy-dashboard uses Restart=always, backup-svc has no Restart (oneshot), (4) No exponential backoff — RestartSec=5 is flat, (5) No restart rate limiting (StartLimitBurst/StartLimitIntervalSec not set), (6) A crashing service could restart-loop consuming resources without alerting. The fleet has no consistent restart strategy and no monitoring of restart events.",
            evidence_locations=["compose-app-prod-01.yaml -> restart", "compose-monitoring-prod.yaml -> restart", "systemd-core-api.conf -> Restart", "systemd-legacy-dashboard.conf -> Restart"],
            reasoning_required="Compare restart policies across all orchestration layers",
        ),
        EvalQuestion(
            id="NA-120",
            question_class=EvalQuestionClass.NEGATIVE_ABSENCE,
            difficulty=Difficulty.HARD,
            question="What are the combined security, operational, and compliance risks of the fleet lacking a security baseline or hardening standard?",
            reference_answer="The fleet has no security baseline or hardening standard, leading to systemic issues: (1) No CIS benchmarks applied — PostgreSQL, MariaDB, Docker, SSH all have CIS benchmarks that are not followed, (2) No security scanning (no container scanning, no SAST/DAST, no dependency scanning), (3) No mandatory access control (no AppArmor/SELinux), (4) No network segmentation (flat Docker bridges), (5) No encryption at rest, (6) Secrets in plaintext without rotation, (7) No audit logging (admin_audit_log: false, log_statement: none), (8) No WAF or edge security, (9) No vulnerability management process, (10) No compliance framework (SOC2, PCI-DSS, GDPR requirements unmet). The fleet would fail any security audit. Each individual gap is manageable, but the cumulative absence of a security baseline means security is ad-hoc and inconsistent across all layers.",
            evidence_locations=["yaml-app-feature-flags.yaml -> features.admin_audit_log", "pg-prod-primary.conf -> log_statement", "traefik-prod-static.yaml", "sshd-staging.conf -> PasswordAuthentication", "docker-daemon-dev.json -> insecure-registries"],
            reasoning_required="Holistic analysis of the fleet's security posture gaps and their cumulative impact",
        ),
    ]